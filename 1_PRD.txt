================================================================
1_PRD.txt — Purity MVP 제품 정의서 (구현 기능 기준)
최종 업데이트: 2026-04-29 (계정 페이지 전 포털 연동)
================================================================

## 서비스 개요

- 서비스명: Purity
- 성격: 전환율 최적화(CRO) 패널 서베이 SaaS MVP
- 목적: 기업이 자사 랜딩페이지/콘텐츠/가격 등을 실제 타겟 패널에게 검증받고,
         정량적 피드백(5개 축 점수)을 통해 마케팅 자산을 개선하도록 지원
- 사용자 역할: 3종 (company / panel / admin)

================================================================
## 역할 1: Company (기업)
================================================================

인증 후 /company 하위 라우트 접근 가능.

### [개요]
1. 대시보드 (/company) — Dashboard.jsx
   - Stats 카드 4개: 전체 의뢰 / 진행 중 / 완료 / 수집 피드백(제출 완료 건수)
   - 5차원 레이더 차트: feedbacks 실데이터 집계 → 내 점수 vs 플랫폼 벤치마크 비교
     (피드백 미수집 시 0점 표시, 수집 시 자동 반영)
   - KPI 달성률 게이지: 5축 평균을 100점 환산한 반원형 RadialBarChart
   - 긍/부정 스택 바 차트: 미션별 평균점수 ≥3.5 → 긍정 비율 수평 바 (최대 5개 미션)
   - 진행 중인 의뢰 카드 목록 + 전체 의뢰 현황 리스트
   - Framer Motion 애니메이션: 섹션 fadeUp / Stats staggerChildren / 미션 카드 whileHover
   ※ 차트 데이터 소스: feedbacks 테이블(status='submitted') 실시간 집계
     - computeRadar(fbs): 5개 score 컬럼 평균 → 레이더 + 게이지
     - computeSentiment(missions, fbs): 미션별 평균 ≥3.5 비율 → 스택 바

### [검증 도구]
2. 의뢰 등록 (/company/new) — NewMission.jsx
   - 제품명, LP URL 입력
   - 타겟 페르소나 설정 (산업군, 직책, 경력 등)
   - 패널 수 선택 → 보상금 자동 산출 (PRICE_PER 테이블 기반)
   - 검증 포인트(assets) 선택 (다중 선택)
   - 이미지 업로드 (최대 5장, 5MB 제한): Supabase Storage mission-assets 버킷
     · 업로드 경로: {company_id}/{missionUuid}/{uuid}.{ext}
     · 썸네일 미리보기 + 삭제 지원
     · missions.image_urls TEXT[] 컬럼에 public URL 배열 저장
   - 제출 시 Supabase missions 테이블에 INSERT (id 사전 생성 UUID), status='active'

3. 피드백 결과 (/company/results) — Results.jsx
   - 보유 미션 목록 표시 (missions 테이블 SELECT)
   - 미션 선택 시 해당 피드백 목록 표시 (feedbacks 테이블 SELECT)
   - 5개 축 평균 점수 시각화

4. 5차원 진단 (/company/diagnosis) — Diagnosis.jsx
   - clarity / relevance / value / differentiation / trust 축 레이더/바 차트

5. 소재 A/B 비교 (/company/preference) — PreferenceTest.jsx
   - 소재 유형 선택, variantA/variantB 텍스트 입력, 패널 수 & 보상 설정
   - 질문 템플릿 선택 (광고소재 category): 드롭다운으로 pre-선택 or 수동 선택
   - 등록 시: missions INSERT(type='preference', description=JSON.stringify({variantA,variantB})) + preference_tests INSERT(mission_id 연결)
   - 결과 화면: A/B 선택 비율 바 + message_clarity / purchase_intent 평균 표시

6. 가격 페이지 테스트 (/company/pricing-test) — PricingTest.jsx
   - 가격 페이지 설명 입력, 패널 수 설정
   - 질문 템플릿 선택 (가격 category)
   - 등록 시: missions INSERT(type='pricing') + pricing_tests INSERT(mission_id 연결)
   - 결과 화면: 구매 의향 비율 + price_fairness / value_perception 평균 + 4축 레이더

7. 이메일 검증 (/company/email-test) — ColdEmailTest.jsx
   - 이메일 원문 입력(제목+본문), 패널 수 설정
   - 질문 템플릿 선택 (이메일 category)
   - 등록 시: missions INSERT(type='email', description=emailText) + cold_email_tests INSERT(mission_id 연결)
   - 결과 화면: 답장 의향 비율 + open_intent / curiosity_score / hook_score / clarity_score 평균

8. 질문 템플릿 관리 (/company/templates) — QuestionTemplates.jsx
   - 탭 3개: 소재 비교 A/B | 가격 페이지 | 이메일 검증
   - 탭별 category 매핑: preference→'광고소재', pricing→'가격', email→'이메일'
   - 템플릿 카드 클릭 시 우측 사이드패널에 질문 목록 미리보기
   - "이 템플릿으로 의뢰하기" → 해당 서브 의뢰 페이지로 navigate(state: { templateId, templateName })
   - Sidebar: 서브 의뢰 그룹 최 하단에 위치 (메인 의뢰 그룹에서 이동)
   ※ 브랜드 포지셔닝 템플릿 삭제, category '광고' → '광고소재' / 'LP' → 'LP검증' 업데이트

### [인텔리전스]
9.  ICP 리서치 (/company/icp) — ICPResearch.jsx
10. ICP Pulse (/company/icp-pulse) — ICPPulse.jsx
11. 브랜드 트래킹 (/company/brand) — BrandTracking.jsx
12. AI 리포트 (/company/report) — AIReport.jsx
    - 피드백 데이터 기반 자동 리포트 (로컬 계산 폴백 + Anthropic AI 강화)
    - "AI 리포트 생성" 버튼 → generate-ai-report Edge Function 호출 → ai_reports 테이블 저장
    - 기존 AI 리포트 있으면 로드 시 자동 표시 (재생성 버튼으로 갱신 가능)
    - ANTHROPIC_API_KEY 미설정 시 로컬 계산 기반 Mock 응답으로 폴백 (서비스 중단 없음)

### [계정]
13. 내 계정 (/company/account) — Account.jsx  ★ 신규
    - 탭 1 기업 프로필: 기업명·업종·웹사이트 → companies UPDATE / 이메일 읽기전용
    - 탭 2 비밀번호 변경: signInWithPassword 검증 → supabase.auth.updateUser({ password })
    - 사이드바 '계정' 그룹 최상단에 위치

14. 팀 & 설정 (/company/settings) — Settings.jsx
    - 탭 팀 관리: team_members INSERT/UPDATE (초대·제거)
    - 탭 플랜 & 결제: companies.plan 표시 + invoices 결제 내역
    - 탭 알림 설정: 토글 상태 localStorage 저장 (키: purit_notif_prefs), 재방문 복원
    ※ 기업 프로필 탭 제거 → Account.jsx로 이전

15. 플랜 & 가격 (/company/plans) — Pricing.jsx
    - companies.plan Supabase 로드 → 현재 플랜 카드에 "현재 플랜" 배지 + 테두리 강조
    - CTA 버튼 클릭 → companies.plan UPDATE (starter/pro/enterprise)
    - 현재 사용 중 플랜 버튼 "사용 중" 표시·비활성화
    - 성공/실패 메시지 상단 표시

================================================================
## 역할 2: Panel (패널/응답자)
================================================================

인증 후 /panel 하위 라우트 접근 가능.

1. 대시보드 (/panel) — Dashboard.jsx
   - 수익 현황, 완료 미션 수 요약
   - 최신 활성 미션 5개 프리뷰 (missions SELECT + feedbacks(id) join, status='active', limit 5)
   - 미션 카드 클릭 시 ?id=${m.id} 전달 → 해당 미션 정확히 로드
   - 잔여 슬롯: feedbacks 실제 건수 기준 표시 (filled_count 대신)

2. 미션 탐색 (/panel/missions) — Missions.jsx
   - 탭 3개: 참여가능 / 진행 중(→/panel/active로 이동) / 완료
   - 참여가능: missions status='active' + 미참여(feedbackMap에 없음) + 슬롯 여유 있는 것
   - 완료: 패널이 submitted/approved/rejected 상태인 피드백이 있는 미션
   - 수락 확인 모달: Purit Filter 경고 3항목 포함
   - 수락 시 feedbacks INSERT(status='draft', 점수는 null) → /panel/active?id=... 이동
   - 수락 취소 모달: cancel_panel_feedback RPC 호출 (draft 삭제, feedbacks DELETE RLS 없어 RPC 필수)
   - Dashboard의 참여가능 목록에서는 draft 포함 모든 참여 미션 제외

3. 미션 수행 (/panel/active) — ActiveMission.jsx
   ★ 목록 뷰 (id 파라미터 없음, /panel/active):
     - 이 패널이 수락한(status='draft') 미션 전체를 카드 목록으로 표시
     - 저장된 진행 없으면 "피드백 시작하기 →", 있으면 "이어하기 →" 버튼
     - 버튼 클릭 시 /panel/active?id=<mission_id>로 이동
   ★ 폼 뷰 (id 파라미터 있음, /panel/active?id=...):
     - 특정 미션 브리핑 화면 → mission.type에 따라 3가지 폼 분기
     - 브리핑에서도 저장 여부에 따라 "이어하기 →" / "피드백 시작하기 →"
     ※ 서브 미션 폼 (type='preference'|'pricing'|'email', isSubMission=true):
       - preference: 두 소재(variantA/B) 나란히 표시 → A/B 선택 + message_clarity + purchase_intent + comment
       - pricing: 가격 페이지 설명 표시 → price_fairness + value_perception + would_buy + comment
       - email: 이메일 원문 표시 → open_intent + hook_score + clarity_score + curiosity_score + would_reply + comment
       - 제출: preference_responses/pricing_responses/email_responses INSERT + feedbacks UPDATE(submitted) + RPC 2개
     ※ 이미지 미션 (image_urls 있음, isSubMission=false):
       - ImageAnnotator 컴포넌트로 이미지 위에 드래그 영역 지정
       - 어노테이션 추가: dimension(5종) + score(1-5) + comment 입력
       - feedback_annotations 테이블에 실시간 저장/삭제
       - 제출 시 dimension별 평균점수 → clarity_score 등 컬럼에 반영
         suggestions = 어노테이션 요약 텍스트 통합
     ※ 텍스트 미션 (image_urls 없음, 하위 호환):
       - "이어하기" 클릭 시 점수가 비어있는 첫 번째 단계로 직접 이동 (resume)
       - 중복 제출 방지: 동일 mission+panel 조합 사전 체크
       - 5개 축 점수(1-5) 입력
       - 각 축 텍스트 코멘트 → [레이블]\n코멘트 형식으로 suggestions 하나에 통합 저장
         (strengths는 draft 중 JSON 임시 저장에 사용, 제출 시 null로 초기화)
       - 자동 저장: 점수/코멘트 변경 후 1.5초 debounce → feedbacks UPDATE
     - 수락 취소 버튼(draft 상태일 때) → cancel_panel_feedback RPC → /panel/missions 이동
     - 제출 시:
       a. feedbacks UPDATE (status: draft → submitted, strengths: null, suggestions 통합)
       b. missions.filled_count +1 → RPC increment_mission_filled_count (SECURITY DEFINER)
       c. panels.total_missions +1 → RPC increment_panel_mission_count (SECURITY DEFINER)

4. 수익 이력 (/panel/history) — History.jsx
   - 완료 피드백 목록 (feedbacks SELECT + missions JOIN → title, reward_amount)
   - draft 상태 피드백 제외 (.neq('status', 'draft'))

5. 프로필 설정 (/panel/profile) — Profile.jsx
   - 탭 기본 정보: 이름·직군·경력·자기소개 → panels UPDATE
   - 탭 전문 분야: expertise 배열 토글 → panels UPDATE
   - 탭 정산 계좌: bank_name·bank_account·bank_holder → panels UPDATE
   - 탭 비밀번호: signInWithPassword 검증 → supabase.auth.updateUser({ password })  ★ 신규

================================================================
## 역할 3: Admin (관리자)
================================================================

인증 후 /admin 하위 라우트 접근 가능.
(전체 Supabase 연동 완료 — 2026-04-23)

※ Admin 계정(purit.admin@gmail.com)은 3개 포털 모두 사용 가능
   - panels + companies 레코드 수동 생성 완료 (SQL 003_admin_records.sql)
   - 사이드바 상단 포털 전환 버튼(어드민/기업/패널) 표시

1. 플랫폼 대시보드 (/admin) — Dashboard.jsx
2. 패널 관리 (/admin/panels) — PanelManagement.jsx
3. 미션 관리 (/admin/missions) — Missions.jsx
   - 미션 목록에 유형 뱃지 표시: LP 검증 / 소재 비교 / 가격 검증 / 이메일 검증

4. Purity Filter (/admin/purity) — PurityFilter.jsx
   - 피드백 품질 필터링 / purity_passed 관리
   - 이미지 미션 피드백: ImageAnnotator(readonly) 로 어노테이션 오버레이 확인
     다중 이미지 탭 + 어노테이션 건수 배지 표시
   - 서브 미션(preference/pricing/email) 피드백: missions.type 감지 → 해당 응답 테이블 로드
     - preference: preference_responses에서 A/B 선택 + message_clarity + purchase_intent + comment 표시
     - pricing: pricing_responses에서 would_buy + price_fairness + value_perception + key_comment 표시
     - email: email_responses에서 would_reply + hook/clarity/open_intent/curiosity 점수 + comment 표시
   - Purit Score: 서브 미션은 calcSubPurityScore (코멘트 충실도 + 지표 충실도 기반)
   - 승인/반려: feedbacks.status 업데이트 (서브 미션도 동일)
5. 수익 & 정산 (/admin/revenue) — Revenue.jsx
6. 내 계정 (/admin/account) — Account.jsx  ★ 신규
   - 탭 관리자 정보: 이름 → supabase.auth.updateUser({ data: { name } }) / 이메일 읽기전용
   - 탭 비밀번호 변경: signInWithPassword 검증 → supabase.auth.updateUser({ password })

================================================================
## 인증 흐름
================================================================

- 방식: Supabase Auth (이메일 + 패스워드)
- 회원가입: role 선택(company/panel) → signUp 호출 →
             DB 트리거(handle_new_user)가 panels 또는 companies 테이블에 레코드 자동 생성
- 로그인: signInWithPassword → user_metadata.role로 역할 판별 → 역할별 대시보드 리다이렉트
- 세션: getSession() 초기 로드 + onAuthStateChange 실시간 구독
- 접근 제어: RoleRoute 컴포넌트 — 비인증 시 /login 리다이렉트,
             역할 불일치 시 해당 역할 홈으로 리다이렉트

================================================================
## 공용 라우트
================================================================

- / : Landing.jsx (공개)
- /login : Login.jsx (공개)
- /signup : Signup.jsx (공개, role 선택 포함)
- /*/notifications : NotificationCenter.jsx (역할별 알림, 각 포털 공유)
