================================================================
1_PRD.txt — Purity MVP 제품 정의서 (구현 기능 기준)
최종 업데이트: 2026-05-06 (의뢰 임시 저장 + 목록 탭 추가 — 전체/진행/완료/임시저장 탭, 나가기 임시 저장 모달)
※ 워딩 규칙: 기업(Company) 포털 UI 표시 텍스트는 "의뢰" 사용.
             패널·어드민 포털 및 코드 내부(변수명·DB컬럼·라우트)는 "미션/mission" 유지.
================================================================

## 서비스 개요

- 서비스명: Purity
- 성격: 전환율 최적화(CRO) 패널 서베이 SaaS MVP
- 목적: 기업이 자사 랜딩페이지/콘텐츠/가격 등을 실제 타겟 패널에게 검증받고,
         정량적 피드백(5개 축 점수)을 통해 마케팅 자산을 개선하도록 지원
- 사용자 역할: 3종 (company / panel / admin)

================================================================
## 역할 1: Company (기업)
================================================================

인증 후 /company 하위 라우트 접근 가능.

### [개요]
1. 대시보드 (/company) — Dashboard.jsx
   - NDA 배너 최상단 표시: "Purit의 피드백 패널은 기업의 정보를 발설할 수 없습니다."
     × 클릭 시 localStorage(purit_nda_banner_dismissed) 저장 → 24시간 비노출
   - Stats 카드 5개: 잔여 크레딧(현재 플랜 표시) / 전체 의뢰 / 진행 중 / 완료 / 수집 피드백(제출 완료 건수)
     · 잔여 크레딧 카드: company.credit_balance 표시, 서브타이틀에 현재 플랜명(STARTER/PRO 등) 표시
     · credit_balance = 0 이면 기본 색, > 0 이면 accent 색으로 강조
   - 5차원 레이더 차트: feedbacks 실데이터 집계 → 내 점수 vs 플랫폼 벤치마크 비교
     (피드백 미수집 시 0점 표시, 수집 시 자동 반영)
   - KPI 달성률 게이지: 5축 평균을 100점 환산한 반원형 RadialBarChart
   - 긍/부정 스택 바 차트: 의뢰별 평균점수 ≥3.5 → 긍정 비율 수평 바 (최대 5개 의뢰)
   - 전체 의뢰 현황 (통합 섹션): ★ 개편
     · "진행 중인 의뢰" 별도 섹션 제거 → "전체 의뢰 현황"에 통합
     · 탭 5개: 전체 / 진행 / 완료 / 임시 저장 / 취소 ★ 임시 저장 탭 추가
     · 메인 의뢰(LP검증) / 서브 의뢰(소재비교·가격검증·이메일검증) 섹션 분리
     · 각 섹션 10개씩 페이지네이션 (CompanyMissionCard 컴포넌트)
     · CompanyMissionCard: 카드 클릭 시 navigate('/company/results?id=<missionId>') → Results.jsx가 해당 의뢰 즉시 선택
     · CompanyMissionCard: status='active' && filled_count=0 조건 시 "수정" 버튼 표시
       클릭 시 navigate('/company/new', { state: { editMode: true, missionId } }) 이동
     · CompanyMissionCard: status='draft' 카드 클릭 시 DRAFT_ROUTE 맵으로 해당 폼으로 이동
       (landing_page→/company/new, preference→/company/preference, pricing→/company/pricing-test, email→/company/email-test)
       draft 카드 우측에 "이어 작성하기 →" 버튼 표시, 카드 테두리 dashed amber
   - Framer Motion 애니메이션: 섹션 fadeUp / Stats staggerChildren
   ※ 차트 데이터 소스: feedbacks 테이블(status='submitted') 실시간 집계
     - computeRadar(fbs): 5개 score 컬럼 평균 → 레이더 + 게이지
     - computeSentiment(missions, fbs): 의뢰별 평균 ≥3.5 비율 → 스택 바

### [검증 도구]
2. 의뢰 등록/수정 (/company/new) — NewMission.jsx  ★ 소개 페이지 + Step 개편 + 임시 저장 (2026-05-06)
   - 목록 뷰 (list): /company/new 진입 시 '마케팅 소재 종합 진단' 소개 페이지 먼저 표시
     · 설명: "랜딩페이지, 광고 소재, 배너 등을 실제 타겟 패널이 종합적으로 진단합니다."
     · 특징 카드 4개 (이미지 업로드·영역 어노테이션·5차원 평가·추가 질문)
     · 기존 의뢰 목록 탭 4개: 전체 / 진행 / 완료 / 임시 저장 ★ 신규
     · 임시 저장 카드 클릭 → form 뷰 진입 + 기존 데이터 복원
     · 서브 의뢰 페이지들(PreferenceTest 등)과 동일한 list/form 뷰 전환 패턴
   - 편집 모드(location.state.editMode=true): list 뷰 건너뛰고 form 뷰 바로 진입
     · description JSON 파싱으로 전체 필드 복원: product/lpUrl/briefText/panels/focusAreas/imageUrls
       + industry/personaAge/personaIncome/personaRole/personaContext/selectedQuestions/careerLevels/step
     · isDraftMode: ms.status==='draft'일 때 true → 제출 시 status='active'+크레딧예약 처리
     · 제출 시 missions INSERT → UPDATE(editMode) 분기
     · Step 4 검토 화면에 ⚠️ "첫 피드백 수신 후 수정 불가" 경고 표시
     · 제출 버튼: 신규="의뢰 제출 →" / 편집="수정 완료 →"
   - 임시 저장 (draft) 기능 ★ 신규
     · form 뷰에서 "취소/목록으로" 버튼 클릭 시 shouldBlockNav 조건이면 3버튼 모달 표시
       (showDraftModal state — useBlocker는 <BrowserRouter> 미지원으로 사용 불가, D-42 참조)
     · "임시 저장 후 나가기" → missions INSERT/UPDATE(status='draft') → list 뷰 전환
     · "저장 없이 나가기" → 즉시 list 뷰 전환
     · "계속 작성하기" → 모달 닫기
     · window.beforeunload: 브라우저 탭 닫기/새로고침 시 기본 확인 다이얼로그 (라우터 없이 동작)
     · shouldBlockNav: product || lpUrl || briefText || imageUrls.length > 0 조건
     · isDraftMode=false인 editMode(active 미션 수정)는 임시 저장 인터셉트 없음
   - 상단 배너 2개 (form 뷰 모든 단계 상시 표시):
     · 🔒 NDA 배너: "평가 참가 패널은 기업의 정보를 외부에 발설할 수 없습니다."
     · ✨ 매칭 배너: "의뢰 조건에 맞는 패널이 자동으로 매칭됩니다."
   - Step 0 서비스 정보 & 타겟 페르소나 (구 '페르소나 설정'): ★ 개편
     · 서브타이틀: "검증할 서비스와 서비스 타겟에 대해 설정합니다."
     · 상단: 제품/서비스명 입력 → 바로 아래 산업군 선택 → 랜딩페이지 URL 입력
     · 산업군 선택: ▼ 토글 드롭다운 — 20개 산업군 칩(뷰티/코스메틱·헬스/피트니스·SaaS 등) + "✏️ 직접 쓰기" 모드
       선택값 → missions.description JSON의 `industry` 필드 저장 / persona 문자열에도 "산업군: XXX" 포함
       패널·어드민 화면에는 별도 표시 없음 (기업 등록 정보로만 활용)
     · 구분선 아래: 연령대, 월 소득, 직군/역할 + '타겟 상세(선택)' 자유 입력
   - Step 1 소재 업로드: 이미지 업로드(최대 3장, 5MB) + 검증 포인트(FOCUS) 선택 + 브리핑
     · 제품/서비스명·LP URL 입력 필드 제거 (Step 0으로 이동)
     · FOCUS 항목: 첫인상/가독성, CTA 전환율, 가격 및 가치 전달, 신뢰 요소,
                   모바일 최적화, 핵심 메시지 명확성, 비주얼 완성도, 타겟 일치도
     · 업로드 경로: {company_id}/{missionUuid}/{uuid}.{ext}
     · missions.image_urls TEXT[] 컬럼에 public URL 배열 저장
   - Step 2 질문 설정:
     · 상단: "내 커스텀 질문" 아코디언 — DB category='랜딩페이지' 저장 질문 자동 로드 (customLPQs)
     · QUESTION_TEMPLATES.lp 3개 템플릿(랜딩페이지 카테고리) 아코디언
     · 최대 5개 질문 선택 / 서술형(text) 최대 2개 한도
     · "N/5 선택됨" 배지 실시간 표시 + 하단 선택 미리보기 카드
     · 질문 선택 시 description = JSON.stringify({ briefText, selectedQuestions, careerLevels })
     · "질문 만들기" 인라인 섹션 (하단 Card): ★ 신규
       - textarea 질문 입력 + 타입 버튼 (옵션형/점수형/서술형)
       - 옵션형: 옵션 입력 필드 동적 추가 / 점수형: min·max 레이블 입력
       - "추가" 버튼: localCustomQs state에 추가 (DB 저장 없음, 현재 폼에만 사용)
       - "템플릿에 저장 →" 버튼: ConfirmModal 확인 → DB INSERT (category='랜딩페이지') → customLPQs 갱신
     · location.state.templateId + templateName 전달 시 자동 진입: ★ 신규
       - initTemplateId/initTemplateName 읽어 QUESTION_TEMPLATES.lp 중 일치 템플릿 찾기
       - 찾은 템플릿의 questions 사전 selectedQuestions에 설정 + 해당 아코디언 펼침 + Step 2 바로 이동
   - Step 3 패널 설정 (PanelTargetStep 컴포넌트): ★ 신규
     · 패널 인원수 슬라이더 (10~30명, 기본 10명)
       - 스타터 플랜: 15명 초과 드래그 후 마우스 놓으면 15명으로 snap-back + 업그레이드 팝업
     · 경력/직급 다중 선택 버튼: 주니어(1.0×) / 미들(1.5×) / 시니어(2.0×, PRO) / C레벨(3.0×, PRO)
       - 스타터 플랜이 시니어·C레벨 클릭 시 체크 없이 업그레이드 팝업만 표시
       - PRO 뱃지: 금빛 shimmer 애니메이션
     · 실시간 크레딧 계산기 (하단 sticky): ceil(패널수 × 최종직급가중치 × 의뢰종류배수)
       - 최종직급가중치: 1~2개 선택 시 최고 배수 / 3~4개 선택 시 혼합상한 1.8 고정
       - 의뢰종류배수: 메인 1.5× / 서브 1.0×
       - 표시 레이블: "최대 예상 소모: N 크레딧"
       - 하단 안내문: "실제 매칭된 패널의 직급 비율에 따라 소모량은 줄어들 수 있으며, 사용되지 않은 차액 크레딧은 테스트 완료 후 즉시 환불(Refund)됩니다."
     · 업그레이드 팝업: ReactDOM.createPortal — 폼 데이터 초기화 없이 현재 화면 위에만 표시
                        [Pro 플랜 전환하기] → /company/plans 이동
   - Step 4 검토: 패널 수·커리어 레벨·예상 크레딧 행 포함, '의뢰 제출 →' 버튼
   - 제출 시 Supabase missions 테이블에 INSERT (id 사전 생성 UUID), status='active'
   - reward_amount = calcPanelPayout(careerLevels, 'main') — 패널 1인당 예상 정산금 (메인 주니어 기준 8,000원)

3. 피드백 결과 (/company/results) — Results.jsx  ★ 서브 미션 제출 소재 표시 + 더보기 토글 추가 (2026-05-04)
   - 레이아웃: 좌측 230px 패널(미션 선택) + 우측 콘텐츠 2열 구조
   - 좌측 패널: 메인 의뢰 / 서브 의뢰 섹션 분리, 타입 뱃지 + 피드백 수 표시
   - 도움 됨/안 됨 평가 버튼 (HelpfulnessButtons): 총평 섹션에만 표시
     · 기업이 패널의 총평 코멘트에 👍도움 됨 / 👎도움 안 됨 평가 (선택사항)
     · 낙관적 UI: 클릭 즉시 반영 + rate_panel_feedback_helpfulness RPC 호출 + 에러 시 롤백
     · 같은 버튼 재클릭 → 평가 취소(토글 오프) / 반대 버튼 클릭 → 평가 변경
     · helpRatings 맵(`${refType}:${refId}` → 'helpful'|'unhelpful'|null)으로 선택 상태 관리
     · ratingInFlight Set으로 동시 클릭 방지
     · 미션 변경(selected) 시 helpRatings 초기화 후 배치 재로드 (feedback_helpfulness_ratings 조회)
     · 표시 위치 (총평 전용):
       - 이미지 미션 종합 탭: 패널 총평 목록(SummaryTabView) — refType="feedback"
       - 서브미션 총 평가 코멘트: preference/pricing/email 각 총 평가 섹션 — refType=미션타입
       - 텍스트 미션: 패널 피드백 요약(TextMissionResults) — refType="feedback"
       ※ 차원 탭 어노테이션 항목에는 표시 안 함 (총평 아님)
   - 우측 콘텐츠: mission.type 및 image_urls 유무에 따라 3가지 분기
     A. 이미지 미션 (image_urls 존재 && !isSubMission):
        · 6개 탭: [명확성][관련성][가치][차별화][신뢰][종합]
        · 차원 탭 (DimTabView): ImageAnnotator(readonly)로 전체 패널 어노테이션 오버레이 + 코멘트 목록 ★ 개편
          - 어노테이션 번호: 한국어 접두사 제거 → 숫자만 표시 (색상으로 차원 구분)
          - 번호 기준: 글로벌(차원 전체 이미지 합산) — 이미지 전환 시에도 번호가 이어짐 (seqPool 방식)
          - 코멘트 클릭: 목록에서 항목 클릭 시 해당 어노테이션 박스만 이미지에 표시, 나머지 숨김
                         클릭한 이미지로 자동 전환 / 다시 클릭하면 전체 복원 (토글)
        · 종합 탭: 5차원 평균 점수 카드 + RadarChart + 패널 총평 목록 + 도움됨 버튼
          - 총평 파싱: extractOverallComment() → feedbacks.suggestions의 [총평]\n 이후 텍스트
          - LP 추가 질문 있으면 하단에 CustomQuestionsSection 집계 표시
        · feedback_annotations: eq('mission_id') 전체 로드 → 클라이언트에서 dimension 필터
     B. 서브 미션 (type: preference/pricing/email):
        · preference: 제출된 소재 A/B 카드(2열) 상단 표시 → 이미지 있으면 표시, 텍스트는 ExpandableText(더보기/접기) → A/B 선택 비율 바 + message_clarity/purchase_intent 평균 카드
        · pricing: 가격 구성 카드(이미지 + ExpandableText 텍스트) → would_buy 비율 바 + price_fairness/value_perception 평균 카드
        · email: 이메일 원문 카드(ExpandableText, limit=250) → would_reply 비율 바 + open_intent/hook_score/clarity_score/curiosity_score 카드
        · 각 타입 공통: CustomQuestionsSection → 하단에 배치 (점수 카드 아래, 패널 코멘트 위)
          - 질문 헤더 행 (항상 표시): 타입 뱃지(옵션형/점수형/서술형) + 번호. 질문 텍스트 + 응답 수 + ▼ 토글
          - 기본 접힘 → 클릭 시 펼쳐짐: 옵션형=비율 바, 점수형=평균 점수 카드, 서술형=답변 목록
          - custom_answers가 없는 레거시 응답은 자동 제외
        · 패널 코멘트(총 평가) 섹션: CustomQuestionsSection 아래 최하단 배치 + 도움됨 버튼
        · 질문 타입 뱃지 통일: 옵션형(radio) / 점수형(scale) / 서술형(text)
     C. 텍스트 미션 (image_urls 없음 && !isSubMission):
        · 패널 목록 + 상세 뷰 + 하단 CustomQuestionsSection 집계 (LP 추가 질문 있을 때) + 도움됨 버튼
   - 공유 링크: 우측 미션 헤더 영역 배치 (missions.share_token 생성/복사/해제)
   - 패널 직군/경력 뱃지: 각 피드백 항목 옆에 패널의 직군(blue)·경력(gray) 뱃지 표시
     · 이름·전화번호 비공개 — get_panel_public_profiles RPC(SECURITY DEFINER) 경유
     · 표시 위치: 종합 탭 총평, 차원 탭 어노테이션, 텍스트 미션 패널 목록,
                  소재비교·가격검증·이메일검증 코멘트 항목
   - ※ panels 조인 없음 (RLS 차단 원칙 D-28 준수)

4. 5차원 진단 (/company/diagnosis) — Diagnosis.jsx
   - clarity / relevance / value / differentiation / trust 축 레이더/바 차트
   - "개선 가이드 보기 →" 버튼: 가장 취약한 차원 감지 → 맞춤 개선 팁 5개 모달 표시
     (IMPROVEMENT_GUIDES 상수: 5개 차원별 실행 가능한 액션 아이템 정의)
   - 탭 '키워드 분석' 추가: feedbacks suggestions/strengths/weaknesses 텍스트에서
     한국어 불용어 제거 후 단어 빈도 분석 → 태그 클라우드 + 상위 10개 빈도 바

4.5 시계열 추적 (/company/timeline) — TimelineTracker.jsx
   - Diagnosis.jsx '시계열 추적' 탭 클릭 시 /company/timeline으로 navigate
   - 사이드바 메뉴에서는 제거됨 (Diagnosis 탭 링크로 흡수)
   - 의뢰별·주간 뷰 전환 (주간/의뢰별 토글)
   - 의뢰 칩 토글로 비교할 의뢰 선택
   - Recharts LineChart: 5차원 점수 시계열 변화
   - 하단 요약 테이블 (점수 색상 코딩)

5. 소재 A/B 비교 (/company/preference) — PreferenceTest.jsx  ★ 임시 저장 + 목록 탭 추가 (2026-05-06)
   - 목록: missions 테이블에서 type='preference' 필터로 로드
     · 목록 탭 4개: 전체 / 진행 / 완료 / 임시 저장 ★ 신규
     · 임시 저장 카드 클릭 → form 뷰 진입 + draftId 설정 + description JSON 복원
     · 목록 카드 클릭 → navigate('/company/results?id=…') / "+ 새 테스트" 버튼 표시
   - 목록 빈 상태: 중앙 "+ 새 테스트" 버튼 하나만 표시
   - 임시 저장 기능: useBlocker + showDraftModal + window.beforeunload (NewMission과 동일 패턴)
     · hasUnsavedChanges: variantA || variantB || productDescription
     · draftId 상태로 draft 이어쓰기 추적, 제출 시 draftId 있으면 UPDATE + active 전환 + 크레딧 예약
   - 폼 상단 배너 2개 (모든 스텝 상시): 🔒 NDA 배너 + ✨ 매칭 배너
   - Step 0: 소재 유형 선택 (이미지/텍스트/랜딩페이지)
   - Step 1: 소재 A/B 입력 + 이미지 업로드 (선택, 각 1장)
     · variantA / variantB 텍스트 필수, 업로드 경로: {companyId}/{missionUuid}/va.{ext} / vb.{ext}
   - Step 2: 제품/타겟 설명 입력 (productDescription) + 산업군 선택 (industry)
     · 산업군: ▼ 드롭다운 — 20개 칩 + "✏️ 직접 쓰기" 모드 (선택 시 description JSON에 industry 저장)
   - Step 3: 질문 설정
     · 아코디언 그룹 1 — 내 커스텀 질문 / 그룹 2~4 — QUESTION_TEMPLATES.preference 3종
     · 최대 5개 / 서술형 최대 2개 / 질문 만들기 → localCustomQs 또는 템플릿 저장
   - Step 4: 패널 설정 (PanelTargetStep — NewMission Step 3과 동일 컴포넌트)
   - 등록 시: missions INSERT(type='preference', description=JSON.stringify({
       variantA, variantB, variantAImage, variantBImage,
       productDescription, industry, selectedQuestions, careerLevels})) + preference_tests INSERT
   - reward_amount = calcPanelPayout(careerLevels, 'sub') — 패널 1인당 예상 정산금 (서브 주니어 기준 4,500원)
   - 결과 화면: A/B 선택 비율 바 + message_clarity / purchase_intent 평균 + CustomQuestionsSection

6. 가격 페이지 테스트 (/company/pricing-test) — PricingTest.jsx  ★ 임시 저장 + 목록 탭 추가 (2026-05-06)
   - 목록: missions 테이블에서 type='pricing' 필터로 로드
     · 목록 탭 4개: 전체 / 진행 / 완료 / 임시 저장 ★ 신규
     · 임시 저장 카드 클릭 → form 뷰 + draftId 설정 + description JSON 복원
     · 목록 카드 클릭 → navigate('/company/results?id=…') / "+ 새 테스트" 버튼 표시
   - 목록 빈 상태: 중앙 "+ 새 테스트" 버튼 하나만 표시
   - 임시 저장 기능: hasUnsavedChanges = pricingDesc, draftId 패턴 (PreferenceTest와 동일)
   - 폼 상단 배너 2개 (모든 스텝 상시): 🔒 NDA 배너 + ✨ 매칭 배너
   - Step 0: 가격 페이지 입력 + 이미지 업로드 (선택, 1장)
     · 업로드 경로: {companyId}/{missionUuid}/pricing.{ext}
   - Step 1: 제품/타겟 설명 입력 (productDescription) + 산업군 선택 (industry)
     · 산업군: ▼ 드롭다운 — 20개 칩 + "✏️ 직접 쓰기" 모드 (선택 시 description JSON에 industry 저장)
   - Step 2: 질문 설정 (category='가격', PreferenceTest Step 3과 동일 구조)
   - Step 3: 패널 설정 (PanelTargetStep)
   - 등록 시: missions INSERT(type='pricing', description=JSON.stringify({
       content: pricingDesc, image, productDescription, industry,
       selectedQuestions, careerLevels})) + pricing_tests INSERT
   - reward_amount = calcPanelPayout(careerLevels, 'sub') — 패널 1인당 예상 정산금 (서브 주니어 기준 4,500원)
   - 결과 화면: 구매 의향 비율 + price_fairness / value_perception 평균 + CustomQuestionsSection

7. 이메일 검증 (/company/email-test) — ColdEmailTest.jsx  ★ 임시 저장 + 목록 탭 추가 (2026-05-06)
   - 목록: missions 테이블에서 type='email' 필터로 로드 (cold_email_tests 테이블 의존 제거)
     · 목록 탭 4개: 전체 / 진행 / 완료 / 임시 저장 ★ 신규
     · 임시 저장 카드 클릭 → form 뷰 + draftId 설정 + description JSON 복원
     · 목록 카드 클릭 → navigate('/company/results?id=…')
     · 미션 있을 때 우측 상단 "+ 새 테스트" 버튼 표시
   - 목록 빈 상태: 중앙 "+ 새 테스트" 버튼 하나만 표시
   - 임시 저장 기능: hasUnsavedChanges = emailText, draftId 패턴 (PreferenceTest와 동일)
   - 폼 상단 배너 2개 (모든 스텝 상시): 🔒 NDA 배너 + ✨ 매칭 배너
   - Step 0: 이메일 원문 설명 입력
   - Step 1: 제품/타겟 설명 입력 (productDescription) + 산업군 선택 (industry)
     · 산업군: ▼ 드롭다운 — 20개 칩 + "✏️ 직접 쓰기" 모드 (선택 시 description JSON에 industry 저장)
   - Step 2: 질문 설정 (category='이메일', PricingTest Step 2와 동일 구조)
   - Step 3: 패널 설정 (PanelTargetStep)
   - 등록 시: missions INSERT(type='email', title='이메일 검증'(고정), description=JSON.stringify({
       content: emailText, productDescription, industry,
       selectedQuestions, careerLevels})) + cold_email_tests INSERT
   - reward_amount = calcPanelPayout(careerLevels, 'sub') — 패널 1인당 예상 정산금 (서브 주니어 기준 4,500원)
   - 결과 화면: 답장 의향 비율 + open_intent / curiosity_score / hook_score / clarity_score 평균 + CustomQuestionsSection

8. 질문 템플릿 관리 (/company/templates) — QuestionTemplates.jsx  ★ 마케팅 소재 종합 진단 탭 추가 (2026-05-03)
   - 탭 5개: 마케팅 소재 종합 진단(🖼) | 소재 비교 A/B | 가격 페이지 | 이메일 검증 | 질문 만들기(✏️)
   - 기본 선택 탭: '마케팅 소재 종합 진단'
   - 탭 1~4 (템플릿 탭):
     · 탭별 category 매핑: lp→'랜딩페이지', preference→'광고소재', pricing→'가격', email→'이메일'
     · 템플릿 카드: 클릭 비활성 — "미리보기" 버튼 클릭 시에만 우측 사이드패널 열림
     · DB query: template_questions(id, question_text, question_order, question_type) 로드
     · DB 데이터 없으면 src/lib/templates.js 로컬 TEMPLATE_BY_NAME 자동 폴백
     · 각 질문에 [옵션형]/[점수형]/[서술형] 타입 뱃지 표시
     · 탭 설명 영역 우측: "바로 의뢰 등록 →" 버튼 → 해당 의뢰 페이지 navigate
       (마케팅 소재 종합 진단 탭 → /company/new)
     · 우측 미리보기 패널 상단: "이 템플릿으로 의뢰 등록 →" 버튼 (전체 탭 공통) ★ 신규
       - 클릭 시 navigate(tab.path, { state: { templateId, templateName } }) 전달
       - DB 템플릿(UUID): use_count +1 UPDATE 후 이동 / 로컬 템플릿(t-lp-*): use_count 업데이트 없이 바로 이동
       - NewMission 진입 시 해당 LP 템플릿 질문 자동 사전 선택 + Step 2 직행
     · LP 탭(마케팅 소재 종합 진단) DB 폴백 (D-31 패턴): ★ 신규
       - DB에 category='랜딩페이지' is_default=true 행이 없으면 QUESTION_TEMPLATES.lp 로컬 상수로 자동 보완
       - 미리보기 질문 목록: DB template_questions 없으면 TEMPLATE_BY_NAME[name].questions 폴백
   - 탭 5 "질문 만들기":
     · 카테고리 선택 버튼 4개 (마케팅 소재 종합 진단 / 소재 비교 A/B / 가격 페이지 / 이메일 검증)
     · 저장된 커스텀 질문 목록: DB에서 is_default=false, 선택 category로 로드
     · 질문 빌더 Card: textarea + 타입 선택(옵션형/점수형/서술형) + 타입별 옵션 UI
     · "질문 추가 →" 버튼: DB INSERT → 저장된 커스텀 질문 목록 갱신
       - companyId 미로드 시 버튼 disabled (비동기 가드)
       - 저장 실패 시 버튼 아래 에러 메시지 표시 (saveError state)
     · category='랜딩페이지'로 저장된 커스텀 질문은 NewMission Step 2 "내 커스텀 질문" 그룹에 자동 표시
     · RLS 필요: 013_custom_question_rls.sql Supabase SQL Editor에서 수동 실행 필요
   - Sidebar: 메인 의뢰 그룹 맨 하단에 위치 (서브 의뢰 그룹에서 이동) ★ 변경
   ※ 전체 기본 템플릿 12종: 랜딩페이지(첫인상&전환신호·메시지명확성&공감도·신뢰&전환저해요인),
                             광고소재(광고 소재 A/B·첫인상&전환력·메시지 공명 분석),
                             가격(가격 페이지 검증·가격 감지 테스트·플랜 비교 명확성),
                             이메일(콜드메일 효과 측정·개봉률 최적화·콜드 리치아웃 효과)

### [인텔리전스]
9.  ICP 리서치 (/company/icp) — ICPResearch.jsx
10. ICP Pulse (/company/icp-pulse) — ICPPulse.jsx
11. 브랜드 트래킹 (/company/brand) — BrandTracking.jsx
12. AI 리포트 (/company/report) — AIReport.jsx
    - 피드백 데이터 기반 자동 리포트 (로컬 계산 폴백 + Anthropic AI 강화)
    - "AI 리포트 생성" 버튼 → generate-ai-report Edge Function 호출 → ai_reports 테이블 저장
    - 기존 AI 리포트 있으면 로드 시 자동 표시 (재생성 버튼으로 갱신 가능)
    - ANTHROPIC_API_KEY 미설정 시 로컬 계산 기반 Mock 응답으로 폴백 (서비스 중단 없음)

### [계정]
13. 내 계정 (/company/account) — Account.jsx  ★ 신규
    - 탭 1 기업 프로필: 기업명·업종·웹사이트 → companies UPDATE / 이메일 읽기전용
    - 탭 2 비밀번호 변경: signInWithPassword 검증 → supabase.auth.updateUser({ password })
    - 사이드바 '계정' 그룹 최상단에 위치

14. 팀 & 설정 (/company/settings) — Settings.jsx
    - 탭 팀 관리: team_members INSERT/UPDATE (초대·제거)
    - 탭 플랜 & 결제: companies.plan 표시 + invoices 결제 내역
      "플랜 변경" 버튼 → /company/plans 이동
    - 탭 알림 설정: 토글 상태 localStorage 저장 (키: purit_notif_prefs), 재방문 복원
    ※ 기업 프로필 탭 제거 → Account.jsx로 이전

15. 플랜 & 가격 (/company/plans) — Pricing.jsx  ★ 크레딧 연동 (2026-05-04)
    - companies.plan Supabase 로드 → 현재 플랜 카드에 "현재 플랜" 배지 + 테두리 강조
    - CTA 버튼 클릭 → grant_plan_credits RPC 호출 (plan + credit_balance 원자적 업데이트)
      · Starter 선택 시 credit_balance = 50, Pro = 165, Enterprise = 400 으로 SET
    - 현재 사용 중 플랜 버튼 "사용 중" 표시·비활성화
    - 성공/실패 메시지 상단 표시
    - Enterprise "영업팀 문의" 버튼: 문의 모달 표시
      · 이메일 연락처(enterprise@purit.io) + 자유 문의 textarea
      · 제출 시 notifications 테이블에 접수 기록 INSERT
      · 완료 후 2.5초 뒤 모달 자동 닫힘
    - 크레딧 기반 구독 모델 (B2B):
      · Starter: 월 82만(무약정) / 68만(연간), 월 50크레딧, 패널 10~15명, 주니어·미들급(1~7년차)
        - 추가 크레딧 구매 가능 (1크레딧 = 25,000원 정가)
      · Pro:     월 238만(무약정) / 198만(연간), 월 165크레딧, 패널 10~30명, 시니어·C레벨
        - 추가 크레딧 구매 시 14% 할인 (1크레딧 = 21,600원)
      · Enterprise: 월 450만~(연간 전용·협의), 월 400+크레딧, 패널 무제한 커스텀
        - 특정 회사·산업군 핀셋 필터링, 전담 CSM 배정
    - 플랜 카드 UI: 월 크레딧 수 + 회당 패널 규모 스펙 그리드 + 타겟팅 권한 배지 표시
    - 연간/월간 결제 토글: 연간 선택 시 절감액(Starter 14만, Pro 40만) 표시
    - 추가 옵션 섹션: 추가 크레딧 충전(Pro/Starter 단가 비교), ICP Pulse 분기 구독

================================================================
## 역할 2: Panel (패널/응답자)
================================================================

인증 후 /panel 하위 라우트 접근 가능.

1. 대시보드 (/panel) — Dashboard.jsx  ★ 명예 레벨 시스템 추가 (2026-05-02)
   - 명예 레벨 카드 (최상단): 10단계 HP 기반 레벨 표시
     · Lv.X 헤더 + 진행률 바(colorMeta.color) + "다음 레벨까지 XXX점" + "다음 레벨 달성 시 1회당 ₩XXX을 받습니다!" 동기 문구
     · 최고 레벨(Lv.10) 달성 시: "🏆 최고 레벨 달성! 현재 1회당 ₩XXX"
     · 레벨 라벨 옆 ⓘ 배지: 마우스 호버 시 HP 규칙 툴팁 표시
       (미션 제출 완료 +5pts / 기업 도움 됨 +15pts / 기업 도움 안 됨 −20pts / 30일 비활동 후 매주 −200pts)
     · 마운트 시 apply_honor_decay RPC 호출 → decay 적용 시 패널 데이터 즉시 재로드
   - 신뢰도 게이지 (trust_score %)
   - 미션 현황 (3칸 묶음): 완료 미션(총 누적) / 참여 가능(슬롯 여유 있는 active 미션) / 이번 주 활동(7일 내 제출)
     · 참여 가능 수: status='active' + 슬롯 여유(filled_count < panel_count) + 미참여 미션 전수 조회 (limit 없음)
   - 수익 현황 (3칸 묶음): 총 정산 완료(금액+건수) / 정산 대기 중(금액+건수) / 필터 탈락(건수)
     · feedbacks(status≠draft) SELECT → purity_passed/status 기준 집계
     · 금액 표시: fmtWon() (honorLevels.js) — 만 단위 + 나머지 천원 단위 (예: ₩1만 5,000)
   - 헤더 배지: Lv.X · 라벨 (colorMeta 스타일) + XXXpts 표시 + 🔥 연속 횟수
   - 참여 가능한 미션 카드 목록 제거 (→ 미션 탐색 페이지로 유도)

2. 미션 관리 (/panel/missions) — Missions.jsx  ★ 전면 재설계 (미션 탐색 + 진행 중 통합)
   - 탭 3개: 새로운 미션 / 진행 중 / 수정 필요
     · "수정 필요" 탭: 반려된 피드백 건수를 빨간 숫자 뱃지로 표시 (0건이면 비노출)
   - [새로운 미션]: missions status='active' + 미참여(feedbackMap에 없음) + 슬롯 여유 있는 것
   - [진행 중]: 이 패널이 수락한(status='draft') 미션 목록 — /panel/active 목록 뷰 기능 통합
   - [수정 필요]: feedbacks.status='rejected'인 미션 목록 — 재작성 버튼은 오직 이 탭에만 존재
     · "탈락 사유 보기 ▼" 토글 클릭 시 인라인 안내문 표시
     · "재작성 →" 버튼 → navigate('/panel/active?id=...&resubmit=<feedbackId>')
   - 모든 탭: 메인 미션(마케팅 소재 종합 진단) / 서브 미션(소재비교·가격검증·이메일검증) 섹션 분리
   - 각 섹션 10개씩 페이지네이션 (MissionCard 컴포넌트, mode prop으로 탭별 버튼 분기)
   - feedbackMap 구조: { [missionId]: { status, id, suggestions } } — feedback id 포함
   - 수락 확인 모달: Purit Filter 경고 3항목 포함 (ReactDOM.createPortal 기반, viewport 중앙 고정)
   - 수락 시 feedbacks INSERT(status='draft') → feedbackMap 업데이트 → /panel/active?id=... 이동
   - 수락 취소 모달: ConfirmModal 컴포넌트 사용, cancel_panel_feedback RPC 호출
   - 사이드바 메뉴: "미션 탐색" + "진행 중" 2개 → "미션 관리" 1개로 통합

3. 미션 수행 (/panel/active) — ActiveMission.jsx
   ★ 목록 뷰 (id 파라미터 없음, /panel/active):
     - 이 패널이 수락한(status='draft') 미션을 메인 미션 / 서브 미션 섹션으로 분리 표시 ★ 개편
     - 각 카드에 미션 유형 배지 표시 (소재 비교/가격 검증/이메일 검증)
     - 저장된 진행 없으면 "피드백 시작하기 →", 있으면 "이어하기 →" 버튼
     - 버튼 클릭 시 /panel/active?id=<mission_id>로 이동
   ★ 폼 뷰 (id 파라미터 있음, /panel/active?id=...):
     - 특정 미션 브리핑 화면 → mission.type에 따라 3가지 폼 분기
     - 브리핑에서도 저장 여부에 따라 "이어하기 →" / "피드백 시작하기 →"
     - 브리핑 description 표시:
       · 서브 미션: parseSubDesc 파싱 → productDescription(제품/타겟 설명) 표시
       · 메인 미션: parseLPDesc 파싱 → briefText만 표시 (raw JSON 미노출)
     ※ 서브 미션 폼 (type='preference'|'pricing'|'email', isSubMission=true):
       - 자동저장: 입력값 변경 시 1.5초 debounce → feedbacks.strengths에 JSON 저장
                  ({__subType, ...fields, customAnswers})
       - 이어하기: 재진입 시 strengths의 __subType 감지 → 각 서브 미션 state + customAnswers 복원
       - 목록 뷰 hasDraftProgress: strengths에 __subType 있으면 "이어하기 →" 표시
       - parseSubDesc(desc, type) 헬퍼로 하위 호환 JSON 파싱 (구 포맷 raw text 자동 감지)
       - preference: productDescription 표시, variantAImage/variantBImage 이미지 표시,
                     variantA/B 텍스트 → A/B 선택 + message_clarity + purchase_intent
       - pricing: parsedDesc.image 이미지 표시, parsedDesc.content 설명 표시,
                  productDescription 섹션, price_fairness + value_perception + would_buy
       - email: parsedDesc.content 원문 표시, productDescription 섹션,
                open_intent + hook_score + clarity_score + curiosity_score + would_reply
       - 타입 질문 동적 렌더링 (TypedQuestionsBlock 컴포넌트):  ★ 신규
         · parsedDesc.templateQuestions + 정규화된 parsedDesc.customQuestions → allTypedQs 배열
         · 레거시 string customQuestions → {id, text, type:'text', options:[]} 자동 정규화
         · radio(옵션형): 옵션 버튼 (선택 시 강조), scale(점수형): 1-5 점수 버튼, text(서술형): textarea (10자 이상 필수)
         · 모든 타입 질문 미응답 시 제출 불가 (canSubmit 유효성 검사 포함)
         · 응답은 customAnswers state: [{questionId, questionText, type, answer}]
       - 총 평가(comment) 섹션: TypedQuestionsBlock 아래 최하단 배치  ★ 개편 (2026-05-01)
         · "총 평가" 레이블이 붙은 별도 Card로 분리
         · preference: "선택 이유 및 개선 의견", pricing: "가격 피드백 (구매 장벽, 개선점)", email: "이메일 피드백"
       - 제출: preference_responses/pricing_responses/email_responses INSERT + feedbacks UPDATE(submitted) + RPC 2개
               custom_answers: customAnswers 컬럼 포함
     ※ 이미지 미션 (image_urls 있음, isSubMission=false):
       - ImageAnnotator 컴포넌트로 이미지 위에 드래그 영역 지정
       - 이미지 탭(복수일 때): 미방문 탭은 흐리게(opacity 0.6) 표시 / 방문한 탭은 accent 색 테두리로 구분
         · 모든 이미지 탭을 방문해야 제출 가능 (viewedImages Set으로 추적, 미방문 시 제출 버튼 비활성화)
         · 미방문 이미지 번호 안내 메시지 표시 (제출 버튼 위)
       - 상단 5개 차원 탭 (명확성/관련성/가치/차별화/신뢰) 중 하나 클릭 → 해당 차원 활성화
         · 탭에 어노테이션 있어도 ✓ 체크 표시 없음 — 어노테이션 수(숫자 뱃지)만 표시
         · 어노테이션 없는 탭: ○ 표시 / skip 상태: — 표시
       - 활성 차원 상태에서 이미지 드래그 → 점수만 선택하는 팝업 (dimension은 자동 고정)
       - 같은 차원 여러 번 드래그 가능 → 명1, 명2 등 번호 자동 부여
       - "피드백할 내용이 없습니다" skip 체크박스: 해당 차원 어노테이션 없이도 완료 처리 가능
       - 5개 차원 모두 완료 시 총평 textarea 표시 (필수)
       - LP 추가 질문 있으면 총평 아래에 TypedQuestionsBlock 표시 ★ 신규 (2026-05-02)
         · 미응답 시 제출 버튼 비활성화 (lpQsAnswered 검사)
       - feedback_annotations 테이블에 실시간 저장/삭제
       - 제출 시: dimension별 평균점수 → score 컬럼, suggestions = 어노테이션 요약+총평
                  custom_answers: customAnswers → feedbacks.custom_answers JSONB 컬럼 저장
       - 자동저장: customAnswers 변경 시 1.5초 debounce → strengths에 { customAnswers } 저장
       - 드래프트 복원: fb.strengths에서 customAnswers 배열 복원
     ※ 텍스트 미션 (image_urls 없음, 하위 호환):
       - "이어하기" 클릭 시 점수가 비어있는 첫 번째 단계로 직접 이동 (resume)
       - 중복 제출 방지: 동일 mission+panel 조합 사전 체크
       - 5개 축 점수(1-5) 입력
       - 각 축 텍스트 코멘트 → [레이블]\n코멘트 형식으로 suggestions 하나에 통합 저장
       - LP 추가 질문 있으면 마지막 차원(신뢰) 섹션에서 TypedQuestionsBlock 표시 ★ 신규 (2026-05-02)
         · 미응답 시 제출 버튼 비활성화
       - 자동 저장: 점수/코멘트/customAnswers 변경 후 1.5초 debounce → feedbacks UPDATE
         · LP 질문 응답 있으면 strengths = { __comments: comments, customAnswers }
         · 드래프트 복원: saved.__comments 감지 → comments + customAnswers 동시 복원
       - 제출 시 custom_answers: customAnswers → feedbacks.custom_answers JSONB 저장
     - 수락 취소 버튼(draft 상태일 때) → cancel_panel_feedback RPC → /panel/missions 이동
     ※ 재작성 모드 (/panel/active?id=...&resubmit=<feedbackId>):
       - History.jsx "재작성" 버튼 클릭 시 진입 (status='rejected'인 피드백에만 허용)
       - 진입 시: feedbacks.status → 'draft' 즉시 리셋, 이전 제출 데이터 자동 복원
         · 이미지 미션: feedback_annotations 로드 + suggestions에서 overallComment/skippedDims 복원
         · 서브 미션: 응답 테이블(preference_responses 등)에서 직접 로드
         · 텍스트 미션: parseCommentsFromSuggestions() 헬퍼로 suggestions → comments 복원
       - 브리핑 화면에 "재작성 모드" 배너 표시
       - 재제출 시: 서브 미션 응답 테이블 UPDATE (INSERT 금지 — unique constraint 위반),
                   filled_count / total_missions RPC 중복 호출 없음
       - 재제출 후: feedbacks.status → 'submitted' → 어드민 검토 대기 탭에 자동 노출
       - 기업 알림: "피드백 재제출" 제목으로 발송
     - 제출 시 (최초):
       a. feedbacks UPDATE (status: draft → submitted, strengths: null, suggestions 통합)
       b. missions.filled_count +1 → RPC increment_mission_filled_count (SECURITY DEFINER)
       c. panels.total_missions +1 → RPC increment_panel_mission_count (SECURITY DEFINER)
       d. panels.honor_points +5 → RPC add_panel_honor_points (SECURITY DEFINER) ★ 신규

4. 정산 내역 (/panel/history) — History.jsx  ★ 개편 (수익 이력에서 명칭 변경)
   - 페이지 제목: "정산 내역", 헤더 태그: LEDGER
   - 상단 통계 3칸:
     · 총 정산 완료: 금액(₩x만) + 건수 (purity_passed=true)
     · 정산 대기 중: 금액(₩x만) + 건수 (status='submitted', purity_passed=false)
     · 지급 거절: 건수 (status='rejected', purity_passed=false) — 0건이면 기본색, 있으면 빨간색
   - 탭 3개: 정산 대기 / 정산 완료 / 지급 거절 (기존 "전체" 탭 제거)
   - 기본 탭: '정산 대기' (pending)
   - [지급 거절] 항목: 재작성 버튼 없음 — "탈락 사유 보기" 텍스트(버튼) 클릭 시 인라인 사유 펼침
     · 반려 사유 안내문 + feedbacks.suggestions 원문을 mono 스타일로 표시
     · 재작성은 [미션 관리 → 수정 필요] 탭에서만 가능
   - Purit Filter 탈락 안내 카드: '정산 완료' 탭에서는 숨김 (정산 대기·지급 거절 탭에서 표시)
   - draft 상태 피드백 제외 (.neq('status', 'draft'))
     → 클릭 시 /panel/active?id=<missionId>&resubmit=<feedbackId> 이동 → 재작성 모드 진입

5. 프로필 설정 (/panel/profile) — Profile.jsx
   - 탭 기본 정보: 이름·직군·경력·자기소개 → panels UPDATE
     · 개인정보 안내 박스: "기업에게는 이름과 핸드폰 번호는 공개되지 않습니다. 직군과 경력만 공개됩니다."
     · 핸드폰 번호 Mock OTP 인증 섹션 (자기소개 아래):
       - 번호 입력 → "인증번호 발송" (800ms delay mock) → OTP 6자리 입력 → "확인" (600ms delay)
       - 6자리 아무 숫자 허용 (Mock 환경) → 성공 시 phone·phone_verified=true DB UPDATE
       - 인증 완료 시 "✓ 인증 완료" 뱃지 표시, "번호 변경" 링크로 재인증 가능
   - 탭 전문 분야: expertise 배열 토글 → panels UPDATE
   - 탭 정산 계좌: bank_name·bank_account·bank_holder → panels UPDATE
   - 탭 비밀번호: signInWithPassword 검증 → supabase.auth.updateUser({ password })  ★ 신규

================================================================
## 역할 3: Admin (관리자)
================================================================

인증 후 /admin 하위 라우트 접근 가능.
(전체 Supabase 연동 완료 — 2026-04-23)

※ Admin 계정(purit.admin@gmail.com)은 3개 포털 모두 사용 가능
   - panels + companies 레코드 수동 생성 완료 (SQL 003_admin_records.sql)
   - 사이드바 상단 포털 전환 버튼(어드민/기업/패널) 표시

1. 플랫폼 대시보드 (/admin) — Dashboard.jsx
2. 패널 관리 (/admin/panels) — PanelManagement.jsx  ★ 명예 레벨 시스템 적용 (2026-05-02)
   - 통계 카드: 전체 패널 / 활성 / 심사 대기 / 평균 Trust
   - 레벨 필터: 전체 / Lv.1-3 / Lv.4-6 / Lv.7-9 / Lv.10 (honor_points 기반 getHonorLevel() 계산)
   - 테이블: 이름 / 직군 / Trust / 레벨(Lv.X 뱃지, HONOR_COLOR_META 스타일) / 완료 / 상태
   - 패널 상세 카드: Lv.X · XXXpts 뱃지 + 명예 포인트 항목 포함
   - 등급 수동 조정 버튼 없음 (honor_points 자동 계산 방식으로 대체됨)
3. 미션 관리 (/admin/missions) — Missions.jsx  ★ 크레딧 연동 추가 (2026-05-04)
   - 기본 탭: '진행' (active) — 사이드바 클릭 시 진행 중 미션으로 바로 진입
   - 탭 4개: 전체 / 진행 / 완료 / 취소
   - 모든 탭에서 메인 의뢰(LP검증) / 서브 의뢰(소재비교·가격검증·이메일검증) 섹션 분리 ★ 개편
   - 각 섹션 10개씩 페이지네이션 (Pagination 컴포넌트, 모든 탭 공통 적용) ★ 개편
   - 미션 목록에 유형 뱃지 표시: LP 검증 / 소재 비교 / 가격 검증 / 이메일 검증
   - 완료(completed) 탭에 '재진행' 버튼 추가 → missions.status = 'active'로 복원
   - 전체(all) 탭: 메인 미션 / 서브 미션 섹션 분리, 각 섹션 10개씩 페이지네이션
   - 취소(cancelled) 상태 미션 영구 삭제: 확인 모달(ConfirmModal) → missions DELETE
   - ★ 크레딧 완료 처리: "완료 처리" 버튼 클릭 시 complete_mission_and_refund RPC 호출
     · purity_passed=true 패널의 experience 기반 실소비량 계산(소수, CEIL 없음) 후 잔여분 기업 계정에 자동 환불
     · 미션 카드 크레딧 표시 (credits_reserved > 0): "예상 N / 사용 N (환불 N cr)"
       - 사용 N: recalc_mission_consumed 결과 (퓨릿 필터 승인/반려 시 자동 갱신)
       - ↺ 재계산 버튼: 취소 제외 모든 미션 — 클릭 시 recalc 호출 후 로컬 상태 즉시 반영
     · ₩N/건 표시 제거 (경력별 단가 상이로 의미 없음)
     · RPC 실패 시 statusError state로 에러 메시지 상단 표시

4. Purity Filter (/admin/purity) — PurityFilter.jsx  ★ LP 질문 응답 표시 추가 (2026-05-02)
   - 피드백 품질 필터링 / purity_passed 관리
   - LP 메인 의뢰 피드백 상세 (isSubMission=false):
     · 상단: parseLPDesc로 briefText 표시 (미션 브리핑 요약)
     · 이미지 + 어노테이션 오버레이 (ImageAnnotator readonly, 다중 이미지 탭)
     · 텍스트: strengths / weaknesses / suggestions 원문
     · LP 추가 질문 응답: fb.custom_answers 배열 → 질문별 문항+답변 목록 ★ 신규
   - 서브 미션(preference/pricing/email) 피드백: parseSubDesc 헬퍼로 하위 호환 JSON 파싱
     - preference: variantA/B 텍스트 + 이미지 표시, productDescription, customQuestions,
                   A/B 선택 + message_clarity + purchase_intent + comment
     - pricing: 이미지 + content 표시, productDescription, customQuestions,
                would_buy + price_fairness + value_perception + key_comment
     - email: 이메일 원문(content) + productDescription + customQuestions,
              would_reply + hook/clarity/open_intent/curiosity 점수 + comment
   - Purit Score: 서브 미션은 calcSubPurityScore (코멘트 충실도 + 지표 충실도 기반)
   - 승인/반려: feedbacks.status 업데이트 (서브 미션도 동일)
   - 승인/반려/초기화 직후: recalc_mission_consumed RPC 호출 → 해당 미션의 credits_consumed 자동 재계산
   - 승인/반려 시 패널 + 기업 양쪽에 알림 자동 발송 (sendNotification 사용)
   - 반려 알림: 패널에게 actionUrl='/panel/history' 로 발송 → 재작성 버튼으로 바로 이동 유도
   - 패널 재제출 시: feedbacks.status 'rejected' → 'submitted' 자동 전환 → "검토 대기" 탭에 노출
5. 수익 & 정산 (/admin/revenue) — Revenue.jsx
6. 내 계정 (/admin/account) — Account.jsx  ★ 신규
   - 탭 관리자 정보: 이름 → supabase.auth.updateUser({ data: { name } }) / 이메일 읽기전용
   - 탭 비밀번호 변경: signInWithPassword 검증 → supabase.auth.updateUser({ password })

================================================================
## 인증 흐름
================================================================

- 방식: Supabase Auth (이메일 + 패스워드)
- 회원가입: role 선택(company/panel) → signUp 호출 →
             DB 트리거(handle_new_user)가 panels 또는 companies 테이블에 레코드 자동 생성
- 로그인: signInWithPassword → user_metadata.role로 역할 판별 → 역할별 대시보드 리다이렉트
- 세션: getSession() 초기 로드 + onAuthStateChange 실시간 구독
- 접근 제어: RoleRoute 컴포넌트 — 비인증 시 /login 리다이렉트,
             역할 불일치 시 해당 역할 홈으로 리다이렉트

================================================================
## 공용 라우트
================================================================

- / : Landing.jsx (공개)
- /login : Login.jsx (공개)
- /signup : Signup.jsx (공개, role 선택 포함)
- /share/:token : ShareResult.jsx (공개, 인증 불필요)
  - get_shared_mission(p_token) SECURITY DEFINER RPC 호출
  - 미션 제목·페르소나·피드백 수·5차원 평균 점수 표시
  - 토큰 없거나 만료 시 404 화면
- /*/notifications : NotificationCenter.jsx (역할별 알림, 각 포털 공유)
  - 알림 클릭 시 action_url 페이지로 자동 이동 (읽음 처리 동시)
  - 알림 발송 이벤트 전체 목록:
    · 새 미션 등록 (DB 트리거) → 어드민
    · 피드백 제출 (ActiveMission.jsx) → 기업
    · 슬롯 완료 (DB 트리거) → 기업
    · 피드백 승인 (PurityFilter.jsx) → 패널 + 기업
    · 피드백 반려 (PurityFilter.jsx) → 패널 + 기업
    · 24h 미완료 draft (panel/Dashboard.jsx) → 패널
    · Enterprise 문의 접수 (Pricing.jsx) → 기업(본인)
