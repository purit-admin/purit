================================================================
2_Architecture.txt — Purity MVP 기술 스택 & 아키텍처
최종 업데이트: 2026-04-29 (서브 의뢰 파이프라인 완성)
================================================================

## 기술 스택

| 영역 | 기술 | 버전 |
|------|------|------|
| UI 프레임워크 | React | 18.2.0 |
| 빌드 도구 | Vite | 5.1.0 |
| 라우팅 | React Router DOM | 6.22.0 |
| 백엔드/DB/Auth | Supabase (PostgreSQL) | JS SDK 2.103.3 |
| 스타일링 | Tailwind CSS | 4.2.2 |
| CSS 변수 시스템 | index.css (커스텀) | — |
| 아이콘 | Lucide React | 0.383.0 |
| 차트 | Recharts | 2.12.0 |
| 애니메이션 | Framer Motion | 최신 (2026-04-29 설치) |
| 배포 | Vercel (SPA rewrite) | — |
| 언어 | JavaScript (TypeScript 미사용) | ES Module |

================================================================
## 폴더 구조
================================================================

purit/
├── src/
│   ├── App.jsx                      # 라우터 루트, RoleRoute 정의
│   ├── main.jsx                     # React 진입점 (AuthProvider 감쌈)
│   ├── index.css                    # 전역 스타일 + 디자인 시스템 CSS 변수
│   │
│   ├── lib/
│   │   ├── supabase.js              # Supabase 클라이언트 싱글턴 (export: supabase)
│   │   └── data.js                  # 목업 데이터 + 헬퍼함수 (getTierColor, getStatusColor)
│   │
│   ├── context/
│   │   └── AuthContext.jsx          # AuthProvider + useAuth 훅
│   │                                #   상태: user, role, loading
│   │                                #   메서드: signIn, signUp, signOut
│   │                                #   파생값: dashboardPath
│   │
│   ├── components/
│   │   ├── layout/
│   │   │   └── Sidebar.jsx          # 역할별 사이드바 레이아웃 래퍼 (CL/PL/AL)
│   │   └── ui/
│   │       ├── index.jsx            # 공용 컴포넌트 (아래 목록 참조)
│   │       ├── ImageAnnotator.jsx   # 이미지 드래그 어노테이션 컴포넌트
│   │       │                        #   Props: imageUrl, imageIndex, annotations, onAdd, onRemove, readonly
│   │       │                        #   퍼센트 좌표 기반, window 레벨 mousemove/mouseup
│   │       └── NotificationCenter.jsx  # 역할별 알림 센터 모달
│   │
│   └── pages/
│       ├── Landing.jsx              # 공개 랜딩 페이지
│       ├── Login.jsx                # 로그인
│       ├── Signup.jsx               # 회원가입 (role 선택 포함)
│       │
│       ├── company/                 # Company 포털 (14개 페이지)
│       │   ├── Dashboard.jsx        # ★ 시각화 엔진 (Recharts 3종 + Framer Motion)
│       │   │                        #   - RadarChart: 5차원 내점수 vs 벤치마크
│       │   │                        #   - RadialBarChart: KPI 달성률 게이지
│       │   │                        #   - BarChart: 미션별 긍/부정 스택 바
│       │   │                        #   - feedbacks 실데이터 집계: computeRadar / computeSentiment
│       │   ├── NewMission.jsx
│       │   ├── Results.jsx
│       │   ├── Diagnosis.jsx
│       │   ├── PreferenceTest.jsx
│       │   ├── PricingTest.jsx
│       │   ├── ColdEmailTest.jsx
│       │   ├── QuestionTemplates.jsx
│       │   ├── ICPResearch.jsx
│       │   ├── ICPPulse.jsx
│       │   ├── BrandTracking.jsx
│       │   ├── AIReport.jsx
│       │   ├── Settings.jsx
│       │   └── Pricing.jsx
│       │
│       ├── panel/                   # Panel 포털 (5개 페이지)
│       │   ├── Dashboard.jsx
│       │   ├── Missions.jsx
│       │   ├── ActiveMission.jsx
│       │   ├── History.jsx
│       │   └── Profile.jsx
│       │
│       └── admin/                   # Admin 포털 (5개 페이지)
│           ├── Dashboard.jsx
│           ├── PanelManagement.jsx
│           ├── Missions.jsx
│           ├── PurityFilter.jsx
│           └── Revenue.jsx
│
├── supabase/
│   ├── migrations/
│   │   ├── 001_all_tables.sql       # 신규 테이블 19개 DDL
│   │   ├── 002_seed_data.sql        # 초기 시드 데이터 (GMV·정산·청구·질문템플릿)
│   │   ├── 003_admin_records.sql    # admin 계정 panels/companies 레코드 생성
│   │   ├── 004_cancel_feedback_rpc.sql  # cancel_panel_feedback RPC
│   │   ├── 005_ai_reports.sql       # ai_reports 테이블 + RLS
│   │   ├── 006_image_annotations.sql  # feedback_annotations 테이블 + missions.image_urls
│   │   ├── 007_subtest_missions.sql   # 서브 의뢰 테이블에 mission_id/template_id/status 추가
│   │   │                               # preference/pricing/email 응답 테이블에 추가 점수 컬럼 + RLS
│   │   └── 008_seed_templates.sql     # 브랜드 삭제, category 업데이트, 서브 의뢰 템플릿 6개 삽입
│   └── functions/
│       └── generate-ai-report/
│           └── index.ts             # Deno Edge Function — Anthropic API → ai_reports INSERT
├── MORNING_REPORT.md                # 배포 가이드 (Supabase Secret·CLI 설정 안내)
├── index.html
├── vite.config.js                   # Vite 기본 설정 + @vitejs/plugin-react
├── vercel.json                      # SPA 라우팅 rewrite 규칙
└── package.json

================================================================
## 라우트 전체 목록 (26개)
================================================================

[공개]
  /                          → Landing.jsx
  /login                     → Login.jsx
  /signup                    → Signup.jsx

[Company 포털] — RoleRoute role="company"
  /company                   → Dashboard.jsx
  /company/notifications     → NotificationCenter.jsx
  /company/new               → NewMission.jsx
  /company/results           → Results.jsx
  /company/diagnosis         → Diagnosis.jsx
  /company/preference        → PreferenceTest.jsx
  /company/pricing-test      → PricingTest.jsx
  /company/email-test        → ColdEmailTest.jsx
  /company/templates         → QuestionTemplates.jsx
  /company/icp               → ICPResearch.jsx
  /company/icp-pulse         → ICPPulse.jsx
  /company/brand             → BrandTracking.jsx
  /company/report            → AIReport.jsx
  /company/settings          → Settings.jsx
  /company/plans             → Pricing.jsx

[Panel 포털] — RoleRoute role="panel"
  /panel                     → Dashboard.jsx
  /panel/notifications       → NotificationCenter.jsx
  /panel/missions            → Missions.jsx
  /panel/active              → ActiveMission.jsx
  /panel/history             → History.jsx
  /panel/profile             → Profile.jsx

[Admin 포털] — RoleRoute role="admin"
  /admin                     → Dashboard.jsx
  /admin/notifications       → NotificationCenter.jsx
  /admin/panels              → PanelManagement.jsx
  /admin/missions            → Missions.jsx
  /admin/purity              → PurityFilter.jsx
  /admin/revenue             → Revenue.jsx

================================================================
## DB 스키마 (Supabase / PostgreSQL)
================================================================

※ 코드에서 역설계한 스키마. 실제 Supabase 대시보드와 교차 검증 필요.

### [auth.users] — Supabase 내장
  id              UUID  PK
  email           TEXT
  user_metadata   JSONB  { role: 'company'|'panel'|'admin', name: TEXT }

---

### [companies]
  id              UUID  PK
  user_id         UUID  FK → auth.users.id  (UNIQUE, 1:1)
  name            TEXT
  plan            TEXT  (플랜명, 예: free/pro 등)

---

### [panels]
  id              UUID  PK
  user_id         UUID  FK → auth.users.id  (UNIQUE, 1:1)
  name            TEXT
  industry        TEXT
  experience      TEXT  (경력 연차 등)
  bio             TEXT
  expertise       TEXT[]  (전문 분야 배열)
  bank_name       TEXT
  bank_account    TEXT
  bank_holder     TEXT
  total_missions  INT   DEFAULT 0

---

### [missions]
  id              UUID  PK
  company_id      UUID  FK → companies.id
  title           TEXT
  type            TEXT  (예: 'landing_page')
  target_url      TEXT  (LP URL)
  description     TEXT  (브리프)
  persona         JSONB (타겟 페르소나 설정값)
  panel_count     INT   (요청 패널 수)
  filled_count    INT   DEFAULT 0 (제출 완료 수)
  reward_amount   INT   (보상금, 원 단위)
  status          TEXT  ('active'|'completed'|'draft'|'in_review'|'cancelled')
  assets          TEXT[]  (검증 포인트 목록)
  image_urls      TEXT[]  DEFAULT '{}' (업로드 이미지 public URL 배열, 최대 5개)
  created_at      TIMESTAMPTZ DEFAULT now()

---

### [feedbacks]
  id                    UUID  PK
  mission_id            UUID  FK → missions.id
  panel_id              UUID  FK → panels.id
  clarity_score         INT   (1–5, nullable — draft 중에는 null 허용)
  relevance_score       INT   (1–5, nullable)
  value_score           INT   (1–5, nullable)
  differentiation_score INT   (1–5, nullable)
  trust_score           INT   (1–5, nullable)
  strengths             TEXT  (draft 중 임시 저장용: JSON.stringify(comments), 제출 시 null)
  weaknesses            TEXT  (null 고정, 하위 호환용)
  suggestions           TEXT  ([레이블]\n코멘트 형식 5개 섹션 통합, 제출 시에만 기록)
  purity_passed         BOOL  DEFAULT false
  status                TEXT  ('draft'|'submitted'|'approved'|'rejected')
                               ※ draft: 수락 후 미완료 (패널이 수락하면 즉시 생성)
                               ※ CHECK 제약: feedbacks_status_check 로 4개 값 허용
  created_at            TIMESTAMPTZ DEFAULT now()

  ※ RLS: SELECT(인증된 전체), INSERT(패널), UPDATE(인증된 전체)
  ※ DELETE 정책 없음 → 수락 취소는 cancel_panel_feedback RPC(SECURITY DEFINER)로 처리

---

### [feedback_annotations] — 이미지 영역 어노테이션 (006_image_annotations.sql)
  id          UUID  PK
  feedback_id UUID  FK → feedbacks.id   ON DELETE CASCADE
  mission_id  UUID  FK → missions.id    ON DELETE CASCADE
  panel_id    UUID  FK → panels.id      ON DELETE CASCADE
  image_index INT   DEFAULT 0 (이미지 탭 인덱스)
  x_pct       NUMERIC(6,2)  (좌측 시작점 %)
  y_pct       NUMERIC(6,2)  (상단 시작점 %)
  w_pct       NUMERIC(6,2)  (너비 %)
  h_pct       NUMERIC(6,2)  (높이 %)
  dimension   TEXT  CHECK IN ('clarity','relevance','value','differentiation','trust')
  score       INT   CHECK BETWEEN 1 AND 5
  comment     TEXT
  created_at  TIMESTAMPTZ DEFAULT now()

  ※ RLS: panel_own_annotations(ALL), company_read_annotations(SELECT), admin_read_annotations(SELECT)
  INDEX: fa_feedback_idx, fa_mission_idx, fa_panel_idx

---

### [gmv_snapshots] — 어드민 월간 GMV 스냅샷
  id           UUID  PK
  month_label  TEXT  (예: '7월')
  year         INT
  month_num    INT
  gmv          NUMERIC
  panel_pay    NUMERIC
  margin       NUMERIC
  created_at   TIMESTAMPTZ

---

### [settlements] — 패널 정산 내역
  id             UUID  PK
  panel_id       UUID  FK → panels.id  (NULLABLE)
  panel_name     TEXT
  tier           TEXT
  missions_count INT
  amount         NUMERIC
  status         TEXT  ('pending'|'paid'|'rejected')
  due_date       DATE
  created_at     TIMESTAMPTZ

---

### [invoices] — 기업 청구서
  id            UUID  PK
  company_id    UUID  FK → companies.id  (NULLABLE)
  company_name  TEXT
  plan          TEXT
  amount        NUMERIC
  status        TEXT  ('pending'|'paid')
  invoice_date  DATE
  created_at    TIMESTAMPTZ

---

### [notifications] — 시스템 알림
  id          UUID  PK
  user_id     UUID  FK → auth.users.id  ON DELETE CASCADE
  type        TEXT  ('success'|'warning'|'info')
  icon        TEXT
  title       TEXT
  body        TEXT
  action_url  TEXT
  read        BOOL  DEFAULT false
  created_at  TIMESTAMPTZ
  INDEX: (user_id, read)

---

### [team_members] — 기업 팀원
  id          UUID  PK
  company_id  UUID  FK → companies.id  ON DELETE CASCADE
  name        TEXT
  email       TEXT
  role        TEXT  ('admin'|'member'|'viewer')
  status      TEXT  ('active'|'invited'|'inactive')
  joined_at   DATE
  created_at  TIMESTAMPTZ

---

### [question_templates] — 질문 템플릿
  id          UUID  PK
  company_id  UUID  FK → companies.id  (NULLABLE = 플랫폼 기본)
  name        TEXT
  icon        TEXT
  category    TEXT
  description TEXT
  use_count   INT   DEFAULT 0
  is_default  BOOL  DEFAULT false
  created_at  TIMESTAMPTZ

### [template_questions] — 템플릿 세부 문항
  id            UUID  PK
  template_id   UUID  FK → question_templates.id  ON DELETE CASCADE
  question_text TEXT
  question_order INT

---

### [preference_tests] — 소재 선호도 A/B 테스트
  id          UUID  PK
  company_id  UUID  FK → companies.id
  asset_type  TEXT
  variant_a   TEXT
  variant_b   TEXT
  panel_size  INT   DEFAULT 10
  status      TEXT  ('draft'|'active'|'completed')
  created_at  TIMESTAMPTZ

### [preference_tests] / [preference_responses] — 소재 비교 A/B
  preference_tests:
    id, company_id, asset_type, variant_a, variant_b, panel_size, status, created_at
    mission_id  UUID  FK → missions.id    ← 007 추가
    template_id UUID  FK → question_templates.id  ← 007 추가

  preference_responses:
    id, test_id FK→preference_tests.id, panel_id FK→panels.id
    preference  TEXT  ('A'|'B')
    comment     TEXT
    mission_id  UUID  FK → missions.id    ← 007 추가
    status      TEXT  DEFAULT 'submitted'  ← 007 추가
    message_clarity  INT CHECK (1-5)      ← 007 추가
    purchase_intent  INT CHECK (1-5)      ← 007 추가
    created_at  TIMESTAMPTZ
  ※ RLS: panel_insert_pref(INSERT), auth_select_pref(SELECT), auth_update_pref(UPDATE)

---

### [pricing_tests] / [pricing_axes] / [pricing_responses]
  pricing_tests:
    id, company_id, status, created_at
    mission_id  UUID  FK → missions.id    ← 007 추가
    template_id UUID  FK → question_templates.id  ← 007 추가

  pricing_axes:  id, test_id, axis_key, label, score

  pricing_responses:
    id, test_id FK→pricing_tests.id, panel_id FK→panels.id
    tier, would_buy, key_comment, barriers JSONB
    mission_id     UUID  FK → missions.id  ← 007 추가
    status         TEXT  DEFAULT 'submitted'  ← 007 추가
    price_fairness INT CHECK (1-5)           ← 007 추가
    value_perception INT CHECK (1-5)         ← 007 추가
    created_at  TIMESTAMPTZ
  ※ RLS: panel_insert_pricing(INSERT), auth_select_pricing(SELECT), auth_update_pricing(UPDATE)

---

### [cold_email_tests] / [email_responses]
  cold_email_tests:
    id, company_id, email_text, status, created_at
    mission_id  UUID  FK → missions.id    ← 007 추가
    template_id UUID  FK → question_templates.id  ← 007 추가

  email_responses:
    id, test_id FK→cold_email_tests.id, panel_id FK→panels.id
    would_reply, hook_score, clarity_score, comment
    mission_id     UUID  FK → missions.id  ← 007 추가
    status         TEXT  DEFAULT 'submitted'  ← 007 추가
    open_intent    INT CHECK (1-5)            ← 007 추가
    curiosity_score INT CHECK (1-5)           ← 007 추가
    created_at  TIMESTAMPTZ
  ※ RLS: panel_insert_email(INSERT), auth_select_email(SELECT), auth_update_email(UPDATE)

---

### [icp_research] / [icp_pain_points] / [icp_buy_triggers] / [icp_language]
  icp_research:     id, company_id, persona_name, status, created_at
  icp_pain_points:  id, research_id, pain_text, percent
  icp_buy_triggers: id, research_id, trigger_text, percent, priority_rank
  icp_language:     id, research_id, phrase, frequency ('high'|'mid'|'low')

---

### [icp_pulse_subscriptions] / [icp_pulse_quarters] / [icp_pulse_pains] / [icp_pulse_gains]
  icp_pulse_subscriptions: id, company_id, panel_size, status, created_at
  icp_pulse_quarters:      id, subscription_id, quarter_label, buying_trigger, top_channel, collected_at
  icp_pulse_pains:         id, quarter_id, pain_text, frequency_percent
  icp_pulse_gains:         id, quarter_id, gain_text, frequency_percent

---

### [brand_tracking_campaigns] / [brand_tracking_monthly] / [brand_perception_items]
  brand_tracking_campaigns:  id, company_id, panel_size, status, created_at
  brand_tracking_monthly:    id, campaign_id, month_label, awareness_percent, clarity_score, differentiation_score, nps_score
  brand_perception_items:    id, campaign_id, statement, agreement_percent

---

### [ai_reports] — AI 리포트 결과
  id              UUID  PK
  mission_id      UUID  FK → missions.id
  company_id      UUID  FK → companies.id
  tldr            TEXT  (AI 생성 요약)
  priority_fixes  JSONB  [{area, issue, action, impact}]
  strengths       JSONB  [string]
  risk_flags      JSONB  [{level: 'high'|'mid', text}]
  overall_score   NUMERIC(3,2)
  panel_count     INT
  created_at      TIMESTAMPTZ DEFAULT now()

  ※ RLS: SELECT(본인 company or admin), INSERT(service role — Edge Function)

---

### 관계 요약

  companies            ←1:1→  auth.users   (companies.user_id)
  panels               ←1:1→  auth.users   (panels.user_id)
  missions             ←N:1→  companies    (missions.company_id)
  feedbacks            ←N:1→  missions     (feedbacks.mission_id)
  feedbacks            ←N:1→  panels       (feedbacks.panel_id)
  feedback_annotations ←N:1→  feedbacks    (feedback_annotations.feedback_id)
  feedback_annotations ←N:1→  missions     (feedback_annotations.mission_id)
  feedback_annotations ←N:1→  panels       (feedback_annotations.panel_id)
  ai_reports           ←N:1→  missions     (ai_reports.mission_id)
  ai_reports           ←N:1→  companies    (ai_reports.company_id)
  notifications        ←N:1→  auth.users   (notifications.user_id)
  team_members         ←N:1→  companies    (team_members.company_id)
  settlements          ←N:1→  panels       (settlements.panel_id, NULLABLE)
  invoices             ←N:1→  companies    (invoices.company_id, NULLABLE)

---

### SECURITY DEFINER RPC 함수 목록

  increment_mission_filled_count(mission_id UUID)
    → missions.filled_count +1 (패널이 직접 UPDATE 불가 → RPC 필수)

  increment_panel_mission_count(panel_id UUID)
    → panels.total_missions +1 (패널이 본인 panels 직접 UPDATE 불가 → RPC 필수)

  cancel_panel_feedback(p_mission_id UUID)
    → feedbacks DELETE (status='draft', 본인 panel) (DELETE RLS 없음 → RPC 필수)
    → 파일: supabase/migrations/004_cancel_feedback_rpc.sql

================================================================
## 공용 컴포넌트 (src/components/ui/index.jsx)
================================================================

재사용 시 이 컴포넌트들을 우선 사용할 것.

  Btn             — 버튼 (variant: primary|secondary|ghost|danger|outline, size: sm|md|lg)
  Card            — 카드 컨테이너 (클릭 가능, hover 효과)
  Stat            — 지표 표시 (label, value, sub, accent)
  ScoreBar        — 점수 바 (score, max=5, color)
  Badge           — 태그/뱃지 (type: gray|gold|green|red|blue)
  Divider         — 구분선
  EmptyState      — 빈 상태 플레이스홀더 (icon, title, desc)
  ImageAnnotator  — 이미지 드래그 어노테이션 (ImageAnnotator.jsx, 별도 파일)

================================================================
## 환경 변수 (.env)
================================================================

  VITE_SUPABASE_URL         = https://[project].supabase.co
  VITE_SUPABASE_ANON_KEY    = [anon public key]
  SUPABASE_SERVICE_ROLE_KEY = [service role secret key]  ← 서버/스크립트 전용, VITE_ 없음

  [Supabase Edge Function Secrets — supabase secrets set 명령어로 등록]
  ANTHROPIC_API_KEY = sk-ant-api03-...  ← generate-ai-report 함수에서 사용
                                           미설정 시 Mock 응답 폴백 (서비스 중단 없음)

  ※ Vite 규칙: 클라이언트 노출 변수는 반드시 VITE_ 접두사 필요
  ※ import.meta.env.VITE_* 로 접근
  ※ SUPABASE_SERVICE_ROLE_KEY 는 클라이언트에 절대 노출 금지
