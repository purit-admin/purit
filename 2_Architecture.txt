================================================================
2_Architecture.txt — Purity MVP 기술 스택 & 아키텍처
최종 업데이트: 2026-05-19 (알림톡 Edge Functions + migrations 040/041 추가)
================================================================

## 기술 스택

| 영역 | 기술 | 버전 |
|------|------|------|
| UI 프레임워크 | React | 18.2.0 |
| 빌드 도구 | Vite | 5.1.0 |
| 라우팅 | React Router DOM | 6.22.0 |
| 백엔드/DB/Auth | Supabase (PostgreSQL) | JS SDK 2.103.3 |
| 스타일링 | Tailwind CSS | 4.2.2 |
| CSS 변수 시스템 | index.css (커스텀) | — |
| 아이콘 | Lucide React | 0.383.0 |
| 차트 | Recharts | 2.12.0 |
| 애니메이션 | Framer Motion | ^12.38.0 |
| 배포 | Vercel (SPA rewrite) | — |
| 언어 | JavaScript (TypeScript 미사용) | ES Module |

================================================================
## 폴더 구조
================================================================

purit/
├── src/
│   ├── App.jsx, main.jsx, index.css
│   ├── lib/       supabase.js | data.js | notify.js (sendNotification 중앙 헬퍼 — prefKey + targetRole 파라미터 포함) | templates.js (QUESTION_TEMPLATES: preference/pricing/email 9종 + lp 12종(4섹션), TYPE_LABEL, TYPE_COLOR)
│   │              notify.js sendNotification(userId, { type, icon, title, body, actionUrl, targetRole, prefKey }): notifications INSERT
│   │                targetRole: 'admin'|'company'|'panel'|null — 포털별 알림 분리 (030 마이그레이션)
│   │                prefKey: 수신자의 notif_prefs JSONB 키 — panel/company 수신자인 경우 DB에서 prefs 조회, false이면 발송 차단 (042 마이그레이션) ★
│   │                  panels/companies.notif_prefs 키 미설정=수신, false=차단 (옵트아웃 방식)
│   │                  panel prefKey 목록: feedbackApproved / feedbackRejected / deadlineWarning
│   │                  company prefKey 목록: missionComplete / missionStatusChange / earlyComplete
│   │                  prefKey 없음: admin 대상 알림 또는 PRD에서 사용자 토글 불가 알림 (버그 답변 등)
│   │              lp 템플릿 sectionLabel 필드: '랜딩페이지 진단'(3종)·'광고 소재 진단'(3종)·'제품 상세 페이지 진단'(3종)·'영상/숏폼 광고 진단'(3종)
│   │              QuestionTemplates.jsx·NewMission.jsx: sectionLabel 변경 감지 → 섹션 구분선+레이블 렌더링 (category='랜딩페이지' 유지 — 기존 필터 로직 불변)
│   │              honorLevels.js (HONOR_LEVELS 10단계 배열, HONOR_COLOR_META 4색 티어, getHonorLevel·getNextLevel·getProgressPct·getExperienceMultiplier·getExperienceCareerKey·getPanelReward·fmtWon·fmtKRW 헬퍼)
│   │                getExperienceCareerKey(experience): experience 문자열 → 'junior'|'middle'|'senior'|'clevel' 키 반환 (getExperienceMultiplier 래핑)
│   │                getPanelReward(honorPoints, experience): 레벨 baseReward × getExperienceMultiplier(experience) → 패널 1인당 동적 미션비
│   │                Missions.jsx MissionCard + ActiveMission.jsx(목록·브리핑·제출완료): missions.reward_amount(고정) 대신 getPanelReward로 동적 계산 표시 (서브미션 × 4500/8000 비율 스케일)
│   │              navigationGuard.js (싱글톤 — register(config)/unregister()/intercept(path); 폼 페이지가 register, Sidebar가 intercept 호출)
│   ├── context/   AuthContext.jsx  (useAuth: user·role·loading / signIn·signUp·signOut / dashboardPath)
│   ├── components/
│   │   ├── layout/  Sidebar.jsx  (역할별 사이드바, unreadCount 실시간 구독 뱃지 — 기업 포털 그룹: 개요/메인의뢰(의뢰등록)/서브의뢰(A/B·가격·이메일)/자산(질문템플릿·피드백결과·5차원진단·AI리포트)/계정)
│   │   │              ★ 클릭 토글 사이드바 패턴:
│   │   │                - 우상단 ‹/› 버튼 클릭 → 220px 펼침 ↔ 60px 접힘 토글
│   │   │                - state: collapsed(bool, 기본 false=펼침)
│   │   │                - isExpanded = isMobile ? true : !collapsed
│   │   │                - sidebarWidth = isMobile ? 220 : (isExpanded ? 220 : 60)
│   │   │                - aside transition: 'width 0.3s cubic-bezier(0.4,0,0.2,1)'
│   │   │              ★ 애니메이션 구현 원칙 (D-78 참조):
│   │   │                - 텍스트·레이블: maxWidth + opacity 전환 (조건부 렌더 대신 always-render)
│   │   │                - 그룹 레이블·구분선: maxHeight + opacity 전환 (즉시 마운트로 인한 세로 텍스트 방지)
│   │   │                - 아이콘 정렬: justifyContent 고정(flex-start) + paddingLeft 전환으로 centering 효과
│   │   │                  (paddingLeft: isExpanded ? 12 : 16 — 16 = (48-16)/2, 접힘 시 아이콘 중앙 정렬)
│   │   │                - 어드민 포털 스위처: 두 레이아웃(세로버튼/가로pill) cross-fade (maxHeight+opacity 역전)
│   │   │              ★ 리서치 그룹(ICP·Pulse·브랜드) 사이드바에서 제거 — 라우트·페이지 파일은 유지
│   │   │              handleNav(path) 헬퍼: navigationGuard.intercept(path) → false면 navigate, true면 guard가 모달 트리거
│   │   │              로그아웃은 guard 체크 제외 (의도적 이탈)
│   │   │              showBugReport state + "버그/불편 신고" 버튼(로그아웃 버튼 위, 전 포털 공통) + BugReportModal 렌더
│   │   │              어드민 NAV '운영' 그룹 순서: 기업 관리 → 패널 관리 → 미션 관리 → 피드백 관리(구 'Purit Filter') → 수익 & 정산 → 버그 리포트 ★
│   │   └── ui/      index.jsx (Btn·Card·Stat·ScoreBar·Badge·Divider·EmptyState·ConfirmModal)
│   │                  ConfirmModal props: title·desc·confirmLabel·cancelLabel·danger·onConfirm·onCancel + errorMsg=''(선택)
│   │                    errorMsg 있으면 모달 내부 하단에 빨간 배너 표시 → 확인 실패 시 모달 유지 (낙관적 롤백 패턴 — D-65 참조)
│   │                ImageAnnotator.jsx (퍼센트 좌표 드래그 어노테이션, readonly 모드 지원, activeDimension prop으로 차원 고정)
│   │                  seqPool prop(글로벌 번호 계산용 전체 어노테이션 배열, 미전달 시 annotations 사용), highlightedId prop(해당 id만 표시·나머지 숨김)
│   │                  어노테이션 뱃지: 한국어 접두사 없이 숫자만 표시 (색상으로 차원 구분)
│   │                NotificationCenter.jsx (역할별 알림, action_url 클릭 시 navigate)
│   │                  쿼리: .or('target_role.eq.${role},target_role.is.null') — 포털별 알림 분리 ★
│   │                  어드민 탭: 'general'|'bugs' — isBug(n)=action_url==='/admin/reports' 구분, 탭별 unread 뱃지
│   │                  페이지네이션: PAGE_SIZE=10, paged 슬라이스, 탭·페이지 전환 시 선택 초기화 ★
│   │                  선택/삭제: selected Set + toggleOne/toggleAll + deleteSelected (notifications DELETE .in('id', ids)) ★
│   │                  markAll(): displayed(현재 탭 전체) 기준 일괄 읽음, 페이지 무관
│   │                  Sidebar loadCount도 동일 .or() 필터로 포털별 카운트 분리 ★
│   │                BugReportModal.jsx (버그/불편 신고 모달 — ReactDOM.createPortal, 전체 포털 공통) ★ 신규
│   │                  props: onClose, pageUrl(현재 pathname)
│   │                  5가지 유형 칩 + 제목 + 상세 + 스크린샷 업로드(bug-screenshots 버킷, 5MB)
│   │                  supabase.storage('bug-screenshots').upload → screenshot_url, bug_reports INSERT
│   │                  업로드 실패 시 screenshot_url=null graceful fallback
│   │                PanelTargetStep.jsx (패널 설정 전용 공용 컴포넌트 — 슬라이더·경력버튼·크레딧계산기·업그레이드팝업)
│   │                  ★ forwardRef + useImperativeHandle 적용 — 부모 폼에서 ref={panelStepRef}로 openCreditModal() 호출 가능
│   │                  크레딧 부족 시 "다음" 버튼에서 panelStepRef.current?.openCreditModal() 트리거 → 업그레이드 모달 표시
│   │                ExpertPanelCloud.jsx (Landing.jsx 섹션 6.5 전용 — 전문가 경력 플로팅 애니메이션) ★ 신규
│   ├── landing/
│   │                StepAnimations.jsx (Landing.jsx 작동방식 섹션 6 전용 — Step1/2/3 루핑 CSS 애니메이션)
│   │                  exports: Step1Animation(의뢰 등록 폼 목업) / Step2Animation(패널 매칭) / Step3Animation(결과 대시보드)
│   │                  구현: usePhases(durations) 커스텀 훅 — setTimeout 체인 기반 페이즈 상태머신 (D1 8단계/D2 10단계/D3 7단계)
│   │                  CSS keyframes: s1_chipPop·s1_fadeIn·s1_blink·s1_btnPulse / s2_slideIn·s2_checkPop·s2_counterPop / s3_fillBar·s3_drawR·s3_slideUp·s3_fadeIn
│   │                  스코어 바 fillBar: CSS 변수 --tw 패턴 (keyframe to{width:var(--tw)}) + phase < 3 시 inline width:'0%' 리셋
│   │                  레이더 재시작: <polygon key={phase<2?'r':'a'}> — key 교체로 강제 리마운트 → stroke-dashoffset 애니메이션 재시작 (D-51)
│   │                  단일 requestAnimationFrame 루프가 20개 태그를 구동 (React state 업데이트 없이 DOM 직접 조작)
│   │                  phase = ((elapsed - i*STAGGER - pausedMs) % T_CYCLE + T_CYCLE) % T_CYCLE 수식으로 각 태그 위치 계산
│   │                  T_CYCLE=20s, T_IN=1s, T_HOLD=1.8s, T_OUT=1.2s, STAGGER=1s → 항상 정확히 4개 동시 노출
│   │                  호버: hoverStartRef/pausedMsRef로 누적 정지시간 추적 → phase 계산 시 차감 (드리프트 없는 일시정지)
│   │                  위치: 4×5 격자 + Math.sin/cos 결정론적 jitter (POSITIONS 모듈 레벨 상수)
│   │                  CSS: .expert-tag (font-size:17px, white-space:nowrap) / .expert-cloud-box (height:300px)
│   │                       모바일 ≤640px: font-size:12px, height:240px
│   │   ※ pages/company/NewMission.jsx: view state('list'|'form') — editMode 시 form 직접 진입
│   │                list 뷰: missions .or('type.is.null,type.eq.landing_page') 로드 (cancelled 포함, .neq 제거), 탭 5개(전체/진행/완료/임시저장/취소)
│   │                  listFilter state로 탭별 필터링, draft 카드 클릭 시 navigate(editMode) 진입
│   │                  카드 레이아웃: mc-row/mc-right CSS 클래스 사용, 상태 뱃지(매칭대기gray/진행중green/완료blue/임시저장gold/취소red), 소재비교·가격검증·이메일검증 타입 뱃지 (LP검증 뱃지 없음)
│   │                  active 카드: filled=0 → '수정' 버튼(navigate editMode) / filled≥1 → '조기 종료' 버튼 → terminateTarget ConfirmModal → status='cancelled' UPDATE
│   │                  terminateTarget state + handleTerminate(): missions UPDATE(status='cancelled') + 로컬 상태 즉시 반영
│   │                  조기 종료 ConfirmModal 본문: "조기 종료 시 사용된 크레딧은 환불되지 않습니다."
│   │                companyId state: fetchPlan useEffect에서 select('id, plan, credit_balance') 로 함께 로드 → creditBalance state 설정
│   │                customLPQs state: companyId 의존 useEffect → question_templates(category='랜딩페이지', is_default=false) 조회
│   │                initTemplateId/initTemplateName: location.state에서 읽어 해당 LP 템플릿 사전 선택 + Step 2 진입
│   │                form.industry state + industryOpen/industryCustomMode/industryCustomInput state 추가
│   │                INDUSTRIES 상수 (20개 산업군): 파일 최상단 모듈 레벨 정의 (4개 등록 폼 공통 패턴)
│   │                Step 0 레이블: '검증할 서비스명(의뢰명)', 산업군(필수, (선택) 없음), '랜딩페이지 URL (선택)'
│   │                Step 2 레이블: '질문 설정' (선택 제거)
│   │                폼 상단: ※ 조기 종료 시 사용된 크레딧은 환불되지 않습니다. (red 12px, NDA 배너 아래)
│   │                Step 0: 산업군 선택(▼ 드롭다운) → 서비스명 → LP URL 순 배치 ★ 순서 변경 (2026-05-09), 페르소나 필드 아래, 부제 "검증할 서비스와 서비스 타겟에 대해 설정합니다."
│   │                INDUSTRY_PLACEHOLDERS 상수 (모듈 레벨): 20개 산업군 → { product, personaAge, personaIncome, personaRole, personaContext, briefText } 매핑
│   │                  DEFAULT_PLACEHOLDERS: 산업군 미선택 시 중립 안내 문구 / ph = INDUSTRY_PLACEHOLDERS[form.industry] || DEFAULT_PLACEHOLDERS
│   │                  적용 범위: Step 0 서비스명·연령대·월소득·직군·타겟상세 + Step 1 브리핑 placeholder 동적 교체
│   │                  산업군 드롭다운: INDUSTRIES 칩 grid + "✏️ 직접 쓰기" 토글 → 텍스트 입력 + 확인 버튼 (Enter 키 지원)
│   │                  buildDescription(): industry 포함 → description JSON / persona 문자열에도 "산업군: XXX" 포함
│   │                폼 상단 배너: 🔒 NDA(accent-dim) + ✨ 매칭(rgba(16,185,129,0.07)) 2개 항상 표시
│   │                Step 2: "내 커스텀 질문" 아코디언 (customLPQs DB 데이터) + QUESTION_TEMPLATES.lp 아코디언
│   │                        "질문 만들기" 인라인 Card: textarea + 타입(옵션형/점수형/서술형) + "추가"(localCustomQs) + "템플릿에 저장 →"(DB INSERT, ConfirmModal)
│   │                임시 저장 상태: isDraftMode(ms.status==='draft'), savingDraft, showDraftModal
│   │                  shouldBlockNav: view==='form' && (!effectiveEditMode || isDraftMode) && 입력값 존재
│   │                  useBlocker: 라우터 이탈 감지 / showDraftModal: 로컬 setView('list') 이탈 감지
│   │                  saveDraft(): description JSON에 모든 필드(step 포함) 저장 → missions INSERT/UPDATE(status='draft')
│   │                  draft→active 제출: UPDATE(status='active') + reserve_mission_credits 호출, 실패 시 'draft' 롤백
│   │                  editMode 복원: description JSON 전체 파싱 → 개별 필드 set (personaAge/Income/Role/Context/industry/step 포함)
│   │   ※ 임시저장 공통 패턴 (4개 등록 폼 — NewMission/PreferenceTest/PricingTest/ColdEmailTest):
│   │                  navigationGuard (src/lib/navigationGuard.js) 싱글톤 import
│   │                  pendingNavPath state: 사이드바 이탈 시 목적지 경로 임시 저장
│   │                  useEffect: shouldBlockNav 변화 시 navigationGuard.register({ onAttempt: (path)=>{ setPendingNavPath(path); setShowDraftModal(true); } }) / unregister()
│   │                  finishLeave(): navigationGuard.unregister() → navigate(pendingNavPath) or setView('list') → setPendingNavPath(null)
│   │                  draftId state: draft 이어쓰기 시 대상 mission id 추적
│   │                  DraftSaveModal: ReactDOM.createPortal(document.body) 인라인 3버튼 모달 (D-19 패턴)
│   │                  window.beforeunload: shouldBlockNav 조건 시 e.preventDefault() + e.returnValue=''
│   │                  deleteTarget state + handleDeleteMission(): missions DELETE + 로컬 필터 제거
│   │                  terminateTarget state + handleTerminate(): missions UPDATE(status='cancelled') + 로컬 상태 반영 (4개 폼 공통)
│   │                  메인·서브 의뢰 모두: 목록 뷰 draft/active 이어하기·수정 버튼은 navigate 불사용
│   │                    openDraftOrActiveForEdit(missionId) 헬퍼로 직접 DB 로드 후 필드 복원 + setView('form'/'create') 호출
│   │                    (같은 경로 navigate는 컴포넌트 리마운트 미실행으로 useEffect 미재실행 — D-43 참조)
│   │                  NewMission.jsx: currentEditId state + effectiveEditMode(isEditMode||!!currentEditId) + effectiveEditId(currentEditId||editMissionId)
│   │                    → saveDraft/handleSubmit 모두 effectiveEditMode/effectiveEditId 사용
│   │                    → Dashboard(외부 경로)에서 진입 시 기존 isEditMode/editMissionId 경로 유지
│   │                  메인 의뢰 목록(NewMission): cancelled 포함 5탭 (전체/진행/완료/임시저장/취소)
│   │                    - draft 카드 클릭 / 이어 작성하기 버튼 → openDraftOrActiveForEdit(id)
│   │                    - active+filled=0 카드 수정 버튼 → 동일 openDraftOrActiveForEdit(id)
│   │                    - active 카드 클릭 → 뷰포트 좌하단 토스트 (D-80) / cancelled+company_notified_at IS NOT NULL → navigate('/company/results?id=…') / completed → navigate('/company/results?id=…')
│   │                  서브 의뢰 목록(Preference/Pricing/Email): cancelled 포함 5탭
│   │                    - draft 카드 클릭 / 이어 작성하기 버튼 → openDraftOrActiveForEdit(id)
│   │                    - active+filled=0 카드 수정 버튼 → 동일 openDraftOrActiveForEdit(id)
│   │                    - cancelled+company_notified_at IS NOT NULL 카드 클릭 → navigate('/company/results?id=…') / IS NULL → 토스트 (D-80)
│   │                    - active 카드 클릭 → 뷰포트 좌하단 토스트 / completed 카드 클릭 → navigate('/company/results?id=…')
│   │                  서브 의뢰 폼 단계 (4단계 공통 구조): ★ 개편
│   │                    PreferenceTest: STEPS=['소재 입력','질문 설정','패널 설정','검토']
│   │                      Step 0: 의뢰명(missionTitle, 선택)+소재유형+소재A/B+이미지+제품설명+산업군 통합 ★ 의뢰명 추가
│   │                    PricingTest: STEPS=['가격 페이지','질문 설정','패널 설정','검토']
│   │                      Step 0: 의뢰명(missionTitle, 선택)+가격내용+이미지+제품설명+산업군 통합 ★ 의뢰명 추가
│   │                    ColdEmailTest: STEPS=['이메일 입력','질문 설정','패널 설정','검토']
│   │                      Step 0: 의뢰명(missionTitle, 선택)+이메일원문+제품설명+산업군 통합 ★ 의뢰명 추가
│   │                    Step 3 '검토': 의뢰명 포함 입력 내용 요약 + 패널수·직급·크레딧 표시 + 제출 버튼 (메인의뢰 Step 4와 동일 패턴) ★
│   │                    missionTitle 공통 패턴 (3개 파일):
│   │                      - state: missionTitle ('' 초기값)
│   │                      - shouldBlockNav: missionTitle 포함 (|| missionTitle)
│   │                      - saveDraft: description JSON에 missionTitle 포함
│   │                      - openDraftOrActiveForEdit: parsed.missionTitle 우선 복원 + ms.title 폴백
│   │                      - useEffect editMode 블록: parsed.missionTitle 우선 복원 + ms.title 폴백 (D-77 참조)
│   │                      - handleSubmit: finalTitle = missionTitle.trim() || 타입별 기본값
│   │                      - missions INSERT: title=finalTitle, description JSON에도 missionTitle 저장
│   │   ※ pages/company/QuestionTemplates.jsx: TABS 5개(LP 탭 신규 맨앞) — 마케팅 소재 종합 진단|소재 비교 A/B|가격 페이지|이메일 검증|➕ 커스텀 질문
│   │                탭 이모지: lp/preference/pricing/email 탭은 icon: null (이모지 없음), 커스텀 탭만 icon: '➕'
│   │                "질문 만들기" → "커스텀 질문" (탭 레이블 + 탭 콘텐츠 헤더 모두 변경)
│   │                CUSTOM_CAT_TABS 4개(LP 포함): lp→'랜딩페이지', preference→'광고소재', pricing→'가격', email→'이메일'
│   │                handleUse (전 탭 통일): navigate(tab.path, { state: { templateId, templateName } }) — DB 템플릿만 use_count +1
│   │                load(): DB에 LP 기본 템플릿 없으면 QUESTION_TEMPLATES.lp 로컬 상수로 보완 (D-31)
│   │                미리보기 패널 상단: "이 템플릿으로 의뢰 등록 →" 버튼 (전 탭 공통, handleUse 호출)
│   │                catLabel 맵 양쪽(loadCustomQs, handleSaveCustomQ)에 lp:'랜딩페이지' 추가
│   │                  exports: default PanelTargetStep, calcCredits(panelCount, careerLevels, missionType), calcPanelPayout(careerLevels, missionType), CAREER_LEVELS, MAIN_BASE_PAYOUT(8000), SUB_BASE_PAYOUT(4500)
│   │                  props: plan('starter'|'pro'|null), panelCount, onPanelCount, careerLevels, onCareerLevels, missionType('main'|'sub', 기본 'sub'), creditBalance(number|null, optional)
│   │                  크레딧 배수: 주니어 1.0× / 미들 1.5× / 시니어 2.0×(Pro) / C레벨 3.0×(Pro)
│   │                  calcCredits 공식: 패널수 × 최종가중치 × 의뢰배수 (소수 그대로, CEIL 없음) — 1~2직급=최고값, 3~4직급=1.8상한, 메인×1.5/서브×1.0
│   │                  calcPanelPayout 공식: round(BASE × 최종가중치) — 패널 1인당 정산금, reward_amount DB 저장용
│   │                  sticky footer: 잔여 크레딧(creditBalance!=null 시) + "최대 예상 소모: N 크레딧" + 환불 안내문
│   │                    → credits > creditBalance 이면 소모량 빨간색, 부족 경고 텍스트 표시
│   │                  스타터 제한: 슬라이더 15명 snap-back, 시니어·C레벨 클릭 시 업그레이드 팝업
│   │                  사용: NewMission(Step 3, missionType="main"), PreferenceTest(Step 2, "sub"), PricingTest(Step 2, "sub"), ColdEmailTest(Step 2, "sub")
│   │   ※ pages/company/Results.jsx: 미션 목록 쿼리 — .or('status.eq.completed,and(status.eq.cancelled,company_notified_at.not.is.null)') ★ 변경
│   │                  cancelled 의뢰: earlyCompleteMission → company_notified_at SET 이후에만 목록 노출 (DB 공개 게이트, migration 039)
│   │                  loadFeedback(): purity_passed=true 피드백 표시 (completed|cancelled 공통)
│   │                  기업이 피드백을 처음 볼 수 있는 시점: 완료→complete_mission_and_refund RPC / 취소→earlyCompleteMission(company_notified_at SET)
│   │                  ?id param 진입 시 cancelled 미션이면 setMissionTab('cancelled') 자동 전환 ★
│   │                좌측 패널: missionTab state('all'|'completed'|'cancelled') + 탭 UI(전체/완료/취소) ★ 개편
│   │                  tabFiltered: missionTab='all'→missions 전체 / 그 외→status 일치 필터
│   │                  mainMissions/subMissions: tabFiltered 기준 타입 분리
│   │                  handleMissionTab(tab): setMissionTab + setMainPage(1) + setSubPage(1) 동시 호출
│   │                  타입 뱃지는 서브 미션만 표시 (m.type && m.type !== 'landing_page' 조건), LP검증 뱃지 없음
│   │                  빈 상태: missions.length===0 → "완료된 의뢰가 없습니다." / tabFiltered 0건 → "해당 의뢰가 없습니다." ★ 통합
│   │                좌측 패널 페이지네이션: mainPage/subPage state, PAGE_SIZE=5, ‹ › 화살표 버튼 ★
│   │                  URL ?id 파라미터 진입 시 해당 미션이 속한 페이지로 자동 이동 (auto-page 로직)
│   │                ExpandableText(text, limit=200) — 텍스트 길이 초과 시 더보기/접기 토글
│   │                TruncatedComment — 어노테이션 코멘트 1줄 표시 + DOM overflow 감지(scrollWidth>clientWidth) → 더보기/접기 토글 ★
│   │                ScoreCardRow: display:grid repeat(N, 1fr), maxWidth=N×220px + margin:0 auto → 아이템 수에 따라 폭 제한 후 화면 가운데 정렬 ★
│   │                CommentList: { label, actions, text } 구조, 카드 padding:14px 16px ★
│   │                  헤더: justifyContent:space-between, gap:8, marginBottom:8
│   │                  label: fontSize:12, fontWeight:600 (패널 번호·뱃지 포함)
│   │                  text: fontSize:14, color:var(--text), lineHeight:1.75, fontWeight:500 ★ (메인/서브 통일)
│   │                  HelpfulnessButtons: actions prop으로 전달 → 헤더 우측 끝 배치
│   │                DimTabView (이미지 미션 차원 탭): .dim-tab-layout 좌우 분할 ★
│   │                  .dim-tab-left (flex:1, sticky top:24px): 이미지 어노테이터 — 어노테이션 수 카운트 제거
│   │                  .dim-tab-right (width:320px): 코멘트 목록 + 헤더 "코멘트 N개 · 평균 X.X점"
│   │                  900px↓: flex-direction:column, sticky 해제 (index.css .dim-tab-* 미디어쿼리)
│   │                SummaryTabView (종합 탭): LP 추가 질문(CustomQuestionsSection) → 패널 총평 순서 ★
│   │                  총평 텍스트: fontSize:14, color:var(--text), lineHeight:1.75, fontWeight:500 ★
│   │                CustomQuestionsSection: 점수형(scale) 질문 헤더에 "평균 X.X점" 인라인 뱃지 표시 (질문 텍스트 바로 옆) ★
│   │                  scale 펼침 결과: 전체폭 ScoreCardRow 제거 → 가운데 정렬 컴팩트 카드(minWidth:140, padding:16px 32px) ★
│   │                PreferenceResults: 소재 A/B 2열 카드(이미지+ExpandableText) → 비율 바 → 점수 카드
│   │                PricingResults: 가격 구성(이미지+ExpandableText) → 비율 바 → 점수 카드
│   │                EmailResults: 이메일 원문(ExpandableText, limit=250) → 비율 바 → 점수 카드
│   │   ※ pages/admin/PurityFilter.jsx: subResponseMap state — 초기 load() 시 서브 미션 응답 테이블 일괄 조회 ★
│   │                  feedbacks select: panels(user_id, name, honor_points, experience) — payout 계산용 honor_points·experience 포함 ★
│   │                  approve(id): getPanelReward(panel.honor_points, panel.experience) 계산 → feedbacks.payout_amount 저장 + rejection_penalty_applied=false 초기화
│   │                    rejection_penalty_applied=true 인 경우 add_panel_honor_points(+5) RPC fire-and-forget (HP 복원)
│   │                  reject(id): feedbacks.rejection_penalty_applied=true 저장
│   │                  bulkApprove: 상태 일괄 UPDATE 후 payoutMap({[id]: amount}) 기반 개별 UPDATE + HP 복원 루프
│   │                  bulkReject: rejection_penalty_applied=true 일괄 UPDATE 포함
│   │                  목록 정렬: .order('created_at', { ascending: true }) — 오래된 순(먼저 낸 피드백 먼저) ★
│   │                  typeFilter state: 'all'|'main'|'sub' — 상태 탭 아래 전체/메인/서브 pill 버튼 (각 건수 표시) ★
│   │                    필터 파이프라인: filteredBase(상태) → filteredByType(타입) → filtered(65점 서브필터)
│   │                    SUB_TYPES = ['preference','pricing','email']; 상태 탭 전환 시 typeFilter 초기화
│   │                  딥링크 안정화: pendingDeeplink = location.state?.feedbackId 를 load() useEffect 최상단에서 캡처
│   │                    pendingDeeplink 있으면 fbs[0] 자동선택 생략 → 딥링크 effect 단독으로 setSelected(X) ★
│   │                    fb lookup: feedbacks.find(f=>f.id===selected) — filtered 아닌 전체 배열 사용 (D-56/D-57)
│   │                  딥링크: useLocation + highlightId state — location.state.feedbackId → 탭 전환 + 페이지 이동 + setSelected + 초록 하이라이트 3초 ★
│   │                  parseLPDesc: briefText / selectedQuestions / product / lpUrl / focusAreas / industry 추출 ★
│   │                    LP 미션 정보 카드: hasAnyInfo 조건 (하나라도 있으면 표시) — briefText 없어도 제품명/업종/LP URL/집중영역 칩 표시 ★
│   │                  prefIds/priceIds/emailIds 추출 → Promise.all 병렬 조회 → feedbackId 키 맵으로 저장
│   │                  getScore(f) 헬퍼: 타입 분기 → 메인=calcPurityScore(f), 서브=calcSubPurityScore(subResponseMap[f.id]||null, type)
│   │                  calcPurityScore: base20 + 길이(max20) + 섹션균형(max10) + 구체성키워드(×4,max25) + 실행(×5,max25) + AI패널티(×-12,min-30)
│   │                    섹션균형 계산: 텍스트 미션 = suggestions를 \n\n으로 분리 후 본문 10자+ 섹션 수 카운트 (4개+→10, 2~3→4, 0~1→0)
│   │                                  이미지 미션 = suggestions 라인에서 [DIM / X점] 패턴으로 고유 차원 수 카운트 (동일 기준)
│   │                  calcSubPurityScore: base15 + 코멘트길이(단계적,max30) + 구체성키워드(×3,max10) + 지표충실도(max45)
│   │                  65점 기준: 승인권장(≥65) / 검토(45~64) / 반려권장(<45) — 뱃지/색상/목록 라벨 통일
│   │                  pendingSubFilter: 'all'|'above65'|'below65' — 검토 대기 탭 서브필터 버튼
│   │                  checkedIds Set + bulkApprove/bulkReject/toggleCheck/toggleAll — 일괄 처리 패턴
│   │                  pendingPageIds/allPageChecked/toggleAll: pagedList 선언 이후에 배치 (D-51 참조)
│   │                  Pagination: 슬라이딩 5-윈도우(WINDOW=5) + «/» 5페이지 점프 버튼
│   │                    winStart=max(1, page-2), winEnd=min(totalPages, winStart+4) 계산 후 pageNums 슬라이스
│   │   ※ pages/admin/Missions.jsx: 딥링크 수신 + 인라인 피드백 상세 패널 ★
│   │                  쿼리: .neq('status', 'draft') + feedbacks(id, status, purity_passed, created_at, panels(name)) join 확장
│   │                  selectedMission state — 단일 컬럼 레이아웃 (우측 고정 패널 폐지, panel-mgmt-layout 그리드 제거)
│   │                  MissionDetail: 클릭 카드 바로 아래 인라인 렌더 (borderLeft 파란 세로선 들여쓰기)
│   │                    내부 state: fbFilter('all'|'pending'|'approved'|'rejected') + detailPage
│   │                    피드백 탭 4개: 전체(N) / 검토중(N) / 승인(N) / 반려(N)
│   │                    전체 탭 정렬: [...approved, ...rejected, ...pending] 순
│   │                    탭 전환 시 detailPage 자동 1 리셋 (handleTab 헬퍼)
│   │                    DETAIL_PAGE_SIZE=5, getFeedbackStatus(f): purity_passed→green / rejected→red / else→gold
│   │                    피드백 행 클릭: navigate('/admin/purity', { state: { feedbackId } })
│   │                  MissionCard: isSelected/onSelect props + Card onClick + e.stopPropagation() on inner Btns
│   │                    onCancelMission(id) / onCompleteMission(m) / onReactivateMission({id,label,desc}) / onEarlyComplete(m) props
│   │                    → 각각 setConfirmCancel / setConfirmComplete / setConfirmReactivate / setConfirmEarlyComplete 연결
│   │                  confirmCancel state: ConfirmModal(danger) → updateStatus(id, 'cancelled') + 기업 취소 알림 발송
│   │                  confirmComplete state: ConfirmModal → 승인 건수(m.feedbacks.filter(f.purity_passed).length) 동적 표시 → complete_mission_and_refund RPC + 기업 완료 알림 발송
│   │                  confirmEarlyComplete state: 조기 종료 미션 완료 처리 — status 유지(cancelled), 크레딧 변경 없음
│   │                    earlyCompleteError state + earlyCompleteMission(m) 핸들러:
│   │                      → missions.company_notified_at = now() UPDATE (기업 공개 게이트 해제, migration 039)
│   │                      → sendNotification(기업, actionUrl='/company/results?id=...') fire-and-forget
│   │                      → setMissions 리로드 + setSelectedMission(prev→data.find) 동기화 ★
│   │                    "완료 처리" 버튼: m.status==='cancelled' && allFbs.length>0 && pendingFbs.length===0 일 때만 활성화
│   │                  confirmReactivate state: { id, label, desc, fromStatus } — fromStatus로 completed/cancelled 분기
│   │                    fromStatus='completed': reactivateCompleted(id) 호출 → reactivate_mission_and_reclaim RPC + 기업 재진행 알림 발송
│   │                      INSUFFICIENT_CREDITS 에러 시 잔액/필요량 표시 + 상태 변경 없음
│   │                      성공 시 missions/selectedMission state를 status:'active'로 동기화
│   │                    fromStatus='cancelled': updateStatus(id, 'active') + 기업 재개 알림 발송 — 취소→재개는 크레딧 처리 없음
│   │                  알림 발송: sendNotification(companies.user_id, ...) — updateStatus/reactivateCompleted 성공 직후
│   │                    missions select 쿼리에 companies(name, user_id) 포함 → foundM.companies.user_id 접근 가능
│   │                    확인 모달 desc에 "기업에게 [상태변경] 알림이 발송됩니다." 문구 포함
│   │                  updateStatus/deleteMission 후 setSelectedMission 동기화
│   │                  취소 탭 뱃지: cancelledPendingCount — status='cancelled' 미션 중 미처리(검토중) 피드백 있는 건수 실시간 표시
│   │                  딥링크 2경로: location.state.missionId (내부 navigate) + ?missionId= 쿼리 파라미터 (알림 클릭)
│   │                    쿼리 파라미터 처리: useSearchParams → 동일 setFilter + 페이지 계산 + highlight → navigate(pathname, {replace:true}) 클리어
│   │                  isHighlighted: outline 2px solid var(--accent) 3초 후 자동 해제, window.history.replaceState로 state 클리어
│   │   ※ pages/admin/CompanyManagement.jsx: 의뢰 내역 전체 페이지네이션 ★
│   │                  recentMissions: 5건 제한 제거 → 전체 저장, missionPage state + DETAIL_PAGE_SIZE=5
│   │                  onMissionClick prop: navigate('/admin/missions', { state: { missionId } })
│   │   ※ pages/admin/PanelManagement.jsx: 제출 내역 전체 페이지네이션 ★
│   │                  loadPanelDetail: .limit(5) 제거 → 전체 로드, feedbackDetailPage state + DETAIL_PAGE_SIZE=5
│   │                  onFeedbackClick prop: navigate('/admin/purity', { state: { feedbackId } })
│   └── pages/
│       ├── Landing.jsx, Login.jsx, Signup.jsx, ShareResult.jsx (/share/:token 공개), OAuthCallback.jsx (/oauth/callback 공개) ★
│   ※ Login.jsx: 역할 선택(기업/패널) → Google 로그인 버튼 + 이메일/비밀번호 폼 구조 ★ 개편
│              role state + googleLoading state / handleGoogle(): signInWithGoogle(role) 호출
│              GoogleIcon 인라인 SVG 컴포넌트 (Google 공식 4색)
│   ※ OAuthCallback.jsx: OAuth 리다이렉트 처리 전용 페이지 ★ 신규
│              getSession() → existingRole 있으면 즉시 대시보드 이동
│              신규 유저: localStorage('purit_oauth_role') 읽어 updateUser(role) + companies/panels INSERT
│              user.user_metadata.full_name || name || email prefix → displayName으로 사용
│   ※ AuthContext.jsx: signInWithGoogle(role) 추가 ★ 신규
│              localStorage.setItem('purit_oauth_role', role) → signInWithOAuth({ provider:'google', redirectTo: origin+'/oauth/callback' })
│              context value에 signInWithGoogle 노출
│       ├── company/  (17개 — 라우트 목록 참조)
│       ├── panel/    (5개 — 라우트 목록 참조)
│       └── admin/    (7개 — 라우트 목록 참조)
├── scripts/   (개발·테스트 전용 Node.js 유틸리티 — 배포 대상 아님)
│   ├── create-panel-accounts.mjs      (Supabase Admin API로 테스트 패널 계정 일괄 생성)
│   ├── create-test-main-mission.mjs   (어드민 계정으로 테스트 메인 의뢰 생성 — SVG 이미지 5장 Storage 업로드 포함)
│   ├── submit-greencart-feedbacks.mjs         (GreenCart 미션 전용 피드백·어노테이션 자동 제출)
│   ├── submit-active-mission-feedbacks.mjs    (최신 활성 메인 의뢰에 패널 1~10 완전한 피드백 자동 제출 — 어노테이션·custom_answers·honor_points 포함)
│   └── update-panel-profiles.mjs              (panel1~10 직군·경력 업데이트 — .env.local 필요)
├── supabase/
│   ├── migrations/  001(DDL) 002(시드) 003(admin) 004(cancel_fb_rpc) 005(ai_reports)
│   │                006(annotations) 007(subtest) 008(templates) 009(gamification)
│   │                010(sharing) 011(notification_triggers) 012(typed_template_questions)
│   │                013(custom_question_rls) 014(feedbacks_custom_answers)
│   │                015(honor_level_system: panels.honor_points·honor_decay_applied_at, feedback_helpfulness_ratings, RPC 3개)
│   │                016(credit_system: companies.credit_balance, missions.credits_reserved·credits_consumed, RPC 3개: grant_plan_credits·reserve_mission_credits·complete_mission_and_refund)
│   │                017(recalc_mission_consumed: 초기 버전 — 018로 대체됨)
│   │                018(credits_numeric: 크레딧 컬럼 INTEGER→NUMERIC(10,2), CEIL 제거, recalc_mission_consumed·reserve_mission_credits·complete_mission_and_refund 재정의)
│   │                020(honor_points_security: add_panel_honor_points·apply_honor_decay·rate_panel_feedback_helpfulness RPC에 호출자 검증 추가 — 본인 or admin만 허용)
│   │                021(badge_system_v2: panels.selected_badge 컬럼 추가, check_and_award_badges RPC — 12개 계층형 뱃지 자동 달성)
│   │                023(bug_report_reply: bug_reports 테이블 admin_memo 컬럼 추가)
│   │                024(admin_company_management: admin_add_credits RPC — 어드민 전용 크레딧 직접 지급)
│   │                025(admin_change_plan: admin_change_plan RPC — 어드민 전용 플랜+크레딧 동시 변경, grant_plan_credits의 owner 제한 우회)
│   │                026(fix_rls_user_metadata: RLS 정책 user_metadata→app_metadata 교체 + 어드민 app_metadata role 설정 — D-58 참조)
│   │                027(reactivate_mission_and_reclaim: 완료 미션 재진행 시 환불 크레딧 회수 RPC — D-60 참조)
│   │                028(fix_notification_triggers: notify_admin_on_mission draft 제외 재정의 + notify_company_on_slots_full actionUrl·워딩 수정)
│   │                029(notify_admin_on_draft_activate: draft→active UPDATE 트리거 — notify_admin_on_mission 재사용)
│   │                030(notifications_target_role: notifications.target_role TEXT 컬럼 추가, notify_admin/notify_company 트리거 target_role 포함 재정의)
│   │                031(feedbacks_payout_tracking: feedbacks.payout_amount NUMERIC(10,2) + rejection_penalty_applied BOOLEAN 컬럼 추가 — 정산 금액 스냅샷·HP 복원 추적)
│   │                032(fix_honor_rpc_app_metadata: add_panel_honor_points·apply_honor_decay RPC의 admin 검증을 raw_user_meta_data→raw_app_meta_data로 교체 — D-58 보안 패치)
│   │                033(redirect_slots_full_to_admin: 슬롯 완료 알림 수신자 기업→어드민으로 변경 — notify_company_on_slots_full 트리거 재정의)
│   │                034(feedback_timeout_slots: feedbacks.submission_deadline·rejection_deadline 컬럼 추가 + decrement_mission_filled_count·expire_panel_drafts·cancel_panel_feedback RPC 정의)
│   │                  submission_deadline: 수락 후 제출 마감 — 메인 4h / 서브 2h (클라이언트 계산, RPC p_submission_deadline 파라미터로 전달)
│   │                  rejection_deadline:  반려 후 재제출 마감 — 메인 4h / 서브 2h (어드민 PurityFilter에서 반려 시점 기준으로 설정)
│   │                035(redesign_slot_on_accept: 슬롯 예약 타이밍 재설계 — accept_mission_slot·reaccept_rejected_feedback 신규 RPC + cancel_panel_feedback·expire_panel_drafts 재정의)
│   │                  수락 클릭 → filled_count +1 (accept_mission_slot 원자적), 제출 시 filled_count 변동 없음
│   │                  반려 → filled_count -1 (decrement_mission_filled_count), 재작성 클릭 → +1 (reaccept_rejected_feedback 원자적)
│   │                  동시 경쟁 처리: WHERE filled_count < panel_count + PostgreSQL 행 잠금 → 마지막 슬롯 동시 수락 / 반려 패널 재진행 vs 신규 수락 동시 경쟁 모두 직렬화
│   │                  draft 만료: decrement + 삭제 / rejected 만료: 삭제만 (expire_panel_drafts)
│   │                040(alimtalk_setup: feedbacks.deadline_alimtalk_sent BOOLEAN DEFAULT FALSE + companies.phone TEXT 컬럼 추가 — 알림톡 발송 인프라)
│   │                041(pg_cron_deadline_alimtalk: pg_cron + pg_net으로 30분마다 check-deadline-alimtalk Edge Function 자동 호출 — 값 교체 후 SQL Editor 실행 필요)
│   │                043(fix_decrement_slot_auth: decrement_mission_filled_count user_metadata OR 패턴 — app_metadata 미설정 어드민 호환, 049에서 보안 강화)
│   │                044(fix_reaccept_slot_bypass: reaccept_rejected_feedback — FOR UPDATE 행 잠금 + 슬롯 복구 경로 분기)
│   │                045(fix_reaccept_no_lock: reaccept_rejected_feedback — PgBouncer FOR UPDATE 오류 제거)
│   │                046(fix_reaccept_ownership_check: reaccept_rejected_feedback 소유권 체크 — panels.user_id=auth.uid() 패턴. SQL 한글 자모 혼입 구문 오류, 049 재정의로 보정)
│   │                047(fix_reaccept_slot_gate: reaccept_rejected_feedback 최종 — WHERE filled_count < panel_count 원자적 게이트)
│   │                048(restore_mission_slot: 반려→검토중 되돌리기 시 filled_count +1 복원 — decrement의 역연산 RPC)
│   │                049(fix_slot_rpc_security: decrement+restore D-58 수정(app_metadata 단독) + restore LEAST 오버플로우 가드 + reaccept 047 재적용 안전망) ★
│   │                050(dismiss_rejected_feedback: feedbacks.dismissed BOOLEAN 컬럼 추가 + dismiss_rejected_feedback·delete_rejected_feedback RPC 2개)
│   │   ※ pages/panel/Missions.jsx: feedbackMap = { [missionId]: { status, id, suggestions, rejection_deadline, dismissed } } ★ dismissed 추가
│   │                  rejection_deadline 만료 항목 → [수정 필요] 탭 클라이언트 필터로 자동 제외
│   │                  dismissed=true 항목 → [수정 필요] 탭 클라이언트 필터로 자동 제외 + rejectedCount 카운트 제외 ★
│   │                  [수정 필요] 탭 missionEnded/slotsFull 항목: 재작성 불가 텍스트 + '삭제' 버튼 → dismiss_rejected_feedback RPC (dismissed=true, 숨김)
│   │                  [이어하기] 탭 missionEnded 항목(ms.status!=='active'): '이어하기 →' 대신 '의뢰가 종료되었습니다' 텍스트 표시 ★
│   │                  수락 핸들러: isSubM = type in ['preference','pricing','email'] → subDl = now + (isSubM?2:4)h → accept_mission_slot RPC(p_submission_deadline=subDl) 호출
│   │   ※ pages/panel/ActiveMission.jsx: 폼 뷰 진입 시 deadline 처리
│   │                  missionEnded state: draft 진입 시 ms.status !== 'active' → true → "의뢰가 종료되었습니다" 화면 (폼 미렌더) ★
│   │                    missionEnded 화면: "초안 삭제하기" 버튼 → cancel_panel_feedback RPC → /panel/missions 이동
│   │                  deadlineBanner state: { label: '제출 마감'|'재제출 마감', value: Date } → 브리핑·폼 상단 배너 표시
│   │                  deadlineExpired state: true 시 "제출 기간이 만료되었습니다" 화면 (폼 미렌더)
│   │                    deadlineExpired 화면: "초안 삭제하기" 버튼 → cancel_panel_feedback RPC ★
│   │                  최초 draft 진입: ms.status!=='active' → missionEnded=true / fb.submission_deadline < now → deadlineExpired=true / 유효 → deadlineBanner 설정
│   │                  재작성 모드 진입: fb.rejection_deadline < now(클라이언트 사전 체크) → deadlineExpired=true, RPC 미호출
│   │                    체크 통과 → reaccept_rejected_feedback RPC(원자적 슬롯 재예약, 슬롯 마감 or 서버 만료 시 false 반환) → 실패 시 deadlineExpired=true
│   └── functions/   generate-ai-report/index.ts  (Deno, Anthropic API → ai_reports INSERT)
│                    send-alimtalk/index.ts  (Deno, notifications INSERT 웹훅 → Solapi 알림톡 발송)
│                      notifications.title → TITLE_TO_TPL 맵으로 템플릿 ID 결정 (7개 이벤트)
│                      패널: panels(phone, phone_verified) / 기업: companies(phone) 전화번호 조회 후 발송
│                    check-deadline-alimtalk/index.ts  (Deno, pg_cron 30분마다 호출 → 마감 1시간 전 알림톡)
│                      최초 draft(submission_deadline 임박) + 재제출 draft/rejected(rejection_deadline 임박) 체크
│                      발송 후 feedbacks.deadline_alimtalk_sent = true (중복 방지)
│                    _shared/solapi.ts  (Deno, Solapi 알림톡 공통 헬퍼 — HMAC-SHA256 인증 + sendAlimtalk 함수)
├── assets/    purit-logo.html (브랜드 로고 — 아이소메트릭 큐브 다이아몬드 프레임, Canvas 렌더링, Light/Dark/Mono 변형 포함)
├── MORNING_REPORT.md, index.html, vite.config.js, vercel.json, package.json

================================================================
## 라우트 전체 목록 (32개)
================================================================

[공개]
  /                          → Landing.jsx
  /login                     → Login.jsx
  /signup                    → Signup.jsx
  /oauth/callback            → OAuthCallback.jsx (공개, Google OAuth 리다이렉트 처리) ★ 신규
  /share/:token              → ShareResult.jsx (공개, 인증 불필요)

[Company 포털] — RoleRoute role="company"
  /company                   → Dashboard.jsx
  /company/notifications     → NotificationCenter.jsx
  /company/new               → NewMission.jsx
  /company/results           → Results.jsx
  /company/diagnosis         → Diagnosis.jsx
  /company/timeline          → TimelineTracker.jsx     ★ 신규
  /company/preference        → PreferenceTest.jsx
  /company/pricing-test      → PricingTest.jsx
  /company/email-test        → ColdEmailTest.jsx
  /company/templates         → QuestionTemplates.jsx
  /company/icp               → ICPResearch.jsx
  /company/icp-pulse         → ICPPulse.jsx
  /company/brand             → BrandTracking.jsx
  /company/report            → AIReport.jsx
  /company/account           → Account.jsx          ★ 신규
  /company/settings          → Settings.jsx
  /company/plans             → Pricing.jsx
  /company/bug-reports       → BugReports.jsx       ★ 신규

[Panel 포털] — RoleRoute role="panel"
  /panel                     → Dashboard.jsx
  /panel/notifications       → NotificationCenter.jsx
  /panel/missions            → Missions.jsx          (미션 관리: 새로운 미션·진행 중·수정 필요 탭 통합)
  /panel/active              → ActiveMission.jsx     (id 없음: 수락한 미션 목록 뷰 / id 있음: 폼 뷰)
  /panel/history             → History.jsx           (정산 내역: 정산 대기·정산 완료·지급 거절 탭)
  /panel/profile             → Profile.jsx
  /panel/bug-reports         → BugReports.jsx       ★ 신규

[Admin 포털] — RoleRoute role="admin"
  /admin                     → Dashboard.jsx
  /admin/notifications       → NotificationCenter.jsx
  /admin/panels              → PanelManagement.jsx
  /admin/companies           → CompanyManagement.jsx ★ 신규
  /admin/missions            → Missions.jsx
  /admin/purity              → PurityFilter.jsx
  /admin/revenue             → Revenue.jsx
  /admin/reports             → BugReports.jsx
  /admin/account             → Account.jsx

================================================================
## DB 스키마 & RPC 함수 목록
================================================================

※ 토큰 절약을 위해 4_DBSchema.txt로 분리됨.
  DB 관련 작업 시에만 Read 도구로 직접 로드할 것 (CLAUDE.md 조건부 로드 규칙 준수).
  주요 테이블: auth.users / companies / panels / missions / feedbacks /
              feedback_annotations / preference_responses / pricing_responses / email_responses

================================================================
## 반응형 CSS 유틸리티 클래스 (src/index.css)

  반응형 구현 방식: 인라인 React 스타일 시스템 유지 + CSS 클래스 !important 오버라이드
  (인라인 스타일이 CSS 클래스보다 specificity가 높으므로 반응형 오버라이드는 !important 필수)

  ### 기존 (미션 카드)
  .mc-row    — 미션 카드 내부 좌/우 2열 레이아웃 (640px 이하: 1열로 전환)
  .mc-right  — 미션 카드 우측 영역 (보상금·슬롯·버튼 묶음)
  .page-wrap — 페이지 컨테이너 (640px 이하: 패딩 20px 16px로 축소)
  ※ 미션 카드 적용 대상: company/Dashboard, panel/Missions, panel/ActiveMission(목록뷰), admin/Missions

  ### 모바일 사이드바 (≤768px: 햄버거 드로어 전환)
  .sidebar-layout-root  — 루트 flex 컨테이너 (모바일: display:block)
  .sidebar-drawer       — aside 래퍼 (모바일: position:fixed + transform:translateX(-100%))
  .sidebar-drawer.is-open — 드로어 열림 상태 (transform:translateX(0))
  .sidebar-backdrop     — 드로어 오픈 시 반투명 배경 오버레이 (모바일에서만 렌더)
  .sidebar-collapse-btn — 데스크탑 접기 버튼 (모바일: display:none)
  .mobile-topbar        — 모바일 전용 상단 바 (햄버거 + PURIT 로고, 데스크탑: display:none)
  ※ Sidebar.jsx: isMobile state(matchMedia), mobileOpen state, Menu/X 아이콘 (lucide-react)

  ### 랜딩 그리드 (Landing.jsx)
  .landing-nav-inner    — 네비 내부 max-width 컨테이너
  .landing-hero         — 히어로 섹션 패딩 (모바일: 56px 16px)
  .landing-section      — 일반 섹션 패딩 (모바일: 56px 16px)
  .landing-footer-inner — 푸터 내부 컨테이너
  .landing-grid-4       — 4열 → 모바일 2열 (900px↓: 2열, 600px↓: 2열)
  .stat-divider-right   — 숫자 임팩트 섹션 아이템 우측 구분선 (border-right: 1px solid #C8C8C8)
                          모바일 900px↓: nth-child(2n) border-right 자동 제거 (2×2 우측 외곽 테두리 방지)
  ※ Landing.jsx 숫자 임팩트 섹션(48h/100%/40%/800만원): W 래퍼 + landing-grid-4 사용
    히어로 h1: fontSize clamp(32px, 4.5vw, 52px), lineHeight 1.25
    통계 숫자: fontSize clamp(28px, 3.2vw, 46px) (가독성 + "800만 원" 한 줄 수용)
  .landing-grid-3       — 3열 → 모바일 1열 (900px↓: 2열, 600px↓: 1열)
  .landing-grid-2       — 2열 → 모바일 1열 (900px↓: 1열)
  .landing-grid-3-steps — 3열 스텝 → 모바일 1열 (900px↓: 2열, 600px↓: 1열)
  ※ 이전 Landing.jsx의 <style> 태그(.purit-grid-*)는 JSX에 연결되지 않아 무효였음 → 삭제 후 교체
  .expert-tag       — ExpertPanelCloud 플로팅 태그 (font-size:17px, white-space:nowrap; 모바일: 12px)
  .expert-cloud-box — ExpertPanelCloud 박스 컨테이너 (height:300px; 모바일: 240px)

  ### 대시보드 그리드 (3개 Dashboard.jsx)
  .dash-header-row      — 헤더 flex row (모바일: flex-wrap)
  .dash-stat-grid-4     — 4열 통계 카드 (768px↓: 2열) — admin/Dashboard
  .dash-stat-grid-5     — 5열 통계 카드 (768px↓: 2열) — company/Dashboard
  .dash-stat-grid-3     — 3열 통계 카드 (768px↓: 2열, 480px↓: 1열) — panel/Dashboard
  .dash-two-col         — 1fr+300px 2열 (768px↓: 1열) — admin/Dashboard
  ※ company/Dashboard 차트 섹션: className="chart-three-col" + 인라인 repeat(3, 1fr)
    - 차트 데이터 소스: completedIds(status='completed' 미션) + feedbacks.eq('purity_passed', true) ★ 수정
      활성(active) 의뢰·미승인 피드백은 차트에서 제외 — 어드민 완료 처리 이후에야 반영
    - 3열: 레이더 / KPI 게이지+5축 바 / 긍부정 분포(메인+서브 섹션)
    - 긍부정 분포: mainSentPage·subSentPage state, SENT_SIZE=4, ‹ › 화살표 캐러셀
    - DB 추가 fetch: preference_responses·pricing_responses·email_responses (서브 의뢰 긍정 비율용, 완료 미션만)
    - 차트 기간 필터: chartPeriod state ('all'|'3m'|'1m', 기본 'all') ★ 신규
      · 전체 / 최근 3개월(90일) / 최근 1개월(30일) pill 버튼 — 차트 섹션 우측 상단 배치
      · periodCutoff = chartPeriod==='all' ? null : new Date(Date.now() - N*86400000)
      · 파생 배열: chartFeedbacks·chartPrefResps·chartPriceResps·chartEmailResps (periodCutoff 기준 created_at 필터)
      · Stats 카드: 원본 feedbacks/prefResps 등 전체 기간 배열 사용 (필터 미적용)
      · 기간 전환 onClick: setChartPeriod(val) + setMainSentPage(1) + setSubSentPage(1) 동시 호출 (D-74 참조)
  ※ 전체 페이지(28개): className="page-wrap" 적용 완료 → 모바일 패딩 자동 축소

  ### 반응형 레이아웃 클래스 (★ 2026-05-08 추가)
  .results-layout       — 마스터-디테일 230px 1fr 그리드 (768px↓: 1fr) — Results.jsx, TextMission
  .results-sidebar      — sticky 좌측 패널 (768px↓: static + 스크롤 박스)
  .purity-layout        — PurityFilter 260px 1fr 그리드 (768px↓: 1fr)
  .panel-mgmt-layout    — PanelManagement 조건부 그리드 (768px↓: 1fr)
  .chart-three-col      — company/Dashboard 차트 3열 (900px↓: 2열, 640px↓: 1열)
  .plans-grid           — Pricing.jsx 플랜 카드 3열 (900px↓: 2열, 640px↓: 1열)
  .stat-inline-three    — border 트릭 3열 통계 (640px↓: 2열, 400px↓: 1열) — History.jsx
  .stat-inline-four     — border 트릭 4열 통계 (768px↓: 2열) — PanelManagement.jsx
  .form-two-col         — 2열 폼 그리드 (640px↓: 1열) — NewMission 페르소나, PreferenceTest A/B, Results 소재
  .score-card-row       — 동적 점수 카드 행 (640px↓: 2열) — ScoreCardRow 컴포넌트
  .score-five-col       — 5차원 평균 점수 (768px↓: 3열, 480px↓: 2열) — Results.jsx 종합탭
  .table-scroll         — 테이블 가로 스크롤 래퍼 — admin/Dashboard, PanelManagement, Revenue, TimelineTracker
  .confirm-modal-inner  — ConfirmModal 내부 박스 (480px↓: 88vw, padding 축소)
  ※ 패턴: className 부여 + 인라인 스타일 유지(데스크탑) + CSS !important 오버라이드(모바일) — D-38 준수

  ### 테마 색상 시스템 (index.css CSS 변수)
  --bg:          #F8FAFC  (쿨그레이 배경)
  --bg-2:        #F1F5F9
  --bg-3:        #E2E8F0
  --surface-2:   #F8FAFC
  --border:      #E2E8F0
  --border-light:#EEF2F6
  --accent:      #10367D  (네이비, 구 오렌지 #CF6A44에서 전체 통일)
  --accent-2:    #0C2A62
  --accent-dim:  rgba(16,54,125,0.10)
  --accent-dim2: rgba(16,54,125,0.05)
  --text:        #0F172A  (주 텍스트)
  --text-2:      #4B556D  (보조 텍스트, 구 #475569에서 가독성 개선)
  --text-3:      #8598AA  (3차 텍스트·레이블, 구 #94A3B8에서 가독성 개선)

  ★ 텍스트 색상 원칙 (기업·어드민·패널 포털 전체):
    - 모든 텍스트·숫자·레이블: var(--text) / var(--text-2) / var(--text-3) 사용
    - var(--accent)(#10367D 네이비)는 버튼/뱃지 배경, 진행바 채움, 차트 fill/stroke에만 허용
    - 텍스트에 네이비 사용 금지 — 차트 차원 색이 텍스트 레이블에도 적용될 경우 #6366F1(인디고) 대체
    - 탭 활성 글자·밑줄: var(--text) 사용 (var(--accent) 금지) — 9개 파일 일괄 적용 완료
      적용 대상: QuestionTemplates·NewMission·ColdEmailTest·PreferenceTest·PricingTest·
                company/Account·admin/Account·company/Settings·company/Dashboard
    - 예외(의도적 색상 코딩): warning ⚠ → #F59E0B(amber), 중간점수 verdict → #F59E0B, EXPERT tier → #92400E
    - 랜딩페이지(Landing.jsx)는 이 원칙 적용 제외

  ※ Sidebar <aside> bg: #FFFFFF (순백) + borderRight: 1px solid #E9ECF0
  ※ <main> bg: #F8FAFC (모든 서브페이지 통일)
  ※ Landing.jsx: PRIMARY = '#10367D', BG = '#F8FAFC', BG2 = '#EDF0F4', BG3 = '#DCDCDC'

================================================================
## 환경 변수 (.env)
================================================================

  VITE_SUPABASE_URL         = https://[project].supabase.co
  VITE_SUPABASE_ANON_KEY    = [anon public key]
  SUPABASE_SERVICE_ROLE_KEY = [service role secret key]  ← 서버/스크립트 전용, VITE_ 없음

  [Supabase Edge Function Secrets — supabase secrets set 명령어로 등록]
  ANTHROPIC_API_KEY = sk-ant-api03-...  ← generate-ai-report 함수에서 사용
                                           미설정 시 Mock 응답 폴백 (서비스 중단 없음)

  ※ Vite 규칙: 클라이언트 노출 변수는 반드시 VITE_ 접두사 필요
  ※ import.meta.env.VITE_* 로 접근
  ※ SUPABASE_SERVICE_ROLE_KEY 는 클라이언트에 절대 노출 금지
