# 프로젝트 통제 원칙 (PURIT)
이 프로젝트의 모든 작업은 다음 핵심 문서를 유일한 진실의 원천(SSOT)으로 삼습니다.

## 상시 로드 문서 (매 대화 자동 로드)
작업 시 항상 이 파일들을 최우선으로 참고하십시오. 이 2개 파일에는 서비스 전체 개요·
공통 규칙·기술 스택·라우트·DB 개요가 담겨 있어 매 대화의 기반이 됩니다:
- @1_PRD.txt        (서비스 개요 · 인증 흐름 · 공용 기능 · 공용 라우트 — 역할별 상세는 분리됨)
- @2_Architecture.txt (기술 스택 · 폴더 구조 · 라우트 목록 · CSS 유틸 · Supabase 최적화 원칙 — 파일별 구현 상세는 분리됨)

## 조건부 로드 문서 (자동 로드 금지 — 해당 조건에서만 Read 도구로 직접 읽을 것)

### 3_Error_Log.txt — 재발 방지 원칙 & D-xx 버그 패턴 (코딩 전 점검표)
⚠️ 이 문서는 "찾아보는 참고서"가 아니라 "같은 버그를 또 내지 않기 위한 코딩 전 점검표"입니다.
   디버깅 중 뒤늦게 읽으면 이미 함정에 빠진 뒤이므로, 코드를 건드리기 "전에" 읽어야 효과가 있습니다.
✅ 다음 상황에서는 반드시 Read 도구로 먼저 읽을 것 (예외 없음):
   - 코드 파일(.jsx/.js/.ts/.sql 등)을 작성·수정·디버깅하기 전
   - Supabase 쿼리·RPC·RLS·트리거·마이그레이션을 작성·수정하기 전
   - 버그 원인을 조사하거나 기존 동작을 바꾸기 전
❌ 순수 질문 답변·기획·문서(.txt/.md) 편집만 하는 작업에서는 읽지 말 것
   ※ 환경 특이적 일부 패턴은 3_Error_Log_archive.txt로 분리됨 (아래 참조)

### 1_PRD_company.txt / 1_PRD_panel.txt / 1_PRD_admin.txt — 역할별 포털 기능 상세
✅ 해당 포털을 작업할 때만 Read 도구로 직접 읽을 것:
   - 1_PRD_company.txt — /company (기업) 포털 페이지·기능 수정·추가 전
   - 1_PRD_panel.txt   — /panel (패널) 포털 페이지·기능 수정·추가 전
   - 1_PRD_admin.txt   — /admin (어드민) 포털 페이지·기능 수정·추가 전
❌ 다른 포털·공용 작업에서는 읽지 말 것 (1_PRD.txt에 개요·공용 기능 있음)

### 5_CodeMap.txt — 페이지·컴포넌트별 구현 상세 (state/헬퍼/패턴)
✅ 다음 상황에서만 Read 도구로 직접 읽을 것:
   - 특정 페이지/컴포넌트 파일(예: NewMission.jsx, PurityFilter.jsx, ActiveMission.jsx 등)을
     수정·디버깅하기 직전 — 해당 파일 섹션만 참고
❌ 신규 기능 설계·단순 라우트 추가·공용 컴포넌트 작업에서는 읽지 말 것

### 3_Error_Log_archive.txt — 환경 특이적·일회성 이슈 아카이브 (D-48·D-63·D-81·D-84·D-95)
✅ 다음 상황에서만 Read 도구로 직접 읽을 것:
   - 랜딩 애니메이션 / Windows 스크립트(.mjs) / SQL 마이그레이션 편집 / 가입 파일 업로드 작업 중
     해당 영역의 함정 확인이 필요할 때
   - 본문(3_Error_Log.txt)에 없는 D 번호([D-48] 등) 교차참조를 확인할 때
❌ 그 외 일반 개발에서는 읽지 말 것

### 4_DBSchema.txt — DB 스키마 & RPC 함수 목록
✅ 다음 상황에서만 Read 도구로 직접 읽을 것:
   - 새 테이블·컬럼·인덱스·RLS 정책 추가/수정 전
   - 새 RPC 함수 작성 전
   - Supabase 쿼리 작성 시 컬럼명·FK 관계 확인이 필요할 때
   - 마이그레이션 파일(supabase/migrations/) 수정 시
❌ 단순 UI 변경, 라우트 추가, 기존 쿼리 복사·수정 등의 작업에서는 읽지 말 것

### 4_Backlog.txt — 미구현·재설계 예정 기능 목록
✅ 다음 상황에서만 Read 도구로 직접 읽을 것:
   - 미구현·재설계 예정 기능을 개발하기 전 (계획 확인)
   - "작전 종료" 시 새로 발견된 미구현 이슈나 재설계 계획이 있으면 추가
❌ 그 외 상황에서는 읽지 말 것

## 기능 개발 및 디버깅 규칙
- 코드를 수정하기 전, 반드시 상시 로드 문서(1_PRD·2_Architecture)로 기존 아키텍처와 충돌하지 않는지 검토하고,
  **3_Error_Log.txt를 Read 도구로 먼저 읽어** 같은 버그 패턴(D-xx)을 반복하지 않도록 점검하십시오.
- 코드를 수정할 때는 전체를 다시 출력하지 말고 변경된 부분만 반영하십시오.

## 3_Error_Log.txt 작성 규칙
- 이 파일에는 반복 실수 방지를 위한 원칙·패턴·주의사항만 기록합니다.
- 날짜별 작업 내역(N차, 수정 파일 목록 등 변경 이력)은 절대 기록하지 마십시오. 변경 이력은 git log로 확인합니다.
- "작전 종료" 트리거 시 3_Error_Log.txt에 추가할 내용: 새로 발견된 버그 패턴 또는 해결책만 D-xx 형식으로 추가.

### D-xx 기록 자격 기준 (엄격 적용)
  ✅ 기록 대상: 알지 못하면 반드시 빠지는 비직관적 함정, 같은 실수가 재발할 수 있는 버그 원인과 해결책
     예) RLS가 에러 없이 쿼리를 통째로 실패시킴 / React setState 비동기 한계 / 라이브러리 특유의 옵션 함정
  ❌ 기록 제외 (아래 항목은 절대 추가하지 말 것):
     - 기능 구현 방법·설계 결정 (→ 1_PRD.txt 또는 2_Architecture.txt에 기록)
     - 비즈니스 규칙·UI 워딩 규칙 (→ 1_PRD.txt에 기록)
     - 단순 코딩 실수 (변수 누락, URL 오기재 등 — 누구나 바로 알아채는 실수)
     - 기능 완성 내역·변경 이력 (→ git log로 확인)

## 문서 동기화 및 백업 트리거 (필수)
사용자가 채팅창에 "작전 종료" 라고 지시하면, 즉시 코딩을 멈추고 다음 단계를 엄격한 순서대로 자동 실행하십시오.
※ 문서가 분리되어 있으므로 "이번 작업이 건드린 영역의 문서만" 골라서 최신화하십시오. 건드리지 않은 문서는 열지도 수정하지도 마십시오.

1. 변경된 기능·UI 워딩·비즈니스 규칙을 해당 문서에 반영(덮어쓰기):
   - 서비스 개요·인증·공용 기능·공용 라우트 변경 → `1_PRD.txt`
   - /company 포털 기능 변경 → `1_PRD_company.txt`
   - /panel 포털 기능 변경 → `1_PRD_panel.txt`
   - /admin 포털 기능 변경 → `1_PRD_admin.txt`
2. 아키텍처 변경 반영:
   - 기술 스택·폴더 구조·라우트 목록·CSS 유틸·Supabase 최적화 원칙 변경 → `2_Architecture.txt`
   - 특정 페이지/컴포넌트의 state·헬퍼·구현 패턴 변경 → `5_CodeMap.txt` (해당 파일 섹션만)
   - 새 라우트·새 분리 문서 추가 시 1_PRD.txt(공용 라우트)·2_Architecture.txt(라우트 목록)·Sidebar.jsx 동시 갱신 (A-2 규칙)
3. DB 스키마·RPC 함수 변경이 있었다면 `4_DBSchema.txt`도 함께 최신화하십시오.
4. 해결된 에러나 주의사항(D-xx 자격 기준 충족분)을 `3_Error_Log.txt`에 추가하십시오.
   - 환경 특이적·일회성 이슈(랜딩 애니메이션·Windows 스크립트·SQL 편집·가입 파일 업로드)는 `3_Error_Log_archive.txt`에 추가.
   - D 번호는 두 파일 통합 네임스페이스이므로 최신 번호를 이어서 부여하십시오.
5. 새로 발견된 미구현 이슈·재설계 계획이 있으면 `4_Backlog.txt`에 추가하십시오.
6. 각 문서 상단 "최종 업데이트" 날짜를 갱신하십시오.
7. 문서 최신화가 끝나면, 터미널 명령어를 통해 `git add .`, `git commit -m "docs: 문서 자동 동기화"`, `git push origin main`을 순차적으로 실행하여 백업하십시오.
