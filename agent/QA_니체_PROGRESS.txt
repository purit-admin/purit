================================================================
QA_PROGRESS.txt — 미완료 버그 목록 & 스킵/보류 목록
최종 업데이트: 2026-06-06 (기업 포털 전수 조사 — 신규 3건 추가)
※ 수정 완료 항목은 이 파일에서 삭제하고 QA_DONE.txt에 추가할 것
※ 스킵/보류 판정 항목은 하단 섹션으로 이동
================================================================

## 표기 규칙
  🔴 P1  Critical — 기능 전체 동작 불가 / 데이터 미저장 / 보안
  🟡 P2  Major    — 주요 기능 오작동
  🟢 P3  Minor    — 마이너 오작동 / UX 이슈
  ⚪ P4  Dead     — 데드 코드 / 문서 불일치
  ⏭      보류/스킵 — 하단 섹션에 별도 관리

================================================================
## 🔴 P1 Critical (0건)
================================================================

================================================================
## 🟡 P2 Major (4건)
================================================================

[Admin-A] PurityFilter.jsx — 반려 취소(reset) 시 feedbacks DB 변경 후 slot 복원 실패 → 미션 슬롯 카운트 영구 불일치
- 🚨 중요도: P2 — 어드민이 에러 모달을 "취소"로 닫으면 feedbacks.status는 'submitted'인데 missions.filled_count는 감소된 채 방치됨. 미션이 실제보다 빈 슬롯이 1개 더 있는 것으로 보여 패널이 초과 수락 가능.
- 📍 위치: src/pages/admin/PurityFilter.jsx (lines 349-367, reset 함수)
- 🕵️‍♂️ 재현: 반려된 피드백을 "반려 취소" → 확인 클릭 → Supabase `restore_mission_slot` RPC 호출 실패 → 에러 모달 표시 → "돌아가기"로 모달 닫기
- 🤦‍♂️ 현재:
    ```js
    // 1. DB UPDATE 먼저 실행 (성공)
    await supabase.from('feedbacks').update({status:'submitted'...})
    // 2. 로컬 state도 즉시 업데이트
    setFeedbacks(fbs => fbs.map(...status:'submitted'...))
    // 3. slot 복원 RPC (실패 가능)
    const { error: slotErr } = await supabase.rpc('restore_mission_slot', ...)
    if (slotErr) { setResetError('슬롯 복원 실패...'); setActing(false); return; }
    // → setConfirmResetId(null) 미호출, 모달 유지 (D-65 준수)
    // → 그러나 feedbacks는 이미 'submitted'로 변경 완료된 상태
    // → 어드민이 "돌아가기" 클릭 시: DB는 불일치(feedback=submitted, filled_count 낮음)
    ```
- ✨ 기대: 순서를 역전 — restore_mission_slot 먼저 실행, 성공 시에만 feedbacks UPDATE 실행. 또는 두 작업을 단일 RPC로 묶어 원자적 처리.
- 🔍 수평 전개: reject() 함수(line 321~347)도 decrement_mission_filled_count가 fire-and-forget이라 실패 시 슬롯 과잉 문제 가능. 단 reject()는 decrement가 부가 처리라 심각도 낮음.

[Pnl-Active-A] 패널 미션 수행 — handleCancelAccept() D-65 재위반: 이미지 모드·만료·종료 화면에서 수락 취소 오류 무음 처리
- 🚨 중요도: P2 — 취소 버튼을 눌렀는데 DB 실패 시 오류 메시지가 전혀 표시되지 않아 패널이 성공인지 실패인지 알 수 없음
- 📍 위치: src/pages/panel/ActiveMission.jsx (line 485-491, handleCancelAccept)
- 🕵️‍♂️ 재현: 이미지 어노테이션 폼(step≥1)에서 "수락 취소" 버튼 → 모달 열림 → "수락 취소" 클릭 → DB RPC 실패 시
- 🤦‍♂️ 현재:
    ```js
    if (error) { setCancelModal(false); setCancelError('취소 중 오류: ' + error.message); return; }
    ```
    에러 경로에서 setCancelModal(false)로 모달이 닫힘 → 이미지 모드의 cancelError는
    `{cancelModal && <ConfirmModal errorMsg={cancelError} />}` 패턴이라 모달이 닫히면 에러 미렌더.
    deadlineExpired·missionEnded 화면도 동일 구조 → 3개 화면에서 에러 무음 처리.
    (브리핑 step=0은 lines 1018-1022에 standalone cancelError 렌더가 있어 정상)
- ✨ 기대: error 경로에서 setCancelModal(false) 제거 → 모달 유지 + errorMsg로 에러 표시 (D-65 패턴)
    ```js
    if (error) { setCancelError('취소 중 오류: ' + error.message); return; }
    ```
- 🔍 수평 전개: [Pnl-D] 항목에서 이미지 모드에 errorMsg={cancelError} prop을 추가했으나,
    setCancelModal(false)가 에러 경로에 그대로 남아 fix가 무효화됨. [Pnl-D]의 불완전 수정.

[완성도-C] 경력 미설정 패널에게 careerLevels 제한 미션 전부 노출
- 📍 위치: src/pages/panel/Missions.jsx (line 406-416)
- 🤦 현재: panelKey=null(경력 미설정)이면 careerLevels 필터 전체 미적용 → 시니어·C레벨 전용 의뢰 노출.
           accept_mission_slot RPC에 careerLevels 검증 없어 프론트 우회 수락 가능.
- ✨ 기대: 경력 미설정 패널은 careerLevels 없는 레거시 미션만 노출. 또는 junior 등급만 + 경력 설정 유도 배너.

[완성도-D] 핸드폰 OTP 인증 100% Mock — 알림톡 오발송 위험
- 📍 위치: src/pages/panel/Profile.jsx (lines 304-322)
- 🤦 현재: handleSendOtp: setTimeout(800ms)만. SMS 미발송.
           handleVerifyOtp: 길이(6자리)만 체크. 아무 코드나 통과 → phone_verified=true DB 저장.
           가짜 번호가 phone_verified=true가 되면 send-alimtalk / check-deadline-alimtalk Edge Function이
           존재하지 않는 번호로 Solapi 발송 시도 → API 에러 누적.
- ✨ 기대: Edge Function으로 실제 인증번호 발송 + 서버사이드 코드 검증 + 5분 유효시간.
           임시 조치: 번호 형식 정규식 검증(01X-XXXX-XXXX) 추가.




================================================================
## 🟢 P3 Minor (6건)
================================================================

[기업-설정-A] company/Settings.jsx — handleChangeRole() DB 실패 시 에러 메시지 화면 미표시
- 🚨 중요도: P3 — 역할 드롭다운이 갑자기 이전 값으로 되돌아가지만 사용자에게 아무런 안내 없음. 왜 변경이 안 됐는지 알 수 없어 혼란.
- 📍 위치: src/pages/company/Settings.jsx (lines 122-130, handleChangeRole)
- 🕵️‍♂️ 재현: 팀 관리 탭 → 활성 팀원 역할 드롭다운 변경 → DB 업데이트 실패 시 (예: RLS 오류, 네트워크 단절)
- 🤦‍♂️ 현재:
    ```js
    if (error) {
      setMembers(prevMembers); // UI 롤백만 됨
      console.error('[changeRole]', error.message); // 에러 메시지 콘솔에만 출력
    }
    ```
    롤백은 되지만 사용자에게는 아무런 피드백 없음.
- ✨ 기대: roleChangeError state 추가 + UI 인라인 에러 표시:
    ```js
    if (error) {
      setMembers(prevMembers);
      setRoleChangeError('역할 변경 중 오류가 발생했습니다.');
      setTimeout(() => setRoleChangeError(''), 3000);
    }
    ```
- 🔍 수평 전개: handleChangeRole은 isOwner 조건에서만 드롭다운이 표시되므로 RLS 실패 케이스는 드물지만, 네트워크 단절 시 재현 가능.

[기업-버그보고-A] company/BugReports.jsx — 페이지 전환 시 우측 상세 패널 `selected` 상태 미초기화
- 🚨 중요도: P3 — 버그 리포트가 6건 이상일 때, 페이지 1에서 항목 선택 후 다음 페이지로 이동 시 우측 패널에 이전 페이지 항목 상세가 계속 표시됨. 좌측 목록에는 선택 강조 없어 어떤 항목의 상세인지 파악 불가.
- 📍 위치: src/pages/company/BugReports.jsx (line 36)
- 🕵️‍♂️ 재현: 버그 리포트 6건 이상 존재 → 페이지 1에서 임의 항목 클릭 → 페이지 이동 버튼(다음) 클릭 → 우측 상세 패널이 이전 항목 내용 유지
- 🤦‍♂️ 현재:
    ```js
    useEffect(() => { load(); }, [page]); // setSelected(null) 누락
    ```
    load()가 새 reports 배열을 로드하지만 selected 상태(state)는 초기화되지 않음.
- ✨ 기대:
    ```js
    useEffect(() => {
      setSelected(null); // 페이지 전환 시 선택 초기화
      load();
    }, [page]);
    ```
- 🔍 수평 전개: panel/BugReports.jsx도 동일 구조 → 동일 버그 존재 가능. panel/BugReports.jsx 동시 수정 필요.

[Admin-B] PurityFilter.jsx — bulkReject() 슬롯 차감 일부 실패 시 ConfirmModal이 영구 열림 상태로 고착
- 🚨 중요도: P3 — 반려 작업은 완료됐는데 모달이 닫히지 않아 어드민이 혼란 (취소 버튼으로만 닫힘). 재클릭 시 checkedIds가 비어 아무 동작 안 함. 단, rejection_deadline 재설정 및 decrement 재호출 위험은 checkedIds 초기화로 인해 실질적으로 발생하지 않음.
- 📍 위치: src/pages/admin/PurityFilter.jsx (lines 507-508, bulkReject 마지막 부분)
- 🕵️‍♂️ 재현: 일괄 반려 → decrement_mission_filled_count RPC가 일부 실패 → slotFailCount > 0 → return true 없음 → onConfirm의 ok=undefined → setConfirmBulkReject(false) 미호출 → 모달 열린 채 고착
- 🤦‍♂️ 현재:
    ```js
    setCheckedIds(new Set()); setBulkActing(false);
    if (slotFailCount === 0) return true;
    // slotFailCount > 0 이면 return없음(undefined 반환) → 모달 미닫힘
    // 그러나 checkedIds는 이미 비워져 재클릭 시 bulkReject 즉시 early return
    ```
- ✨ 기대: 슬롯 차감 부분 실패 여부와 무관하게 반려 자체가 완료됐으면 모달 닫기. statusError에 경고 메시지 유지.
    ```js
    setCheckedIds(new Set()); setBulkActing(false);
    return true; // 항상 모달 닫기 (slotFailCount 경고는 statusError로 표시)
    ```

[Admin-C] BugReports.jsx — 어드민 역할 버그 제출자에게 알림 발송 시 actionUrl 오류
- 🚨 중요도: P3 — 어드민이 버그/불편을 직접 제출한 경우, 답변 알림의 actionUrl이 '/panel/notifications'로 잘못 설정되어 알림 클릭 시 패널 포털로 이동함.
- 📍 위치: src/pages/admin/BugReports.jsx (lines 160-167, handleSendReply)
- 🕵️‍♂️ 재현: 어드민 계정으로 버그 신고(role='admin') → 어드민이 BugReports 페이지에서 답변 전송 → 알림 발송 → 클릭 시 /panel/notifications로 이동(어드민 페이지가 아님)
- 🤦‍♂️ 현재:
    ```js
    const actionUrl = selected.role === 'company'
      ? '/company/notifications'
      : '/panel/notifications'; // role='admin'도 여기에 걸림
    ```
- ✨ 기대:
    ```js
    const actionUrl = selected.role === 'company'
      ? '/company/notifications'
      : selected.role === 'admin'
      ? '/admin/notifications'
      : '/panel/notifications';
    ```
- 🔍 수평 전개: 어드민이 직접 버그를 제출하는 경우가 많지 않으나, ROLE_LABEL에 'admin'이 정의되어 있어 발생 가능.

[Admin-D] Dashboard.jsx·CompanyManagement.jsx — select('*') 아키텍처 원칙 위반
- 🚨 중요도: P3 — Architecture 규칙 "panels 전체: 명시적 컬럼 select(select('*') 금지)", "companies 전체: 전체 로드 금지" 위반. 패널·기업 수 증가 시 응답 크기 부담.
- 📍 위치:
    · src/pages/admin/Dashboard.jsx (line 39): `supabase.from('panels').select('*').limit(10)` → limit(10)이라 당장 문제 없으나 원칙 위반
    · src/pages/admin/CompanyManagement.jsx (line 429): `supabase.from('companies').select('*')` → 기업 수 증가 시 모든 컬럼 전체 로드
- ✨ 기대: 실제 렌더에 사용하는 컬럼만 명시적으로 select.
    · Dashboard.jsx panels: `select('id, name, industry, trust_score, honor_points, total_missions, status')`
    · CompanyManagement.jsx companies: `select('id, name, industry, website, plan, credit_balance, user_id, created_at')`
- 🔍 수평 전개: PanelManagement.jsx load()는 이미 명시적 컬럼 select 적용됨(line 91). Dashboard·CompanyManagement만 누락.

[완성도-E] AI 리포트 Mock/실제 UI 구분 없음
- 📍 위치: src/pages/company/AIReport.jsx generateReport(), supabase/functions/generate-ai-report/index.ts
- 🤦 현재: ANTHROPIC_API_KEY 미설정 시 로컬 계산 Mock 리포트가 AI 리포트와 동일한 UI로 표시.
- ✨ 기대: Edge Function Mock 폴백 시 isMock:true 반환 → 리포트 상단 "ℹ️ 로컬 분석 기반" 뱃지.


================================================================
## ⚪ P4 Dead Code (1건)
================================================================

[기업-문서-A] 4_DBSchema.txt — missions 테이블 `dismissed` 컬럼 미기재
- 🚨 중요도: P4 — 코드(Dashboard.jsx, NewMission.jsx 등 5개 파일)는 dismissed 컬럼을 실제 사용 중이지만 4_DBSchema.txt에 컬럼 정의 누락. 신규 개발 시 스키마 참조 오류 가능.
- 📍 위치: 4_DBSchema.txt의 [missions] 섹션 (line 71-93)
- 🤦‍♂️ 현재: migrations/056_missions_dismissed.sql에 `dismissed BOOLEAN DEFAULT FALSE` 컬럼이 추가됐으나 문서 미반영.
- ✨ 기대: [missions] 섹션에 아래 한 줄 추가:
    `dismissed BOOLEAN DEFAULT FALSE ← 056 추가 (기업 포털 소프트 삭제 — dismissed=true 시 기업 목록에서 숨김, 어드민은 전체 조회)`

================================================================
## ⏭ 보류·스킵 목록 (재조사 금지)
================================================================

[N-8]  Landing 로그인 사용자 리다이렉트 없음  ⏭
[N-9]  어드민 포털 전환 시 맥락 배너 없음  ⏭

-- 5차 재검증 (2026-05-15) — 시스템 변경으로 해당 없음 판정 --
[5차-A] PurityFilter approve/reject — 기업 알림 누락  ⏭ (어드민 "완료 처리" 시에만 기업에 알림 발송)
[5차-B] PurityFilter bulkApprove/bulkReject — 기업 알림 누락  ⏭ (5차-A와 동일 사유)
[5차-D] company/Dashboard 차트 — active 미션 승인 피드백 제외  ⏭ (PRD 수정됨: 완료 미션 피드백만 집계)
[5차-H] Sidebar useEffect role 의존성 누락 — stale closure  ⏭ (어드민 계정만 포털 전환 가능)
[5차-I] Sidebar 포털 전환 버튼 — navigationGuard 불필요 인터셉트  ⏭ (어드민 계정만 포털 전환 가능)
[5차-O] company/Diagnosis feedbacks — active 미션 승인 피드백 제외  ⏭ (PRD 수정됨: 5차-D와 동일)
[5차-P] company/Dashboard 서브 의뢰 긍정비율 — active 미션 응답 제외  ⏭ (PRD 수정됨: 5차-D와 동일)
[5차-T] company/AIReport feedbacks — active 미션 승인 피드백 AI 분석 제외  ⏭ (PRD 수정됨: 5차-D와 동일)
[5차-U] company/TimelineTracker 시계열 차트 — active 미션 제외  ⏭ (PRD: active 미션은 기업에게 피드백 비공개)

-- 어드민 포털 오판 항목 --
[A-04] admin/Revenue.jsx — loadAll() try-catch 이미 적용됨  ⏭
[A-15] admin/Missions.jsx — deleteMission() error 처리 이미 적용됨  ⏭
[A-16] admin/Missions.jsx — setStatusError('') 초기화 이미 적용됨  ⏭
[A-20] admin/Missions.jsx — deleteMission() D-65 위반 오진단. 실제 코드 이미 적용됨  ⏭
[A-23] admin/Revenue.jsx — --red/--red-dim CSS 변수 미정의 오진단. 실제 정의됨  ⏭

-- 재검증 오판 항목 --
[BUG-NEW-6] admin/Missions.jsx — complete_mission_and_refund 모달 D-65 재검증.
            `const ok = await updateStatus(...); if (ok) setConfirmComplete(null)` 패턴 이미 적용됨.  ⏭

-- 검색 기능 --
[검색-C] admin/Missions.jsx + PurityFilter.jsx — 검색 input 디바운스 없음.
          순수 클라이언트 필터·소규모 데이터로 성능 영향 없어 수정 보류  ⏭

-- 비교 모드 --
[Diag-C2] company/Diagnosis.jsx — toggleMission 4번째 의뢰 선택 후 compareMode 해제 의심.
           실제 코드 확인 결과 이미 처리됨. 재현 불가  ⏭

-- 설계 의도 확인 후 오진단 처리 --
[BUG-VD-1] VerifyDocs.jsx careerChoice 필수 강제 — 의도된 설계. 건강보험=연차 인증, LinkedIn/포트폴리오=직무 인증으로 둘 다 필수. PRD/Architecture 문서 수정 완료.  ⏭

-- OAuth LinkedIn 수정 보류 --
[OAuth-E] signInWithLinkedIn source 파라미터 누락 — LinkedIn 미활성화 상태라 기능적 버그 없음.
          LinkedIn 버튼 활성화 시 적용 필요  ⏭

[OAuth-F] ?signup=google 하드코딩 — 현재 Google 단독 OAuth. LinkedIn 추후 활성화 시 변경 필요  ⏭

[BUG-AUTH-2] AuthContext.jsx LinkedIn OAuth source 저장 누락 — OAuth-E와 동일 사유. LinkedIn 미활성화 상태.
             LinkedIn 기능 구현 시 함께 처리  ⏭


-- 비활성 페이지 조사 스킵 --
[ICPPulse/BrandTracking] 사이드바 비노출 페이지 (PRD 명시: 파일 유지만, 기능 미활성)  ⏭

================================================================
## 📊 현황 요약
================================================================

미완료 합계: 11건
  🔴 P1  0건
  🟡 P2  4건
  🟢 P3  6건
  ⚪ P4  1건

스킵/보류: 22건

완료 항목: QA_DONE.txt 참조 (총 194건)

================================================================
