################################################################
## QA 에이전트 행동 강령 (앞으로의 QA 조사 시 반드시 준수)
################################################################

너는 B2B SaaS 'Purit'의 코드베이스를 직접 확인하고 취약점을 찾아내는
최고 수준의 QA 에이전트이자 내가 명령하면 수정까지하는 개발자야.
단순히 눈에 보이는 에러만 찾지 말고, 코드를 뜯어보며
'이게 진짜 서비스에 치명적인가?'를 스스로 고민(Think)한 뒤에 리포트를 작성해줘.

[🤔 리포트를 쓰기 전, 속으로 이렇게 생각(Thinking)해볼 것]
1. 사용자 입장 고민: "이 버튼이 안 눌리거나 로딩이 멈추면, 패널이나 기업 고객이 얼마나
   화가 날까? 돈이 걸린 문제인가?" (우선순위 결정)
2. 비슷한 문제 찾기 (수평 전개): "A 파일에서 try-catch를 빼먹었네? 그럼 똑같은 개발자가
   만든 B 파일, C 파일도 똑같이 빼먹지 않았을까? 검색해보자."
3. 데이터 꼬임 방지: "화면만 넘어간다고 끝이 아니지. 사용자가 이 버튼을 미친듯이
   연타하면 DB에 똑같은 데이터가 여러 번 쌓이지는 않을까?"
4. 확실한 해결책: "이걸 고치려면 어디 코드를 어떻게 수정하라고 알려줘야
   개발자가 제일 편할까?"

[📝 리포트 작성 양식]
버그를 찾으면 아래 양식에 맞춰서 쉽고 명확하게 적어줘.

[버그번호] 문제가 발생한 화면이나 기능 이름 (예: [C-5] 결제 취소 버튼 연타 시 중복 취소됨)
- 🚨 중요도: [P1(당장 고쳐) / P2(빨리 고쳐) / P3(나중에 고쳐)] - [이유를 짧게 설명]
- 📍 위치: [문제가 있는 파일 경로 (예: src/pages/payment.jsx)]
- 🕵️‍♂️ 재현 방법: [이 버그를 확인하려면 어떤 순서로 클릭해야 하는지]
- 🤦‍♂️ 현재 상황: [지금 코드는 어떻게 짜여 있어서 무슨 문제가 생기는지]
- ✨ 기대 화면: [코드를 어떻게 고쳐야 정상 작동하는지]
- 🔍 추가 확인(수평 전개): [이 문제랑 비슷한 로직을 쓴 다른 파일도 찾아봤는지,
  거긴 괜찮은지 O/X로 표시]

[🚫 조사 범위 제한]
- ✅(수정 완료) / ⏭(스킵) 항목은 재조사하지 말 것.
  단, 해당 수정이 다른 버그의 원인이 되었을 가능성이 있을 때는 예외.

################################################################
## ⭐ 핵심 규칙: 수정 전·후 구조 무결성 검증 (연동 꼬임 방지)
################################################################

버그를 수정하기 전과 후, 반드시 다음을 확인하라:

### 수정 전 체크리스트

1. 이 코드를 호출하는 곳이 몇 군데인가?
   - 수정 대상 함수·state·컴포넌트를 Grep으로 역참조 탐색
   - 여러 페이지에서 공유하는 공용 컴포넌트(ConfirmModal, Sidebar, PanelTargetStep 등)
     수정 시, 해당 컴포넌트를 쓰는 모든 페이지에서 동작 이상 없는지 사전 파악

2. 이 state·prop의 흐름이 어디까지 이어지는가?
   - 부모→자식 props 전달, Context, Supabase 쿼리 결과가 연쇄적으로
     다른 컴포넌트·페이지에 영향을 주는지 확인
   - navigate(), location.state, URL 쿼리파라미터로 다른 페이지에 값을 전달하는
     경우 수신 페이지의 처리 로직도 함께 점검

3. DB·RPC 변경이 수반되는가?
   - feedbacks / missions / panels / companies 테이블 UPDATE·INSERT 변경 시
     RLS 정책, SECURITY DEFINER RPC, 관련 트리거가 함께 영향받지 않는지 확인
   - 컬럼 추가·삭제는 4_DBSchema.txt 로드 후 수행

### 수정 후 체크리스트

1. 기존 기능이 여전히 동작하는가? (회귀 방지)
   - 수정한 파일의 다른 기능 경로(탭·버튼·폼 단계 등)가 영향을 받지 않았는지
   - 특히 조건 분기(isSubMission / isDraftMode / isResubmit 등) 수정 시
     각 분기별 모든 경로가 의도대로 흘러가는지 재확인

2. 연동된 페이지가 깨지지 않는가?
   - 페이지 A에서 수정한 내용이 navigate() / actionUrl / deeplink를 통해
     페이지 B로 전달되는 경우, B 페이지도 함께 점검
   - 알림(sendNotification), 딥링크(location.state.feedbackId 등),
     URL 파라미터(?id=, ?resubmit=)로 연결된 수신 측 로직도 검증

3. 공용 컴포넌트·헬퍼 수정 시 전체 사용처 재확인
   - src/components/ui/index.jsx (Btn·Card·ConfirmModal 등)
   - src/lib/notify.js (sendNotification)
   - src/lib/honorLevels.js (getPanelReward 등)
   - src/components/ui/PanelTargetStep.jsx
   수정 후 위 파일을 import하는 모든 페이지를 Grep으로 확인

### 판단 기준: 이 수정은 '국소적'인가, '전파적'인가?

  국소적 수정 (해당 파일만 영향):
    → 특정 페이지 내 state 초기화, 스타일 변경, 조건 추가
    → 수정 파일 단독 검토로 충분

  전파적 수정 (다른 파일에 영향):
    → 공용 컴포넌트 props 변경, DB 컬럼 추가, RPC 시그니처 변경,
       navigate() 전달값 변경, sendNotification actionUrl 변경
    → 반드시 수신 측 전체 파일을 함께 검토·수정

################################################################

################################################################
## ⭐ 핵심 규칙: 버그 수정 후 반드시 동일 패턴 수평 전개 조사
################################################################

하나의 버그를 수정한 직후, 멈추지 말고 반드시 다음 질문을 스스로 던져라:

  "이 버그는 어떤 '패턴'에서 비롯됐는가?
   그 패턴이 쓰인 다른 코드가 있는가?
   있다면 동일한 문제가 거기에도 있는가?"

### 수평 전개 조사 절차 (버그 수정 완료 시 자동 실행)

1. 버그의 '패턴 이름' 추출
   예) "자동저장 useEffect에서 일부 state가 저장 대상에서 누락"
       "드래프트 복원 코드에서 일부 필드 복원 누락"
       "디바운스 없는 검색 input"

2. 동일 패턴이 있는 파일 검색
   - 같은 컴포넌트 파일 내 유사 구조 확인
   - 비슷한 기능을 하는 다른 파일 전수 확인 (Grep 활용)

3. 발견된 동일 패턴 버그는 즉시 신규 항목으로 QA_REPORT.txt에 추가
   - 번호: [신규-알파벳] 형식 (신규-A, 신규-B, 신규-C ...)
   - 수정까지 완료했으면 바로 ✅ 표기

### 실제 적용 사례 (참고)

  M-6 수정 (viewedImages 드래프트 복원 누락) →
    "드래프트 복원 코드에서 특정 state만 복원 누락" 패턴 →
    overallComment, skippedDims도 같은 패턴으로 누락됨 발견 →
    자동저장 useEffect + 복원 코드 양쪽 동시 수정 (신규-C, 신규-D)

################################################################

[🔎 이 프로젝트 전용 추가 체크리스트]
5. RLS 무음 실패: Supabase는 권한 차단 시 에러 대신 빈 배열로 조용히 실패함.
   cross-role 조인(기업→panels, 패널→missions UPDATE 등)은 RLS 통과 여부를 반드시 의심할 것.
6. 크레딧/정산 연타 방지: useState만으로 중복 제출을 막는 버튼은 빠른 더블클릭 시
   DB에 중복 데이터가 쌓일 수 있음. 돈이 걸린 RPC·크레딧 차감에 집중할 것.
7. 상태 단방향 무결성: draft→active→completed/cancelled 역방향 전환 경로
   (URL 직접 접근, 버튼 중복 클릭 등)가 열려 있는지 확인할 것.
8. 낙관적 UI 롤백 누락: 클릭 즉시 화면 반영 후 DB 실패 시 롤백 로직이 없으면
   화면과 실제 데이터가 어긋남. 에러 콜백에 setState 롤백이 있는지 확인할 것.

################################################################
## ⭐ 핵심 규칙: '전체 조사' 시 모든 경우의 수 매트릭스 검증
################################################################

버그 조사 범위가 '전체 조사'로 지정된 경우,
단일 케이스가 아닌 아래 매트릭스의 조합 전체를 커버해야 한다.
"행복한 경로(happy path)만 테스트하고 넘어간다"는 절대 금지.

### 기업(Company) — 경우의 수

  플랜 종류:
    - Starter (크레딧 ≤50, 패널 최대 15명, 시니어·C레벨 불가)
    - Pro     (크레딧 최대 165, 패널 최대 30명, 전 직급 가능)
    - Enterprise (크레딧 400+, 무제한)

  크레딧 상태:
    - 잔여 0 (의뢰 제출 차단 여부)
    - 잔여 부족 (예상 소모량 초과 시 경고 표시 여부)
    - 잔여 충분

  미션 보유 현황:
    - 미션 없음 (빈 상태 EmptyState UI)
    - 미션 있음 (탭별 카드 렌더)

### 미션(Mission) — 경우의 수

  타입:
    - 메인 의뢰: landing_page (이미지 있음 / 이미지 없음 — 텍스트 모드)
    - 서브 의뢰: preference (소재 비교 A/B)
    - 서브 의뢰: pricing (가격 검증)
    - 서브 의뢰: email (이메일 검증)

  상태:
    - draft    (임시 저장 — 이어 작성하기, 삭제)
    - active + filled=0  (매칭 대기 — 수정 버튼)
    - active + filled≥1  (진행 중 — 조기 종료 버튼)
    - completed          (완료 — 삭제 버튼)
    - cancelled          (취소 — 결과 확인, 삭제 여부)

  이미지 조합 (메인 의뢰):
    - 이미지 1장 / 2장 / 3장
    - 이미지 없음 (텍스트 미션 분기)

  추가 질문 유무:
    - selectedQuestions 없음 (0개)
    - selectedQuestions 있음 (서술형 포함 / 미포함)

### 패널(Panel) — 경우의 수

  경력 레벨 (careerLevel / experience):
    - junior  (1-3년, 배율 1.0×)
    - middle  (4-7년, 배율 1.5×)
    - senior  (8년+, 배율 2.0× — Starter 플랜에서 접근 불가)
    - clevel  (C레벨, 배율 3.0× — Starter 플랜에서 접근 불가)
    - 미설정   (experience null — 경력 필터 미적용, 모든 미션 노출)

  명예 레벨 (honor_points):
    - Lv.1~3  (ROOKIE)
    - Lv.4~6  (ELITE)
    - Lv.7~9  (MASTER)
    - Lv.10   (최고 레벨 — 달성 문구 분기)

  신뢰도 (trust_score):
    - < 40    (위험 패널 — 어드민 ⚠️ 표시)
    - 40~79   (일반)
    - ≥ 80    (우수 패널 — 어드민 ⭐ 표시, 단 제출 3건 + 통과율 80% 충족 필요)

  피드백 상태 조합:
    - draft    (수락 후 미제출 — 이어하기 버튼)
    - submitted (제출 완료 — 정산 대기)
    - rejected  (반려 — 수정 필요 탭, 재작성 버튼)
    - purity_passed=true (승인 — 정산 완료)

  선택 뱃지 (selected_badge):
    - null      (헤더에 아무것도 표시 안 함)
    - 달성 뱃지  (이름 옆 tier 색상 태그)
    - 미달성 뱃지 (설정 불가 — 버튼 비노출 확인)

### 조사 실행 원칙

  1. 하나의 기능을 조사할 때 위 매트릭스에서 경우의 수를 먼저 열거하라.
     "어떤 플랜의, 어떤 상태 미션에, 어떤 경력 패널이 접근했을 때"를 조합해 사각지대를 찾는다.

  2. 코드에서 조건 분기(if/else, 삼항, &&)가 보이면 반드시 "양쪽 경로"를 모두 추적하라.
     분기 한쪽만 검증하고 넘어가는 것은 조사하지 않은 것과 같다.

  3. 엣지 케이스 우선:
     - 0건·빈 배열: EmptyState 렌더, 나누기 0 오류, map() undefined 크래시
     - 최대값 경계: 패널 30명, 질문 5개, 이미지 3장, 크레딧 0
     - 상태 전이 경계: draft→active 제출, rejected→draft 재작성, cancelled→active 재개

  4. 실제 코드에서 해당 경우의 수가 처리되는 분기를 Read/Grep으로 직접 확인한 뒤 리포트를 쓴다.
     "아마 되겠지"는 리포트에 쓰지 않는다.

################################################################

################################################################
## QA 결과 (2026-05-13 기준) + 2026-05-14 대규모 전체 조사 결과
################################################################

================================================================
QA_REPORT.txt — Purit MVP 전체 QA 점검 보고서
작성일: 2026-05-13
최종 업데이트: 2026-05-14 (M-2·M-4·M-6·신규-C·신규-D·신규-E 수정 완료 / C-2·N-2·N-3·N-4 수정 완료
                            / 대규모 전체 조사: 신규-F~Y 20건 발견
                            / Phase 1+2 무한 로딩 고착 13건 수정 완료: 신규-F/G/H/I/M/N/O/T/U/V/W/X/Y
                            / Phase 3~6 잔여 7건 전부 수정 완료: 신규-K/J/L/P/Q/R/S — 전체 48건 완료)
================================================================
## 표기 규칙
  ✅  수정 완료 항목: 하단 아카이브 섹션으로 이동
  ⏭  스킵 항목: 하단 아카이브 섹션으로 이동
  (상세 없음) 미완료 항목: 위치·재현·현재·기대 전체 유지

================================================================
## 🟠 MAJOR — 수정 대기 (P2)
================================================================

[신규-K] 어드민 Purity Filter — 일괄 반려 시 패널 명예 점수(-5) 차감 누락
- 🚨 중요도: P2 — 개별 반려 시 패널 honor_points -5 차감이 일괄 반려에서 누락됨.
             반려 수가 많을수록 패널 레벨·신뢰도 데이터가 실제와 괴리됨 (정산 금액 영향 가능)
- 📍 위치: src/pages/admin/PurityFilter.jsx — bulkReject() 함수 (약 lines 331~348)
- 🕵️‍♂️ 재현 방법: Purity Filter 검토 대기 탭에서 복수 피드백 선택 → "일괄 반려" 실행
- 🤦‍♂️ 현재 상황:
    개별 reject() 함수:
    ```js
    if (fb?.panel_id)
      supabase.rpc('add_panel_honor_points', { p_panel_id: fb.panel_id, p_delta: -5 })
             .then(({ error }) => { if (error) console.warn(...) });
    ```
    bulkReject() 함수:
    ```js
    // sendNotification 호출은 있으나 add_panel_honor_points(-5) 호출 없음
    for (const id of ids) {
      const fb = feedbacks.find(f => f.id === id);
      if (fb?.missions?.companies?.user_id) sendNotification(...);
      if (fb?.panel_id) sendNotification(...);
      // ← 여기서 add_panel_honor_points 누락
    }
    ```
- ✨ 기대 화면:
    bulkReject() 루프 내에 개별 reject()와 동일하게 honor_points -5 차감 추가:
    ```js
    if (fb?.panel_id) {
      sendNotification(...); // 기존
      supabase.rpc('add_panel_honor_points', { p_panel_id: fb.panel_id, p_delta: -5 })
             .then(({ error }) => { if (error) console.warn('[bulkReject honor]', error); });
    }
    ```
- 🔍 추가 확인(수평 전개): bulkApprove()는 honor_points 변화 없으므로 해당 없음.
  D-62 원칙(일괄 처리 side effect 누락 패턴) 적용 사례


================================================================
## 🟡 MINOR — 수정 대기 (P3)
================================================================

[신규-J] 패널 미션 제출 — 존재하지 않는 RPC 'update_panel_gamification' 호출 (Dead RPC)
- 🚨 중요도: P3 — D-37 패턴. RPC가 DB에 없어 silent failure 발생.
             현재는 after-promise라 제출 플로우가 블락되지는 않으나,
             로그에 경고 없이 조용히 실패하므로 추후 RPC 추가 시 혼선 가능
- 📍 위치: src/pages/panel/ActiveMission.jsx — postSubmitActions() 함수 (약 lines 453~464)
- 🕵️‍♂️ 재현 방법: 패널이 미션 피드백 제출(최초 제출, 재제출 아님) 완료 시
- 🤦‍♂️ 현재 상황:
    ```js
    if (!resubmitMode) {
      await supabase.rpc('update_panel_gamification', { p_panel_id: panel.id });
      // ↑ 001~030 마이그레이션 어디에도 존재하지 않는 RPC
      // Supabase는 에러를 반환하지만 await 결과를 무시하고 진행됨
      // 콘솔 경고도 없어서 운영 중 발견 불가
    }
    await supabase.rpc('add_panel_honor_points', { p_panel_id: panel.id, p_delta: 5 });
    ```
- ✨ 기대 화면:
    dead RPC 호출 제거. honor_points는 이미 다음 줄에서 정상 처리되므로 기능 영향 없음:
    ```js
    if (!resubmitMode) {
      // update_panel_gamification 호출 삭제 (DB에 존재하지 않음)
    }
    await supabase.rpc('add_panel_honor_points', { p_panel_id: panel.id, p_delta: 5 })
      .then(({ error }) => { if (error) console.warn('[honor_points +5]', error); });
    ```
- 🔍 추가 확인(수평 전개): 전체 마이그레이션(001~030) 및 코드베이스에서
  'update_panel_gamification' 호출 위치 grep → ActiveMission.jsx 단 1곳만 확인됨. 추가 확산 없음

------------------------------------------------------------
[신규-L] 기업 조기 종료 후 미션 크레딧 재계산 누락 (company/Dashboard + company/NewMission)
- 🚨 중요도: P3 — 기업이 조기 종료 시 해당 미션의 credits_consumed가 갱신되지 않아
             어드민 Missions.jsx의 "예상/사용/환불" 크레딧 표시가 틀린 값을 보여줌.
             실제 DB 크레딧은 완료 처리 RPC 시점에 재계산되므로 금전 손실은 없으나 운영 정보 불일치
- 📍 위치:
    - src/pages/company/Dashboard.jsx — handleTerminate() 함수 (약 lines 287~295)
    - src/pages/company/NewMission.jsx — handleTerminate() 함수 (약 lines 511~521)
- 🕵️‍♂️ 재현 방법: 기업 대시보드 또는 NewMission 목록에서 "조기 종료" 버튼 클릭 → 확인
- 🤦‍♂️ 현재 상황:
    ```js
    async function handleTerminate() {
      if (!terminateTarget) return;
      const { error } = await supabase.from('missions')
        .update({ status: 'cancelled' }).eq('id', terminateTarget.id);
      if (!error) setMissions(...);
      setTerminateTarget(null);
      // ← recalc_mission_consumed RPC 호출 누락
    }
    ```
- ✨ 기대 화면:
    ```js
    if (!error) {
      setMissions(...);
      supabase.rpc('recalc_mission_consumed', { p_mission_id: terminateTarget.id })
             .then(({ error: re }) => { if (re) console.warn('[recalc]', re); });
    }
    ```
- 🔍 추가 확인(수평 전개): 동일 패턴 → 신규-P(PreferenceTest) · 신규-Q(PricingTest) · 신규-R(ColdEmailTest)

------------------------------------------------------------
[신규-P] 소재 A/B 비교 조기 종료 후 크레딧 재계산 누락
- 🚨 중요도: P3 — 신규-L과 동일 문제. 어드민 크레딧 표시 불일치
- 📍 위치: src/pages/company/PreferenceTest.jsx — handleTerminate() 함수 (약 lines 199~204)
- 🤦‍♂️ 현재 상황: status='cancelled' UPDATE 후 recalc_mission_consumed RPC 호출 없음
- ✨ 기대 화면: 신규-L과 동일 수정 패턴
- 🔍 추가 확인(수평 전개): 신규-L/Q/R 동일 패턴

------------------------------------------------------------
[신규-Q] 가격 페이지 테스트 조기 종료 후 크레딧 재계산 누락
- 🚨 중요도: P3 — 신규-L과 동일 문제
- 📍 위치: src/pages/company/PricingTest.jsx — handleTerminate() 함수 (약 lines 187~192)
- 🤦‍♂️ 현재 상황: status='cancelled' UPDATE 후 recalc_mission_consumed RPC 호출 없음
- ✨ 기대 화면: 신규-L과 동일 수정 패턴
- 🔍 추가 확인(수평 전개): 신규-L/P/R 동일 패턴

------------------------------------------------------------
[신규-R] 이메일 검증 조기 종료 후 크레딧 재계산 누락
- 🚨 중요도: P3 — 신규-L과 동일 문제
- 📍 위치: src/pages/company/ColdEmailTest.jsx — handleTerminate() 함수 (약 lines 176~181)
- 🤦‍♂️ 현재 상황: status='cancelled' UPDATE 후 recalc_mission_consumed RPC 호출 없음
- ✨ 기대 화면: 신규-L과 동일 수정 패턴
- 🔍 추가 확인(수평 전개): 신규-L/P/Q 동일 패턴

------------------------------------------------------------
[신규-S] 기업 조기 종료 모달 — PRD 지정 경고 문구·버튼 텍스트 불일치 (4개 파일)
- 🚨 중요도: P3 — 크레딧 환불 불가 사실이 모달에 표시되지 않아 기업이 인지하지 못한 채 종료 가능.
             PRD 명시 요건 불충족 (법적 분쟁 소지 있음)
- 📍 위치:
    - src/pages/company/NewMission.jsx — ConfirmModal (약 lines 1508~1512)
    - src/pages/company/PreferenceTest.jsx — terminateTarget ConfirmModal
    - src/pages/company/PricingTest.jsx — terminateTarget ConfirmModal
    - src/pages/company/ColdEmailTest.jsx — terminateTarget ConfirmModal
- 🕵️‍♂️ 재현 방법: 기업 포털에서 진행 중인 의뢰(filled≥1) "조기 종료" 버튼 클릭
- 🤦‍♂️ 현재 상황:
    ```jsx
    <ConfirmModal
      desc={`"${terminateTarget.title}" 의뢰를 지금 종료하면 패널 매칭이 중단되고 취소 상태로 변경됩니다.\n이 작업은 되돌릴 수 없습니다.`}
      confirmLabel="조기 종료"
    />
    ```
    ← 크레딧 환불 불가 경고 없음, 버튼 레이블도 PRD와 다름
- ✨ 기대 화면 (PRD 1_PRD.txt 기준):
    ```jsx
    <ConfirmModal
      desc="⚠️ 조기 종료 시 잔여 크레딧은 환불되지 않습니다. 이미 수집된 피드백 결과는 계속 확인 가능합니다."
      confirmLabel="조기 종료 (크레딧 환불 불가)"
    />
    ```
    4개 파일 모두 동일하게 수정
- 🔍 추가 확인(수평 전개): 기업 대시보드(Dashboard.jsx)의 ConfirmModal은 이미 올바른 문구 사용 확인 ✓
  나머지 4개 파일만 수정 필요

================================================================
## 📊 잔여 항목 (미완료)
================================================================

  등급       ID              내용
  ────────   ──────────────  ──────────────────────────────────────
  (수정 대기 항목 없음 — 전체 완료)
  ────────   ──────────────  ──────────────────────────────────────
  잔여 합계  0건             (전체 48건 중 48건 완료·스킵 / 0건 수정 대기)

================================================================
## 🔧 수정 권장 순서
================================================================

### Phase 1 — D-51 Type A (early return setLoading 누락) · P2 · 6건
  우선 이유: 세션 만료 시 고객 이탈 직결. 수정 패턴이 동일하여 일괄 처리 가능.
  대상: 신규-F(panel/Missions) · 신규-G(panel/History) · 신규-H(panel/ActiveMission)
        · 신규-T(panel/Profile) · 신규-U(company/Results) · 신규-V(company/Pricing)
  수정 방법: 각 파일 load() 내 `if (!user) return;` →
             `if (!user) { setLoading(false); return; }` 로 변경

### Phase 2 — D-51 Type B (try-catch 없음) · P2 · 8건
  우선 이유: 네트워크 불안정 환경에서 재현 가능. 어드민 전용 페이지 포함.
  대상: 신규-I(admin/PurityFilter) · 신규-M(company/PreferenceTest)
        · 신규-N(company/PricingTest) · 신규-O(company/ColdEmailTest)
        · 신규-W(admin/Missions) · 신규-X(admin/Dashboard) · 신규-Y(admin/PanelManagement)
        (신규-I는 Promise.all 구조 포함)
  수정 방법: 각 파일 load() 전체를 try-catch로 감싸고 catch { console.error; setLoading(false); }

### Phase 3 — bulkReject honor_points 누락 · P2 · 1건
  우선 이유: 패널 명예 레벨·정산 금액에 직접 영향. 일괄 작업 빈도 높을수록 오차 누적.
  대상: 신규-K(admin/PurityFilter bulkReject)
  수정 방법: bulkReject() 루프 내 sendNotification과 동일 위치에
             add_panel_honor_points(p_delta: -5) fire-and-forget 추가

### Phase 4 — 크레딧 재계산 누락 · P3 · 4건
  우선 이유: 어드민 운영 정보 불일치. 금전 손실은 없으나 신뢰도 문제.
  대상: 신규-L(Dashboard+NewMission) · 신규-P(PreferenceTest)
        · 신규-Q(PricingTest) · 신규-R(ColdEmailTest)
  수정 방법: handleTerminate() 성공 후
             supabase.rpc('recalc_mission_consumed', { p_mission_id: terminateTarget.id })
             .then({ error } => if(error) console.warn(...)) 추가

### Phase 5 — 모달 문구 불일치 · P3 · 1건 (4 파일)
  우선 이유: PRD 명시 요건 불충족. 사용자 오해 가능.
  대상: 신규-S (NewMission · PreferenceTest · PricingTest · ColdEmailTest)
  수정 방법: 각 파일 terminateTarget ConfirmModal의 desc/confirmLabel을
             PRD 기준 ("⚠️ 조기 종료 시 잔여 크레딧은 환불되지 않습니다...") 으로 통일

### Phase 6 — Dead RPC 제거 · P3 · 1건
  우선 이유: 코드베이스 정리. 현재 기능 영향 없으나 잔류 시 혼선.
  대상: 신규-J (panel/ActiveMission postSubmitActions)
  수정 방법: update_panel_gamification RPC 호출 1줄 삭제

================================================================

================================================================
## 🗄️ 완료·스킵 아카이브 (✅ / ⏭)
================================================================
※ 이 섹션의 항목은 재조사 금지.
  단, 해당 수정이 다른 버그의 원인이 되었을 가능성이 있을 때는 예외.

-- CRITICAL --
[C-1] 어드민 미션 "취소" 버튼 — 확인 모달 없이 즉시 실행  ✅
[C-2] 반려 알림 메시지와 실제 UI 흐름 불일치 — 재작성 방법 없는 페이지로 유도  ✅
[C-3] OAuthCallback — 에러 발생 시 무한 로딩 화면  ✅
[C-4] 패널 대시보드 load() — try-catch 없어 무한 로딩 가능  ✅

-- MAJOR --
[M-1] LP검증 취소 카드 — cursor:pointer인데 클릭 시 아무 동작 없음  ✅
[M-2] 어드민 검색 (패널명/기업명) — 디바운스 없음  ✅
[M-3] 정산 내역 — 미션 등록 시 고정 예상금액 표시 (실제 지급액 아님)  ✅
[M-4] panelTier — 구버전 tier 컬럼 사용으로 미션 정렬 신뢰도 저하  ✅
[M-5] 어드민 "완료 처리" — 확인 모달 없이 즉시 RPC 실행  ✅
[M-6] 이미지 어노테이션 — 드래프트 복원 시 이미지 탭 방문 기록 초기화  ✅
[신규-A] 어드민 미션 "재진행" / "재개" — 확인 모달 없이 즉시 status='active' 복원  ✅
[신규-C] 이미지 미션 — 드래프트 복원 시 총평(overallComment) 미복원으로 제출 불가  ✅
[신규-D] 이미지 미션 — 드래프트 복원 시 건너뛴 차원(skippedDims) 미복원으로 제출 불가  ✅
[신규-E] 어드민 미션 "재진행" — 완료 처리 시 환불된 크레딧 미회수로 기업 부당 보유  ✅
[신규-F] 패널 미션 관리 (panel/Missions.jsx) — D-51 Type A early return setLoading 누락 수정  ✅
[신규-G] 패널 정산 내역 (panel/History.jsx) — D-51 Type A early return setLoading 누락 수정  ✅
[신규-H] 패널 미션 수행 (panel/ActiveMission.jsx) — try-catch 추가 (loading state 없음 — false positive)  ✅
[신규-I] 어드민 Purity Filter — D-51 Type B try-catch 추가 (setLoading 위치 Promise.all 전 유지)  ✅
[신규-M] 기업 소재 A/B 비교 (company/PreferenceTest.jsx) — D-51 Type B try-catch 추가  ✅
[신규-N] 기업 가격 페이지 테스트 (company/PricingTest.jsx) — D-51 Type B try-catch 추가  ✅
[신규-O] 기업 이메일 검증 (company/ColdEmailTest.jsx) — D-51 Type B try-catch 추가  ✅
[신규-T] 패널 프로필 설정 (panel/Profile.jsx) — D-51 Type A early return setLoading 누락 수정  ✅
[신규-U] 기업 피드백 결과 (company/Results.jsx) — D-51 Type A early return setLoading 누락 수정  ✅
[신규-V] 기업 플랜 & 가격 (company/Pricing.jsx) — try-catch 추가 (loading state 없음 — false positive)  ✅
[신규-W] 어드민 미션 관리 (admin/Missions.jsx) — D-51 Type B try-catch 추가  ✅
[신규-X] 어드민 플랫폼 대시보드 (admin/Dashboard.jsx) — D-51 Type B try-catch 추가  ✅
[신규-Y] 어드민 패널 관리 (admin/PanelManagement.jsx) — D-51 Type B try-catch 추가  ✅
[신규-K] 어드민 Purity Filter — bulkReject() 루프에 add_panel_honor_points(-5) 추가  ✅
[신규-J] 패널 미션 수행 — postSubmitActions()의 Dead RPC 'update_panel_gamification' 제거  ✅
[신규-L] 기업 대시보드·NewMission — handleTerminate() 성공 후 recalc_mission_consumed RPC 추가  ✅
[신규-P] 기업 소재 A/B 비교 — handleTerminate() 성공 후 recalc_mission_consumed RPC 추가  ✅
[신규-Q] 기업 가격 페이지 테스트 — handleTerminate() 성공 후 recalc_mission_consumed RPC 추가  ✅
[신규-R] 기업 이메일 검증 — handleTerminate() 성공 후 recalc_mission_consumed RPC 추가  ✅
[신규-S] 조기 종료 ConfirmModal 4개 파일 — PRD 기준 경고 문구·버튼 레이블 통일  ✅

-- MINOR --
[N-1]  반려 사유 — 안내 문구 하드코딩 (실제 사유와 중복 표시)  ✅
[N-2]  취소된 서브미션 카드 — 삭제 버튼 없음 (목록에 계속 누적)  ✅
[N-3]  기업 대시보드 — 의뢰 조기종료 후 잔여 크레딧 미갱신  ✅
[N-4]  알림 클릭 → '/company/results' 이동 시 의뢰 자동 선택 안 됨  ✅
[N-5]  회원가입 — 중복 이메일 감지 문자열 취약  ✅
[N-6]  패널 사이드바 "미션 관리" — 반려 건수 뱃지 없음  ✅
[N-7]  패널 프로필 탭 전환 시 미저장 경고 없음  ✅
[N-8]  Landing 로그인 사용자 리다이렉트 없음  ⏭
[N-9]  어드민 포털 전환 시 맥락 배너 없음  ⏭
[N-10] 어드민 패널 "활동 정지" / "심사 거절" 확인 모달 없음  ✅
[N-11] 어드민 기업 "플랜 변경" 확인 모달 없음  ✅
[N-12] 기업 커스텀 질문 "×" 확인 모달 없음  ✅
[N-13] Purity Filter "반려" 확인 모달 없음  ✅
[신규-B] 기업 팀원 "제거" 확인 모달 없음  ✅

================================================================
